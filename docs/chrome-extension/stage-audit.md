# 阶段完成证据审计

最新审计日期：2026-09-26，扩展0.1.91；最近全仓集成基线为`918996c`／0.1.87。第一、三节维护当前状态；第二节保留2026-09-25、`5a93cda`／0.1.48的初次审计记录。历史分项通过不等于完整目标已完成。

## 一、六阶段状态

| 阶段 | 已取得证据 | 尚缺的完整验收 |
| --- | --- | --- |
| 站点接入 | 制作、Data、条件编辑、导入、背包和模拟器均有分项公开DOM及实装记录 | 0.1.54已确认Data物品框可检索材料，独立制作材料框仍未确认；每种输入的全部事件／导航／语言矩阵不完整 |
| 术语核心 | 显示、检索、领域隔离、来源哈希、真实词典灰区关闭均可验证 | 初次TDD流程记录本轮未追溯；游戏名称版本未知项仍明确标unknown，不能宣称所有译名版本完整 |
| 可安装显示层 | 双入口构建、最小权限、资源检查、启停、原文更新与复制节点恢复均有证据；0.1.49自制图标通过Chrome加载；0.1.73新增设置读取重试，0.1.79／0.1.81补搜索名称与用途变化清理，0.1.87补六节流程教程和三态逐节点验收 | 0.1.58补齐首页／新版说明页双标签设置同步、双语重载及德语窄窗提示恢复；完整页面／模式矩阵仍缺 |
| 中文搜索 | 基底、词缀、Data、条件候选与键盘操作有实装证据 | Linux Fcitx5的Data、制作基底及词缀查询已验收；条件点击失效已复现为自动化点击越出视口，滚动后选择及停用对照通过；其他输入法与完整矩阵仍待验收；0.1.54已实测Data崇高石检索，0.1.60补齐价格页材料领域与崇高石对照；0.1.78补四种特殊符文中文查询、原生身份和用途HTML对照；全部材料／用途对照链未完成 |
| 高级装备导入 | 十种基底的指定结构，以及法器复合／独立魔力已有原生结果对照；0.1.76／0.1.80补三种首饰的生命与元素抗性组合，各六组中文／英文导出逐字一致；0.1.83新增黄玉／紫晶六组原生对照；0.1.88新增破碎手套三组 | 0.1.50确认环锁腰带咒符位原站拒绝／丢失并加入提示；权杖四组显式属性已核对，但原站不保留输入当前技能等级（见sceptre-import-audit.md）；普通武器和其他首饰、特殊结构仍不完整；不等于用户全部样本可制作 |
| 完整链与分发 | 本地ZIP、安装文档、若干保存／载入／撤销、多步互斥路线已验收 | 0.1.60已补并列组增删导出往返；组内嵌套入口尚未确认，完整条件／回边等流程、CI远端运行仍未完成；0.1.87再次通过标准全仓verify：582文件、4893项通过，1项既有本地样本跳过；ZIP与构建及交付副本一致 |

旧自研制作网站继续搁置。CI工作流的既有推送凭证限制只阻塞该工作流，不阻塞本地功能开发；不据此将整个目标标为blocked。

## 二、0.1.48初次审计记录

- `packages/l10n-core/src/index.test.ts`与扩展`build-dictionary.test.mjs`：2文件26项通过。覆盖ID冲突、领域隔离、未知项、正负号、小数、范围、数值顺序、固定数字、精确／别名／部分候选、50项排序与范围限制；构建测试确实包含`.mjs`，没有漏跑。
- 独立`tsc --noEmit -p packages/l10n-core/tsconfig.json`通过。检查核心源文件，未引入DOM、Chrome API、文件访问或制作引擎。
- 用真实入库源先完整构建，再在同一个临时输出路径关闭gray重建：6880→4892条，1487195→1179549字节；结果无gray来源、无以gray路径为来源的残留词条。剩余领域为stat、item、ui，base／unique会随gray数据移除。这是降级行为证据，不是“关掉gray后功能覆盖不变”的承诺。
- 真实材料名称来源沿用已登记catalog的名称子集，版本未知时写unknown；不带其制作规则进扩展词典。游戏文本权利和MIT自有代码范围继续按data-sources.md及NOTICE区分。
- manifest与package-check：2文件5项通过；当前dist再次执行check通过，0.1.48、6880条、仅storage权限。检查`build.mjs`的两个独立Vite入口、关闭第二次清空、内容IIFE、popup资源和根脚本；不把产物检查等同于所有页面实装通过。
- `git show d426303`确认核心、测试和构建器的实际交付提交。标题为“实现新版 CoE 简体 Chrome 扩展开发预览”，不是计划里原拟的单独核心标题。
- 仓库级skills仍在`.agents/skills/`，含vendor-lock与各自许可证；没有新增全局安装。

测试通过只证明对应断言。未以本轮31项针对性测试替代上轮133项回归或全仓verify，也未以有测试文件推断最初编写顺序。

## 三、后续施工顺序

1. 已于0.1.49补齐自制扩展图标、manifest引用、构建复制与打包验证；最终ZIP在Chrome管理页显示正常，四种尺寸解码通过。原审计基线保留在第二节，新增证据见compatibility.md。
2. 环锁腰带咒符位已确认原站兼容性缺口（0.1.50），不删行放行；权杖技能／复合词缀已核对，当前等级未保留，暂不放行；后续继续其他类别，并将允许原站差异的提交模式作为独立设计问题；每种新增能力先查明原站输入与原生导出，不从普通法器直接泛化。
3. 0.1.52已用真实键盘将组内匹配数量保存为1；英文原站与最终中文包完整导出一致，均运行完成10件且第一步可直接结束。0.1.53已补显式回边建立、起点选择、运行和手动停止；无动作回边未自动结束，0.1.54在全新无扩展会话复现普通等阶与属性阈值未保存，0.1.69用失焦后等待一秒的对照验证普通等阶及渎灵前缀数值均可保存，快速操作在无扩展原站也可保留旧值；同轮后续已验证能量护盾41／43阈值分别进入两条分支且与无扩展运行一致，其他属性路径仍待复核。0.1.74检查的原生编辑器只确认并列组，尚未发现嵌套入口；后续先核实当前UI是否提供此能力，不自行发明上游结构。0.1.86补两条同时成立路线的优先级及保存载入对照，首轮异常后续定位为自动化未命中箭头，见priority-click-audit.md。继续其他属性、多路线及回边矩阵；保留已知原站步骤复制异常。
4. 0.1.54已确认Data物品搜索可查询材料，并完成崇高石中文候选与英文结果对照；0.1.78已追加四种特殊符文的中文查询和用途，0.1.79补Data两种模式的可访问名称与键盘提交；继续其余材料与可能独立的制作控件，以及真实输入法与语言切换矩阵，不用合成composition事件当真机验收。
5. 标准全仓验证与ZIP复核按本轮集成记录登记；继续完成受凭证范围限制的CI工作流与远端验收，未完成前保持开发预览状态。上架商店另行安排，不把未上架本身当作当前功能未完成的证据。

这份顺序是后续执行优先级，不删除plan.md中的其他原始要求。更具体的页面证据仍以compatibility.md和acceptance.md为准。


## 四、0.1.81集成复核（2026-09-26）

运行时基线`58a6861`，本轮只更新审计文档，不升扩展版本。标准`pnpm verify`退出0，包管理器报告锁文件已是最新、依赖Already up to date；六个包类型检查、全仓Biome1118文件、581测试文件均通过，4883项通过／1项跳过，测试耗时463.21秒。跳过项对应build-core的本地真实`.build`往返测试，当前私有样本目录无该类文件；未删测试或调整超时。全仓构建、dict:check和craft:check均通过。

`pnpm extension:check`通过：0.1.81、6918词条、仅storage权限。ZIP的12个文件及完整文件集合与本轮全仓构建相同，5份词典来源哈希逐项匹配，仓库级skills的8份文件哈希与vendor-lock一致，skills不在ZIP内；项目dist交付副本逐字相同。ZIP SHA-256：`a0744ecf55e238b37e8ff886e46aee8096745894e8f78062509642ff1c47b56c`。

既有边界未消失：旧网站仍有大于500kB的构建块警告，国服／台服词典审计问题仍为13／17，目录仍报告既有未解析映射及未知权重。扩展771条材料名称版本保持unknown。本轮没有重新实测所有浏览器路径、输入法或来源版本；未以全仓绿灯替代上述缺口，也未恢复旧做装功能开发。


## 五、0.1.87集成复核（2026-09-26）

运行时基线`918996c`。标准`pnpm verify`退出0：六个包类型检查、全仓Biome1130文件、582测试文件通过，4893项通过／1项跳过，测试耗时465.96秒；全仓构建、dict:check、craft:check通过。跳过仍为build-core缺私有真实.build样本的原有测试，本轮只读确认目录无该类文件，未改跳过逻辑。首次沙箱运行在pnpm缓存SQLite数据库处失败，获准访问既有缓存后运行完整原命令，锁文件及依赖均Already up to date，无版本变更或跳过门禁。

随后`pnpm extension:check`及`pnpm extension:package`通过，后者实际调用`pnpm extension:build`。0.1.87、6923词条、仅storage权限。ZIP完整12文件集合与dist一致；重打包内容与上一轮浏览器实测交付包逐文件相等，项目dist副本更新后与新ZIP逐字节一致。五份词典来源SHA-256和仓库skills八份文件锁定哈希逐项相符，skills不进入ZIP。

当前ZIP SHA-256：`4d1a986597d901f082a6c619d929b5eee21f7f6d9e1dfde1b4d726878ac0bdea`。ZIP封装可能因重新打包的时间戳改变，已另核对文件载荷相同，不将包哈希变化误认为运行逻辑变更。

从0.1.81至本轮，旧网站、item-core、dict-builder、data/dict和data/craft无新增diff；只保留其规定回归，未重启搁置计划。旧网站大于500kB构建块警告、国服13／台服17审计问题及目录既有未解析映射／未知权重仍保留，扩展771条材料名称版本仍unknown。完整页面／输入法／装备矩阵、远端CI等缺口仍见第一节，本轮不重跑所有浏览器路径、不宣称六阶段完成。
