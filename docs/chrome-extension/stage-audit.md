# 阶段完成证据审计

审计日期：2026-09-25。基线：`5a93cda`，扩展0.1.48。初次审计只更新计划与证据。后续0.1.49已补齐图标，表格与施工顺序同步更新；第二节保留0.1.48原始核对记录，完整目标仍未完成。

## 一、六阶段状态

| 阶段 | 已取得证据 | 尚缺的完整验收 |
| --- | --- | --- |
| 站点接入 | 制作、Data、条件编辑、导入、背包和模拟器均有分项公开DOM及实装记录 | 0.1.54已确认Data物品框可检索材料，独立制作材料框仍未确认；每种输入的全部事件／导航／语言矩阵不完整 |
| 术语核心 | 显示、检索、领域隔离、来源哈希、真实词典灰区关闭均可验证 | 初次TDD流程记录本轮未追溯；游戏名称版本未知项仍明确标unknown，不能宣称所有译名版本完整 |
| 可安装显示层 | 双入口构建、最小权限、资源检查、启停、原文更新与复制节点恢复均有证据；0.1.49自制图标通过Chrome加载 | 0.1.58补齐首页／新版说明页双标签设置同步、双语重载及德语窄窗提示恢复；完整页面／模式矩阵仍缺 |
| 中文搜索 | 基底、词缀、Data、条件候选与键盘操作有实装证据 | Linux Fcitx5的Data、制作基底及词缀查询已验收；条件点击失效已复现为自动化点击越出视口，滚动后选择及停用对照通过；其他输入法与完整矩阵仍待验收；0.1.54已实测Data崇高石检索，0.1.60补齐价格页材料领域与崇高石对照；全部材料／用途对照链未完成 |
| 高级装备导入 | 七种基底的指定结构（0.1.72新增红玉戒指火抗基底及普通火抗后缀），以及法器复合／独立魔力已有原生结果对照 | 0.1.50确认环锁腰带咒符位原站拒绝／丢失并加入提示；权杖四组显式属性已核对，但原站不保留输入当前技能等级（见sceptre-import-audit.md）；普通武器和其他首饰、特殊结构仍不完整；不等于用户全部样本可制作 |
| 完整链与分发 | 本地ZIP、安装文档、若干保存／载入／撤销、多步互斥路线已验收 | 0.1.60已补并列组增删导出往返；组内嵌套入口尚未确认，完整条件／回边等流程、CI远端运行仍未完成；0.1.67再次通过标准全仓verify：581文件、4857项通过，1项既有跳过 |

旧自研制作网站继续搁置。CI工作流的既有推送凭证限制只阻塞该工作流，不阻塞本地功能开发；不据此将整个目标标为blocked。

## 二、本轮实际核对

- `packages/l10n-core/src/index.test.ts`与扩展`build-dictionary.test.mjs`：2文件26项通过。覆盖ID冲突、领域隔离、未知项、正负号、小数、范围、数值顺序、固定数字、精确／别名／部分候选、50项排序与范围限制；构建测试确实包含`.mjs`，没有漏跑。
- 独立`tsc --noEmit -p packages/l10n-core/tsconfig.json`通过。检查核心源文件，未引入DOM、Chrome API、文件访问或制作引擎。
- 用真实入库源先完整构建，再在同一个临时输出路径关闭gray重建：6880→4892条，1487195→1179549字节；结果无gray来源、无以gray路径为来源的残留词条。剩余领域为stat、item、ui，base／unique会随gray数据移除。这是降级行为证据，不是“关掉gray后功能覆盖不变”的承诺。
- 真实材料名称来源沿用已登记catalog的名称子集，版本未知时写unknown；不带其制作规则进扩展词典。游戏文本权利和MIT自有代码范围继续按data-sources.md及NOTICE区分。
- manifest与package-check：2文件5项通过；当前dist再次执行check通过，0.1.48、6880条、仅storage权限。检查`build.mjs`的两个独立Vite入口、关闭第二次清空、内容IIFE、popup资源和根脚本；不把产物检查等同于所有页面实装通过。
- `git show d426303`确认核心、测试和构建器的实际交付提交。标题为“实现新版 CoE 简体 Chrome 扩展开发预览”，不是计划里原拟的单独核心标题。
- 仓库级skills仍在`.agents/skills/`，含vendor-lock与各自许可证；没有新增全局安装。

测试通过只证明对应断言。未以本轮31项针对性测试替代上轮133项回归或全仓verify，也未以有测试文件推断最初编写顺序。

## 三、后续施工顺序

1. 已于0.1.49补齐自制扩展图标、manifest引用、构建复制与打包验证；最终ZIP在Chrome管理页显示正常，四种尺寸解码通过。原审计基线保留在第二节，新增证据见compatibility.md。
2. 环锁腰带咒符位已确认原站兼容性缺口（0.1.50），不删行放行；权杖技能／复合词缀已核对，当前等级未保留，暂不放行；后续继续其他类别，并将允许原站差异的提交模式作为独立设计问题；每种新增能力先查明原站输入与原生导出，不从普通法器直接泛化。
3. 0.1.52已用真实键盘将组内匹配数量保存为1；英文原站与最终中文包完整导出一致，均运行完成10件且第一步可直接结束。0.1.53已补显式回边建立、起点选择、运行和手动停止；无动作回边未自动结束，0.1.54在全新无扩展会话复现普通等阶与属性阈值未保存，0.1.69用失焦后等待一秒的对照验证普通等阶及渎灵前缀数值均可保存，快速操作在无扩展原站也可保留旧值；同轮后续已验证能量护盾41／43阈值分别进入两条分支且与无扩展运行一致，其他属性路径仍待复核。继续更深层嵌套及完整分组／回边矩阵；保留已知原站步骤复制异常。
4. 0.1.54已确认Data物品搜索可查询材料，并完成崇高石中文候选与英文结果对照；继续其余材料与可能独立的制作控件；继续真实输入法与语言切换矩阵，不用合成composition事件当真机验收。
5. 0.1.58标准全仓verify已通过，继续完成受凭证范围限制的CI工作流与远端验收，未完成前保持开发预览状态；不自动上架商店。

这份顺序是后续执行优先级，不删除plan.md中的其他原始要求。更具体的页面证据仍以compatibility.md和acceptance.md为准。
