# 导入面板键盘焦点验收（2026-09-26）

Linux Chrome151，CoE Beta／PoE2／English。0.1.103实装中，导入自造稀有粗制弓中文样本，实际Enter激活“填入英文到原站导入框”；文本已填入英文，但document.activeElement为BODY。按钮成功后禁用导致焦点丢失。

0.1.104在成功填入或恢复原文后，将原按钮丢失的焦点交还原生importerInput。仅当按钮原先持有焦点、弹窗仍有效、焦点仍在原按钮或body时执行；原站input处理已移到其他控件时不抢回。没有更改转换内容、失效检查或原站确认动作。

新增回归先复现填入焦点缺失，再验证填入／恢复归还和原站转移焦点不抢回。31文件262项相关测试、扩展类型、全仓Biome、构建和包检查通过；未重复全仓verify，最近完整基线为0.1.101。

最终ZIP独立实装，Enter激活填入后焦点为importerInput，文本为英文；Enter恢复后焦点仍为importerInput，文本与完整输入样本相同。随后移除附加物理数值的一处范围，重新预览；实际Enter激活诊断，准确选中第16行且不改变文本。ArrowRight／Space修改后，旧诊断按钮全部禁用，状态为“原文已改变，请重新预览。”。全程未点击原站继续按钮，不将本轮焦点验收当作新导入结构放行。

本轮以focus命令定位待测按钮，再发送真实按键，验证键盘激活及焦点结果；没有宣称完整Tab顺序、读屏播报或所有诊断类型已验收。原站转移焦点边界为DOM回归证据，未伪造原站内部状态。

最终ZIP12文件集合与字节均与dist、实装目录及交付副本一致，6933词条、6来源哈希相符，仅storage权限，没有新依赖。
