# 新版 CoE 接入与覆盖记录

调查日期：2026-09-24。目标入口为 `https://beta.craftofexile.com/whats-new` 所述 **Craft of Exile 2**；该页面未提供可用于锁定构建的版本号。浏览器为 Linux HeadlessChrome 151.0.0.0。只观察公开 DOM 与可见结果，不读取 CoE 源码或词缀库。

## 接入约定

模式来自 `#gameToggler a.poe2.selected`，语言来自 `#languageToggler .list [key="us"].active`，两者都须唯一。导航后 URL 可能没有 `game=poe2`，因此不凭 URL 参数判断。只允许 Beta 精确域名。

| 页面／用途 | 公开控件 | 已观察行为 | 范围 |
|---|---|---|---|
| 首页基底搜索 | `#searchItemInput input[type=text]` | value + input 不足以稳定出结果；keyup 后异步出现 Runed Focus 与 Runeforged Runed Focus | 已接入中文候选，选择后可进入制作页 |
| 制作词缀搜索 | `#searchInput input[type=text]` | keyup 后 Lightning Resistance 只显示对应词缀；中文候选回填英文模板后结果相同 | 已接入 |
| Data 物品搜索 | `#dataItemSearchInput input[type=text]` | keyup 后出现 Runed Focus 等候选 | 已接入；最终 ZIP 实装中文选择返回同一对基底 |
| Data 词缀搜索 | `#dataModSearchInput input[type=text]` | 控件存在，使用相同适配器 | 0.1.2 最终 ZIP 实装：等待查询结果稳定后，中文候选与英文查询的 68 条结果标识及顺序一致 |
| 材料检索 | 未确认独立稳定文本框 | 制作方式中的 Essences 未出现可单独核实的材料输入控件 | 不接入，避免把条件编辑器当材料搜索 |
| 高级装备导入 | `dialog #importerInput`、`.importCommitButton` | 原站接受用户点击 Proceed 后读取文本 | 接入独立中文预览与主动回填；覆盖见下文 |
| Inventory | `dialog#inventoryDialog` | Reset／Import／Export／Close；背包标签名属于用户文本 | 按钮可翻译，标签区排除 |
| Simulator | 原站 Simulator 按钮进入流程区 | 数值配置、标题、描述及流程节点存在 | UI 精确词条可翻译；输入框与描述不做查询替换；复杂节点流程验收待补 |

## 文本与原站语义

普通界面按文字节点精确匹配，不修改 HTML、链接或业务属性。输入、textarea、contenteditable、代码、广告、扩展区域和背包标签排除。切换原站语言／游戏模式后停用并恢复拥有的文本；未知模式不启用。

词缀 `.stat` 可能包含 CoE 自动插入的 `.keyword` 节点。它们保留原英文及悬浮交互，用旁边的 Shadow DOM 显示中文；不把词缀元素内的英文替换成中文。数字、`#`、`(16-20)`、高级 `18(16-20)` 范围按源文本保留。未匹配复杂分段／合并词缀仍是英文。

首页中文 `符文法器` 已在实际安装扩展中显示候选，选择 Runed Focus 后原站出现制作按钮，并可进入对应基底制作界面。词缀中文 `闪电抗性` 选择后输入值为 `#% to Lightning Resistance`，可见结果为对应的两条词缀组，与直接英文检索一致。

## 导入覆盖与待验收

扩展复用 `item-core/text` 的解析与文本对照；导入资格由扩展单独检查，不调用本机制作引擎。首批只放行 Runed Focus 的魔法／稀有普通前后缀，六类属性、范围和分组要求见安装文档。既有普通法器转接研究保留在 `docs/coe-focus-bridge-research.md`，不据此声称本扩展全流程已验收。

合成测试覆盖原文、分组、范围、未知行、缺阶级／范围、备注隔离、传奇、咒符、腐化和未验收类别。腰带、权杖、武器、防具、首饰以及复合／特殊来源词缀仍只对照，不放行自动回填。没有使用或提交用户私人装备样本。

扩展真实导入、设置持久化、语言切换、复制／导出和完整模拟链的最终结果在本页继续补录；未列为通过的项视为未验收，不能用单元测试数代替。

## 本轮实际扩展验收补录

- 安装 dist 中的 MV3 扩展运行，而非只用 eval 注入待测代码；真实 popup 操作关闭后现有制作页恢复 Crafting／Data／Import an item，扩展辅助节点为 0；本机 storage 记录 `enabled:false`。
- 自造稀有符文法器经“中文预览 → 填入英文 → 原站确认”导入成功：物等86、护盾属性81，护盾上限 `39(36-41)` 与闪电抗性 `17(16-20)%` 在装备卡中相符；对应中文 Shadow DOM 行实际可见。原站把自造名称显示为 New Item，不承诺保留稀有随机名称。
- 普通词缀表和装备卡的分段关键词均有中文叠加，不更改原词缀 textContent。原站同节点移动的回归已覆盖位置迁移、数值更新和关闭清理。
- 自动测试覆盖 malformed／重复普通属性、前后缀不符、明显异常阶级；该检查不是完整游戏合法性或数值推导引擎。
- 本轮仍未覆盖魔法装备、其他六词缀组合、真实 IME 操作、全部模拟流程、材料下拉框与 Data 词缀查询的完整实测。这些仍是后续验收任务。

- 六词缀自造稀有符文法器也完成同一导入流程：护盾39、冰霜伤害28%、魔力49、闪电抗性17%、魔力再生32%、混沌抗性14%，原站显示的范围和等阶与输入相符。
- 最终 ZIP 解压目录作为新扩展实际载入；Data 输入“符文法器”，选择后原站显示 Runed Focus 与 Runeforged Runed Focus 的对应简体基底卡。
- 词典共 6,559 条，SHA-256：`12cc14a6e46a76316b030487ec9ffba876c4a5cdd7a87b3dab1309e8e313d24c`。ZIP 约272 KiB，已通过压缩完整性检查。

- 原站语言从 English 切到 cn 后，扩展辅助节点为0；切回 English 后制作／数据导航恢复简体译文。扩展没有主动替用户修改原站语言。

后续返回制作页时出现 CDP 响应超时；无扩展对照会话随后亦出现导航超时，尚未归因，未将该次完整导航链标记通过。原站导出按钮后的剪贴板读取也超时，因此复制／导出的最终语义仍未验收。

## 自动验证与基线问题

扩展与术语核心：8 个测试文件、43 项通过；全仓类型检查、lint、构建、dict:check、craft:check 通过。dict:check 仍报告既有 zh-CN 13／zh-TW 17 项审计记录，不代表本轮新增或全部词条已人工复核。

默认 `pnpm verify` 的测试阶段出现旧大型制作界面超时和沙箱子进程 EPERM，因此不能报告全套 verify 通过。移出沙箱、将 CLI 默认超时提高到60秒后重跑全套；部分旧测试文件自行设定15秒等超时，仍存在失败。未修改的 main 分支亦复现 SerleCrafting.test.tsx 的两个15秒超时用例。扩展没有改动旧 UI、策略或制作规则；不为掩盖失败调高其文件内限时。

重跑全套在观察到 SerleCrafting 4项、AstridTargetRoutes 2项旧界面失败后停止（退出130），没有完整测试总数，不视为全套通过。开发预览仅提交独立分支，不合入 main；后续仍需处理基线测试与剩余浏览器验收。

## 发布分支与 CI 权限

GitHub 当前推送凭证缺少 workflow scope，含新增 `.github/workflows/extension.yml` 的推送被拒绝。完整提交 `f80341f` 保留在本地 `feat/coe-chrome-extension-ci-local`；可推送的 `feat/coe-chrome-extension` 仅排除该新增工作流，运行源码、测试、词典与已验证产物一致，未改写原提交。新增 CI 尚未在远端运行，不能报告 CI 通过。


## 0.1.2 最终包补验（2026-09-25）

- 将最终 ZIP 解压后作为真实扩展加载，PoE2 / English 下导航文字正常汉化。
- 首页输入“符文”，50 个候选中 ArrowUp 进入末项；Escape 后辅助面板为0，焦点回到原 INPUT，查询仍为“符文”。
- 再输入“符文法器”，将焦点移到“数据”链接，候选立即撤下且焦点留在该链接；进入 Data 正常。
- Data → 词缀，输入“闪电抗性”并选择 `#% to Lightning Resistance`。异步刷新后显示68行。初次读取到“至少一个筛选项”的提示，曾误判标签为必需条件；0.1.3 检查稳定状态仅有搜索条件，已纠正此前推断。与直接英文查询比较，68条行标识和顺序一致。这里证明查询一致，不证明这些原站行全部属于当前游戏有效词缀。
- 发现 Data 结果行结构为 `.modifierTable .row > .label .text`，有 `.modValue`、`.range` 与 `.keyword` 子节点；不同于制作页 `.stat`，当前仍有分段英文。此为下一轮显示覆盖缺口，尚未接入，不能称 Data 全部中文化。
- 本轮扩展与核心9文件52项测试、扩展类型检查、Biome、构建及包检查通过。未重跑全仓旧制作 UI 测试。


## 0.1.3 Data 词缀显示补验（2026-09-25）

- 最终 ZIP 解压实装，首页进入 Data → 词缀，中文“闪电抗性”选择 `#% to Lightning Resistance` 后等待结果行出现：仍为68行，52行得到中文叠加。此计数仅指该查询样本，不代表全站覆盖率。
- 实际首三行原文为 +10%、+20%、+30% to Lightning Resistance，分别显示“闪电抗性 +10%／+20%／+30%”；原 `.keyword` 节点和英文 textContent 保留。
- 截图审阅：中文独占下一行，位于英文与标签之间，未挤入相邻的词缀名称／等级列。点击结果行仍能打开原站 Modifier details 并正常关闭。
- 稳定时选中的 li 只有 `mods`，未选中标签；此前“需要先选择标签”的结论证据不足，已改为等待异步结果。不要把结果刷新前的空态当作最终限制。
- 分段结果接入范围为 `.modifierTable .row > .label .text`；复杂合并词缀、详情弹窗中的其他结构及未命中术语继续保留原文。核心／扩展9文件55项测试、扩展类型检查、Biome、构建与包检查通过。


## 0.1.4 魔法法器导入补验（2026-09-25）

- 最终 ZIP 解压实装，使用自造魔法符文法器、一前一后、物等86；经中文预览、主动填入、原站继续进入制作页。原站当前地址状态记录 rarity 为 magic；没有据名称 New Item 判断稀有度。
- 初始护盾词缀40，预览后改为39，真实界面立即禁用旧填入按钮并提示重新预览；重新确认后成功导入当前值。
- 原站装备卡显示护盾81，前缀 `+39(36-41) to maximum Energy Shield`（Radiating，T6）、后缀 `+18(16-20)% to Lightning Resistance`（of the Storm，T6），等级86；未知的合成词缀名字由原站规范化，不承诺保留自造词缀名称。
- 自动回归额外验证重复预览、停用扩展后的旧按钮不能修改原站；58项核心／扩展测试、类型、格式、构建与包检查通过。
- 导出按钮操作后，页面 Clipboard API 限时读取未返回，专用接口报告 Document is not focused；导出文本语义尚未验收，不能据按钮可点击声明通过。

## 0.1.5 控件提示与新版介绍页补验（2026-09-25）

- 页面来源：https://beta.craftofexile.com/whats-new 。最终包实装，PoE1 模式保持英文；切换 PoE2 后，“认识 Craft of Exile 2”、计算器／制作演练／流程模拟、功能、用户设置、其他改进等标题显示中文。长段落与未收录项目仍为英文，不宣称介绍页完整翻译。
- 导入框真实 placeholder 在两句之间带两个换行，无障碍快照会省略。初版因字符串不匹配未翻译；按 DOM 原值修正词条、重建并重新实装后显示“在此粘贴装备文本（请在游戏中按 Ctrl+Alt+C 复制高级装备信息）”，value 仍为空。
- 从另一标签页打开真实扩展 popup，取消启用；返回已打开的导入框，placeholder 恢复完整英文及两个换行，value 仍为空，扩展辅助节点为0。
- 属性层只匹配 UI 词条，限定普通控件的 placeholder／title／aria-label；未知提示保留原文。63项核心／扩展测试、扩展类型检查、Biome、构建与包检查通过。词典6590条，未新增权限或第三方运行时请求。

## 0.1.6 动态边界回归（2026-09-25）

节点仅在原先加载时处于排除区并不够：原站复用节点时可能改变祖先 class／hidden／contenteditable、输入 type 或区域 id／role。本轮对这些变化补充恢复与重新翻译；只在当前值仍为扩展写入值时恢复，避免覆盖后来的用户文本。普通选中／悬停类不要求子树重扫。11文件68项测试、类型、格式与构建／ZIP检查通过；这些是合成 DOM 回归证据，本轮未补实站操作记录。

## 0.1.7 搜索排序离线回归（2026-09-25）

中文候选以完整名称精确匹配优先，再按匹配名称／别名的长度排序；短的普通词缀不再被稳定ID排到特殊长词缀之后。真实打包词典6590条中，“闪电抗性”首项为 `#% to Lightning Resistance`，exact=false，仍需选择；前50项在排序后截取。11文件70项测试及两包类型、格式、构建、ZIP检查通过。本轮隔离浏览器两次访问远端均 ERR_CONNECTION_CLOSED，Simulator 实站链未执行，不以离线通过替代。

## 0.1.7 Simulator 连接对照（2026-09-25）

- 当前源码34c3127，浏览器Linux HeadlessChrome 151.0.0.0。带扩展访问PoE2入口返回 ERR_CONNECTION_CLOSED；直接HTTP HEAD首页返回200。
- 同一会话首页导航等待超时，但之后能读到导航。点击PoE2后，DOM.enable请求超时；该请求已终止后才关闭会话。
- 新无扩展对照浏览器访问首页也返回 ERR_CONNECTION_CLOSED。因此连接失败不是扩展独有现象；不能据此排除扩展对DOM响应时间的影响，也没有证明站点整体不可用。
- 两个任务会话均已关闭，本轮未进入Simulator，不记录流程验收通过。没有改动网络代理、浏览器持久配置或原站数据。
- 当前包检查重跑通过：0.1.7，6590词条，仅storage权限。词典SHA-256 `c4d206bd225246db5ada0fbba9b9785460fd6d8bacf4d75cc793460847cf4cf6`；ZIP SHA-256 `64c683771e67c80f0b772becddfa112b59aa5d6c624d2b1fc958fa2b89795aef`。

## 0.1.8 Simulator 用户文本保护（2026-09-25）

- 网络恢复后进入新版 Beta 的 English／PoE2 Simulator。0.1.7将自造流程名称、步骤标题 `Runed Focus` 显示为“符文法器”，将用户说明 `Search` 的悬浮提示显示为“搜索”，构成用户内容误译。原始 tooltip 属性未变化，问题发生在其显示节点。
- 仅依据公开 DOM 为保存列表标题、步骤标题及其说明子树统一加保护，正文、提示属性、词缀叠加三层共用；边界变化时恢复仍由扩展拥有的原文。输入框与公共按钮继续分别遵守原有边界。
- 同一隔离浏览器从扩展管理页重新加载构建目录，确认显示版本0.1.8，刷新页面后进入Simulator：已有步骤标题为 `Runed Focus`，悬浮说明为 `Search`，Load列表中的已保存名称为 `Runed Focus`。公共按钮译文正常。此为构建目录升级实测，不等同于最终ZIP重新解包安装。
- 新增9条实际观察的公共界面译文，打包词典6599条。两项回归先失败后修复；12文件72项核心／扩展测试、扩展类型检查、Biome、构建、包检查及ZIP完整性检查通过。
- 点击Run simulation后原站立即连续运行并显示Stop simulation。停止点击请求返回工具daemon忙／无响应，后续读取失败；不能确认正常停止，也不能归因于扩展。随后独立关闭命令明确返回Browser closed，测试会话已结束。尚未完成有效制作路线、运行结果、撤销、拖拽、多路线、重新加载及导出对照验收。

## 0.1.9 有限模拟与结果显示（2026-09-25）

- 环境：Chrome 151.0.0.0，Beta `?game=poe2`，English／PoE2；独立无扩展会话与扩展会话。以默认普通胸甲创建单步点金石流程，无有效路线时End，Fixed／Actions模式。没有复制原站制作数据库。
- 自动化fill让目标框显示10，但首次实际执行10000步；换用Ctrl+A、逐键输入10、Tab后实际执行10步。后续验收以运行计数为准，不以输入显示值代替。无扩展及0.1.8对照均为10步／10件，原站Run count显示11、Success chance为90.9%；扩展保留这些数值，不纠正原站计数定义，也不据随机词缀不同判断语义不一致。
- 实际结果DOM显示两项缺口：结果表的用户步骤名称不在保护范围；统计表头复用`.stat`类被词缀保护排除。两项DOM失败用例先复现再修复，只有`.simulatorResultsTable > .header .stat`按界面文案处理，数据行词缀仍保留英文并叠加中文。
- 新增54条人工短译文。真实词典测试发现`# Stored`中的字面井号被当作数字模板，新增失败用例后将ui领域限定为字面精确匹配；其他领域的模板数量及顺序校验保留。词典6653条。
- 最终构建目录经Chrome扩展管理页重载至0.1.9，刷新后重跑保留流程：10步／10件完成，结果表名称`Runed Focus`保持原文，动作次数／通过次数／占比／比率及用量／折合混沌石／崇高石／神圣石列均显示中文。截图人工检查未见列错位或遮挡，装备词缀中英叠加正常。中途构建的Next操作也成功切换结果装备；最终构建未重跑每个翻页按钮。
- 12文件75项核心／扩展测试、两包类型、Biome、构建及权限资源检查通过。此为构建目录实装，不等同于最终ZIP解包安装验收。两个测试浏览器在操作结束后关闭。
- 原站Export Simulation点击后，剪贴板读取明确返回Read permission denied；没有绕过权限或代替原站导出，导出语义仍未验收。实时连续运行的停止异常尚未复测，多路线、拖拽、撤销及真实IME仍待补；有限单步运行成功不代表完整Simulator验收通过。

## 0.1.10 导出对照与克隆空壳清理（2026-09-25）

- 本轮先以0.1.9构建目录实装，从中文预览导入自造魔法符文法器：物等86、护盾前缀40（36–41）、T6。原站规范化为护盾82、需求45级／64智慧。没有使用真实私人装备。
- 先尝试仅对隔离测试来源调用[Chrome DevTools权限接口](https://chromedevtools.github.io/devtools-protocol/tot/Browser/)；接口返回成功但页面仍为prompt，显式上下文调用被拒绝，Clipboard API仍超时或报告焦点错误。因此不把这些调用当成权限已生效。切回测试标签页后，改用临时textarea和真实Ctrl+V粘贴原站刚复制的内容；没有改写剪贴板API或读取其他剪贴板历史。
- 原站Export输出装备JSON，启用翻译和经真实popup关闭翻译两次输出逐字相同，均244字符；基底身份、物等86、magic及护盾词缀数值40一致。原站POB按钮输出英文物品文本，停用和再启用两次输出逐字相同，均172字符。此证据仅覆盖该自造装备的两种导出，不等于Simulator整流程或所有装备导出通过。
- 关闭0.1.9时发现一个`data-poe2-l10n="stat"`空span，无Shadow DOM，位于原站词缀旁；与cloneNode复制宿主而不复制其Shadow DOM的行为吻合。用合成DOM克隆复现为失败测试，再清理此类克隆空壳。扫描范围为新增子树，关闭时额外清理尚未处理的空壳；不移除原站词缀。
- 最终0.1.10构建目录经管理页重载并确认版本；同一自造装备页面启用时词缀叠加435个、无Shadow DOM的空壳0个。通过真实popup停用后所有扩展辅助节点0个，原站`.stat`节点仍为582个。此计数仅为本次页面快照，不作为性能或全站覆盖指标。
- 13文件79项核心／扩展测试、扩展类型、Biome、构建、词典校验及ZIP检查通过；本轮仅构建目录实装，未做最终ZIP解包安装或完整运行链。测试浏览器已关闭，临时控件和权限不进入扩展。扩展权限仍仅storage。

## 0.1.10 最终ZIP、流程保存与导出（2026-09-25）

- 从最终ZIP解包到独立临时目录，用新隔离浏览器实装；Chrome管理页确认0.1.10，英文PoE2页面呈现中文。未使用上一轮构建目录替代该包。
- 自造单步骤流程，流程名`Runed Focus — 流程验收`、步骤标题`Runed Focus`、说明`Search；中文流程说明 123`，通过原站保存。此流程无制作动作，不用于验证运行概率或多路线。
- 原站Export Simulation后，使用临时textarea和正常Ctrl+V读取789字符JSON。经真实popup关闭翻译后再导出，与启用时逐字一致，辅助节点为0。没有尝试授予剪贴板权限，没有覆盖Clipboard API。
- 通过原站New Simulation → Import a Simulation粘贴同一JSON并导入，再导出与最初基准逐字一致（789字符）；流程／步骤ID、说明及配置完整保留。
- 另用该自造JSON建立不同ID的空流程，再打开Load列表，选中原存档后点击出现的Load按钮，步骤标题和说明恢复。点击列表名称只是选择；当前项没有重新载入按钮，不把点击名称误当作完成恢复。
- 保存载入后导出与初始基准唯一差异为`lastModified`时间戳；其他字段逐字段一致。单步骤保存／载入内容通过，不声称整文件始终逐字不变。
- 用鼠标拖动步骤手柄，原站节点位置发生变化；再次导出记录`position: {x: 0, y: 185.333}`，标题及说明不变。此为单节点拖动，不覆盖多节点连线、条件路由或撤销。
- 测试浏览器已关闭。该轮仅新增实站证据和说明，未改运行代码、未升版本；未重新运行单元测试。多路线、实时停止、真实IME及更广装备导入继续待验收。

## 0.1.11 实时计数性能与两步流程（2026-09-25）

- 先以最终0.1.10 ZIP建立普通胸甲的蜕变石→增幅石流程：步骤从侧栏拖入画布后才显示连线控件，使用New link／Create link连接。原站导出确认fixed／items／10，第一步links指向第二步；有限运行完成10件、20步，两种通货各执行10次。此为两步串联，不等于多条件分支或循环路线通过。
- 0.1.10实时测试的首次停止请求最终报告引用失效；改用按按钮名称定位后仍报告daemon无响应。关闭各隔离会话后，使用同一自造两步配置做无扩展对照，停止成功进入Clear simulation。不能仅凭这些工具错误证明原站或扩展必然卡死，但差异足以触发性能排查。
- 发现每个未知数字也遍历6653条词典。当前词典1000个纯数字输入的初次离线测试约8353ms；随后同进程用旧线性匹配参考与新实现对照，1000次分别为2721ms和1ms。后者经过预热，数值仅用于说明该路径开销，不作为网页整体性能倍数承诺。
- 每23条抽取实际词典项，以原文及替换数字范围文本、限定领域及不限领域组合，共1160次新旧输出比较全部一致。回归测试另外验证纯符号模板、数字UI词条、字面／模板冲突、同译文多身份及领域筛选。
- 改为静态文本精确索引；含字母模板不参与纯数字计数匹配，纯符号模板保留。仍合并字面与模板结果检查歧义，不使用“第一个匹配即返回”。
- 最终0.1.11 ZIP独立解包实装，导入同一自造实时配置并使用真实中文停止按钮：页面进入清除模拟结果，运行次数1754、步骤3506、物品1753；随后再次读取计数不变，停止按钮不可见。两个步骤各1753次。此为本次样本通过，不外推所有复杂流程或默认无动作循环。
- 13文件81项核心／扩展测试通过，两包类型、Biome、构建和包检查通过。测试代码最初缺少RegExp this类型标注，修正后类型检查通过；不把首次失败隐去。所有测试浏览器已关闭，未新增权限、依赖或第三方代码／制作数据。


## 0.1.12 细枝头冠中文导入（2026-09-25）

- 原站英文对照先确认 Twig Circlet 可接受高级文本。将法器的护盾40（36–41）、T6照搬到头盔时，原站规范化为T4，说明同一词条不能跨装备推断等阶。扩展不引入制作规则或复制原站数据，只按明确基底／类别划分已验收属性。
- 最终0.1.12 ZIP独立解包实装，Chrome 151.0.0.0，Beta English／PoE2。两份自造简体中文文本均完成“预览中文转换→填入英文→原站继续”。不是用户真实头盔样本，也不作为国服所有高级文本格式均已验收的证据。
- 魔法细枝头冠：物等86，护盾总值59，护盾前缀40（36–41）、T4，闪电后缀18（16–20）、T6；装备卡数值与范围一致。
- 稀有细枝头冠：物等79，护盾总值58，护盾前缀39（36–41）、T4，闪电后缀17（16–20）、T6；交易备注保留在左侧原文，不在填入文本中。原站名称显示New Item，不承诺随机名称保留。
- 分别点击原站导出并用真实Ctrl+V读取自造JSON：基底身份均为`Metadata/Items/Armours/Helmets/FourHelmetInt1`；两个词缀分别为`LocalIncreasedEnergyShield5`、`LightningResist3`，数值分别为40／18和39／17；`r`分别magic／rare，`l`分别86／79。地址查询参数曾显示旧normal／100状态，未用它作为这次稀有度证据。
- 资格仅新增此基底的护盾上限、闪电抗性；孔位、品质、复合词缀、缺等阶、特殊状态以及其他头盔词缀仍未开放。新增类别别名只影响文本转换，不推进已搁置制作引擎。腰带、权杖、首饰等仍需后续验收。
- 新回归先因未开放基底失败，实施后核心／扩展及类别回归共14文件92项通过；扩展与item-core类型、相关Biome、构建及包检查通过。本轮两次隔离浏览器均已确认关闭。


## 0.1.13 搜索焦点返回（2026-09-25）

- 0.1.12实装复现：输入“符文法器”，将焦点移到数据链接再返回原输入，中文仍在而候选为0。用户必须重新编辑才能选择。两条新增回归先失败，再实现focusin恢复；原站输入值和事件提交仍由显式选择驱动。
- 最终0.1.13 ZIP独立解包实装，Beta English／PoE2：首页查询离开后候选为0，返回后候选为2且焦点仍在输入框。真实ArrowDown→Escape后面板为0、焦点归还输入框，没有立即重开。
- 再次离开／返回后按真实Enter，输入值变为Runed Focus；异步原站结果出现符文法器和符文符文法器两张卡，各有制作按钮。不是仅凭扩展自己的提示判断搜索成功。
- 单元回归还核对旧按钮不能提交、重新聚焦不自动提交、英文不自动转换。13文件85项核心／扩展测试、扩展类型、Biome、构建与包检查通过；两个隔离浏览器均已确认关闭。焦点切换用DOM focus，方向键与Enter／Escape用真实浏览器按键，不等于操作系统中文输入法验收。
- 顺带发现首页操作说明中的英文and／or被通用条件标签译为“全部满足／满足任一”，上下文不合适，列为下一轮显示匹配修正；本版本未声称全页翻译完成。


## 0.1.14 说明连词上下文（2026-09-25）

- 0.1.13公开DOM确认首页`#instructions`中的独立and／or文本被通用UI词条译为“全部满足／满足任一”；制作条件控件则是`li[value=and]`、`li[value=or]`，需要保留这组条件译文。
- 在CoE区域适配器加入仅限`#instructions`的人工连词译法与／或。显示缓存键和节点所有权记录纳入上下文；移动节点或改变容器id时使用保留的英文重新翻译，不把已写译文当作原文。其他英文段落仍可能未翻译，不声称说明页全覆盖。
- 两条新增回归先失败，实施后验证相同原文在不同区域分别翻译、跨区域移动、容器id变化、中英对照和关闭恢复。核心／扩展13文件87测试、扩展类型、Biome、构建与包检查通过。
- 最终0.1.14 ZIP独立解包实装，Beta English／PoE2：说明实际文本为advanced／与／classic及添加／或／移除；制作页仍为全部满足／满足任一，value保持and／or。点击满足任一后selected从and转到or；这不等于复杂计算条件链已全面验收。
- 首次读取旧包制作页发生在导航过渡期报main不存在，随后同会话重读成功；未把工具瞬时错误归因于扩展。两个隔离会话均已确认关闭。


## 0.1.15 新版介绍页功能说明（2026-09-25）

- 直接读取用户指定的`https://beta.craftofexile.com/whats-new?game=poe2`公开可见文案及文本节点，以独立人工译文补充52条UI记录；没有读取原站执行源码或制作数据。词典共6705条，来源哈希随构建更新。
- 覆盖条件组、空余词缀位、保存／导出、装备编辑、路线结果、基底费用、主要设置、特殊符文支持及原始数据浏览。保留ALT、and／or／not条件语义、普通／高级显示、排序和筛选选项的区别；译文描述的是CoE功能，不是本扩展已自行实现这些制作能力。
- 最终0.1.15 ZIP独立解包实装，English／PoE2介绍页显示新增中文；读取完整main文本并查看页面上下部截图，三列卡片文字正常换行，没有观察到遮挡。该视口innerWidth1266、scrollWidth1251，无横向溢出，不外推所有屏幕尺寸。
- 切换到PoE1后，保存装备说明为英文，中文对应句不存在，确认模式边界仍生效。两个隔离浏览器均已关闭。
- 87项核心／扩展测试、相关Biome、构建、词典及包检查通过。本轮仅人工词条、版本和文档变更，未新增逻辑或测试，未另跑类型检查或全仓verify。
- 未覆盖：被链接／强调标签拆开的开场介绍、装备标记等部分未核准术语、部分PoE2材料／基底名称。Chronomancy等示例名称保留英文，不猜译国服身份。Emulator／Calculator／Simulator／Legacy模块徽标由CSS伪元素生成，不是文本节点，本轮保持英文。PoE1专属内容不纳入译名覆盖；同页通用描述可显示中文。


## 0.1.16 介绍富文本与伪元素徽标（2026-09-25）

- 公开DOM确认介绍为`main .messageBox`中的6个可翻译片段，品牌强调span及`href="roadmap"`链接保留；仅在`/whats-new`匹配。单词new不会因此在其他按钮或页面中被替换为新增功能。
- 四类模块徽标为`main .layout article li`下空span的::after，新增可撤销样式显示制作演练、计算器、流程模拟、历史版本，提供中英对照。样式不修改原站节点、class或业务属性，不读取原站执行源码。
- 新增5项回归先确认介绍未翻译、历史导航残留译文和样式未生成，再完成实现。回归覆盖链接／节点身份、停止后完整原HTML恢复、其他页面不处理、popstate、样式清理以及返回介绍页后重建。14文件92项核心／扩展测试、扩展类型、Biome、构建及包检查通过。
- 最终0.1.16 ZIP解包实装，Chrome管理页确认版本。PoE2／English介绍开场显示连贯中文；截图检查链接与强调仍可见。四类徽标的computed ::after content分别为预期中文。
- 真实popup勾选中英对照后，徽标显示“制作演练 · Emulator”，注入样式仅1份。关闭简体中文后，样式0份，徽标恢复Emulator，开场恢复完整英文、链接仍为roadmap。两个隔离会话均已确认关闭。
- Chrome扩展管理页首次读取过早未找到shadow节点，等待加载后同会话重读成功；未通过重启或修改站点状态绕过问题。复杂条件路由、真实IME和其他装备结构仍待验收，介绍页部分专有名词继续保留英文。

- 提交前追加动态messageBox类名回归，先失败后补入边界观察并重新构建，最终为92测试。启停／对照实测使用补入观察类名前的0.1.16包；最后一次ZIP另做介绍与徽标复核，分别记录，不将两次产物混为同一次验收。
- 最后一次ZIP复核：介绍包含“的全面重构”，四种徽标为预期中文、样式1份、链接仍为roadmap；最终复核会话也已确认关闭。


## 0.1.17 背包使用链与保存目标名称（2026-09-25）

- 本轮先使用最终0.1.16包：自造魔法符文法器经中文预览／填入／继续导入，物等86、护盾82、护盾前缀40（36–41）、T6。原站导出基准通过真实Ctrl+V读取；点击保存并选默认仓库页。
- 背包右键装备→载入，导出中的基底、词缀、数值、物等、稀有度不变；与导入基准的差异仅ig（null→0）、pt（null→4.5.5.3）和id（新增存档UUID）。不能将保存后的JSON视为逐字不变。
- 在载入装备上选增幅石并点击装备卡，新增ManaRegeneration1、数值13（10–19）、T6；点击原站撤销，导出与“载入后基准”逐字一致，中文叠加行恢复为40（36–41）护盾。此证据只涵盖一次增幅与撤销，不等于任意制作、多次历史或重做都通过。
- 右键仓库页→Rename，将自造页命名为Runed Focus。背包标签保持原文，但新装备的Save to弹窗将该名称误译为符文法器。公开DOM显示目标按钮以仓库页UUID作为id，New tab按钮id为0。
- 统一用户内容判断增加此明确结构，覆盖文本、属性及词缀叠加三条显示路径。新增失败回归后实施，验证用户名称和提示不翻译、公共按钮仍翻译、id动态变化时恢复／重启翻译。补入message类名观察，新增8条人工UI文案。
- 最终0.1.17 ZIP解包到该隔离配置的已加载目录，Chrome管理页重载并确认版本0.1.17，刷新CoE以替换旧脚本。相同保存目标显示Runed Focus，公共文案为保存到／新建仓库页；选择后保存成功。再执行一次增幅，新词缀17（10–19），花费0.043、操作次数1与总花费／最近更改／查看详情标签显示正常；撤销后装备卡恢复单前缀。本次未重复0.1.16的导出逐字比较，不混淆版本证据。
- 14文件93测试、扩展类型、Biome、构建和包检查通过，词典6713条。隔离浏览器已确认关闭。移动目标选择、跨页拖拽、完整背包导入导出和重做仍待核对。
- 工具边界：click不支持--button right，最初未出现菜单不是扩展故障；改为mouse down right／up right后菜单正常。记录此差异，后续不得将静默忽略参数当作已执行右键。


## 0.1.18 装备诊断按行定位（2026-09-25）

- 将扩展资格诊断保留为line／message结构，解析器诊断的原文行号也继续传递。词缀结构定位标题行，范围／身份／分组问题定位属性行，未验收区块定位首行；全局或无法解析的问题不伪造行号。按行号与消息去重，避免逐条扫描已有诊断。
- 预览以列表展示原因，带行号的按钮选中原站导入框对应文本，不修改原文、不自动提交。编辑后定位与填入按钮均禁用，旧按钮仍受版本及原文一致性检查保护。有效装备资格边界未扩大。
- 两条新增回归先失败后通过：缺等阶标题第8行、缺范围属性第9行；LF与CRLF诊断一致；点击定位准确、内容不变，编辑后旧定位无效。14文件95测试、扩展类型、Biome、构建及包检查通过。最初出现非空断言格式警告，已改为闭包捕获行号后重跑检查。
- 最终0.1.18 ZIP独立解包实装，Beta English／PoE2：自造魔法法器缺范围，提示第8行且填入禁用；真实点击定位，焦点回到原输入，selection为“+40 能量护盾上限”，全文未变。截图确认问题行高亮、两栏预览及诊断可读；不同视口和长文本滚动尚未全面核对。
- 补为40（36–41）后，旧定位按钮disabled，提示重新预览。再次预览→填入→原站继续，装备卡为符文法器、物等86、护盾82、护盾前缀40（36–41）、T6。两个隔离浏览器均已确认关闭，其中首次会话在诊断去重调整后关闭，实际流程在最终重打包会话执行。


## 0.1.19 非英文语言提示（2026-09-25）

- 0.1.18 对照确认：原站 PoE2 切换 `cn` 后没有说明提示。0.1.19 增加左下角可关闭通知，说明需在右上角切为 English；不调用原站切换接口，也不更改原站语言。未知页面结构、PoE1 或扩展停用状态不显示提示。
- 两条新增测试先失败后通过，覆盖上下文分类、去重、关闭保持原站语言、离开非英文状态撤除及允许再次提示。核心／扩展15文件97测试、扩展类型、Biome（41文件）、构建及包检查通过，词典6713条，权限仍仅storage。
- 最终0.1.19 ZIP独立解包实装，在新版whats-new页通过原站菜单选择cn：一个提示出现，截图已审阅。真实点击关闭，提示为0、原站仍为cn；选择us后确认提示为0、开场简体译文恢复。再次选择cn，提示重新出现且只有一个。
- 第二次cn→us（没有手动关闭提示）之后的读取发生CDP Runtime.evaluate超时，因此本轮不把此项自动撤除计为实站通过；撤除逻辑有单元回归。该工具超时尚未归因，不能据此认定扩展故障或原站故障。隔离浏览器已确认关闭。非英文与弹窗开关的完整矩阵、窄视口覆盖情况尚待核对。


### 0.1.19 语言清理补充验收（2026-09-25，448fd16）

- 新隔离会话加载同一最终ZIP，Chrome管理页确认版本0.1.19。新版whats-new页，PoE2先选择cn，提示数量1；不点击关闭，直接从原站语言菜单切us，读取到提示0且开场“全面重构”简体译文恢复。补齐上一轮读取超时留下的证据，不据此推断上次超时根因。
- 再切cn后，从真实扩展popup取消“启用简体中文”：原站语言仍cn，提示0。重新勾选后提示1，原站仍cn。随后真实点击POE 1，DOM为poe1 selected、语言cn、提示0。
- 所有操作均通过菜单／复选框，未直接写入原站或扩展存储；浏览器已确认关闭。本轮只更新验收文档，没有修改运行代码或重跑历史97项测试。跨浏览器重启、其他语言及窄视口仍未完整验收。


## 0.1.20 连线条件编辑（2026-09-25）

- 在Beta English／PoE2模拟器建立两个不执行操作的自造节点，以真实鼠标拖入画布、连接，打开连线条件编辑。发现Link edition、Remove link、Change target、Evaluation mode、Passthrough、Conditions缺少译文，新增6条人工UI映射。
- 最终ZIP解包到原隔离会话已加载目录，管理页重载确认0.1.20，刷新CoE后从原站保存状态恢复流程。编辑连线截图已审阅，6条译文可见；实际点击“直接通过／条件判断”，原站selected value分别为pass／conditions。用户节点名Step 1／Step 2保持原文。
- 条件选用Number of Affixes。最初fill数值后没有保留，随后真实按键1、Tab、应用更改并重新打开，数值保持1。没有无扩展对照，不将先前现象归因为扩展错误。载入列表下误点隐藏画布曾报告页头遮挡，返回流程图后正常，不作为可见布局缺陷。
- 核心／扩展15文件97测试通过，3个修改JSON的Biome检查通过；构建与包检查通过，6719词条、仅storage。pnpm extension:package入口因本机SQLite存储无法打开而失败，随后直接运行仓库build.mjs与package.mjs完成构建和打包；未安装／更改依赖。本轮无TypeScript代码变化，未重跑类型检查和全仓verify。
- 浏览器已关闭。此轮仅证明条件编辑与身份切换，不证明条件分支运行、循环或制作动作链通过。条件下拉框的元属性、Conditions configuration、Value、All等仍有英文，搜索适配也待补；下一步继续沿真实条件流程验证，不缩小完整验收范围。


## 0.1.21 模拟器条件中文搜索（2026-09-25）

- 旧版实测：条件下拉框中文“词缀数量”没有扩展候选，Tab使原站退出editing。新适配限定在simulatorConditionRequirements的非hidden下拉搜索框，读取原站li[search]英文属性筛选候选，合并已有stat和人工ui词典；不读取译文反推原站身份。
- 条件候选使用保留输入焦点的方向键选择与aria-activedescendant，鼠标按下不转移焦点；离开时按扩展持有值恢复原属性。提交前重新检查当前原站选项，已被删除的候选不能提交。普通搜索沿用原键盘方式，不接管数值框／说明。
- 两项回归先后验证候选限制、虚拟选择、输入焦点、原属性恢复、鼠标按下和过期候选；首次无候选失败，实现后暴露Enter仍走精确候选的缺口，修正后全部通过。15文件99测试、扩展类型、Biome42文件、构建及包检查通过，6726词条、仅storage。本轮使用已有Node脚本构建打包，未重跑全仓verify。
- 最终ZIP解包到已加载的隔离目录，管理页重载确认0.1.21并刷新CoE。PoE2 English模拟器结束条件中，中文“词缀数量”→一项候选；ArrowDown后焦点仍INPUT、editing仍1，aria-activedescendant对应选项；Enter后原站仅显示special:num-affix，可实际点击选择。
- 中文“闪电抗性”→鼠标选择英文查询，原站结果value为78、78_1001；直接英文输入并真实按键触发筛选，结果身份及顺序相同。点击78可选入普通闪电抗性条件。截图审阅通过，条件配置、数值、全部等新增文案可见；原站自己的标签与部分其他属性仍有英文。
- 浏览器已确认关闭。本轮证明这两类搜索和选中身份，不证明全部条件／基底覆盖、多条件组保存、分支运行或真实系统IME。搜索结果最多沿用核心每个域50条候选，再按当前选项过滤；较宽泛关键词可能需缩短或细化，不声称穷尽整个条件池。


## 0.1.22 普通基底中文导入（2026-09-25）

- 0.1.21实站复现：普通符文法器英文预览已正确，但被稀有度限制和“缺少高级词缀分组”阻止填入。扩展资格增加normal，且仅无词缀输入免除高级分组要求；魔法／稀有仍要求高级分组，普通稀有度带词缀仍阻止导入。沿用原有两基底、类别、属性、特殊状态与未知区块检查，未修改搁置网站的转接资格。
- 新增两项回归，普通基底用例先失败后通过；15文件101测试、扩展类型、Biome41文件、构建及包检查通过，6726词条，仅storage。未重跑全仓verify。
- 最终0.1.22 ZIP解包到已加载的隔离目录，管理页重载确认版本后刷新CoE。自造普通符文法器文本：护盾42、物等86；预览→填入英文→原站继续成功。装备卡对应42／86，原生导出通过真实Ctrl+V取回：i为Metadata/Items/Armours/Focii/FourFocus9，r为normal，l为86，m为空数组。
- 自造普通细枝头冠文本：护盾19、物等79；相同完整链成功，卡片为19／79，原生导出i为Metadata/Items/Armours/Helmets/FourHelmetInt1，r为normal，l为79，m为空数组。两份导出q为0、s为空数组；没有把普通装备补成魔法或虚构词缀。
- 浏览器已确认关闭。以上为自造文本的原站导入证据，不是国服实际复制样本验收；不推广到所有普通装备、固有词缀、品质、孔位、符文或技能结构。完整制作与条件分支链继续待核对。


## 0.1.23 制作页计算条件中文搜索（2026-09-25）

- 0.1.22实站确认制作页条件框位于calculatorZone的requirements区域，原中文“闪电抗性”没有候选。新增这一明确容器边界，复用模拟器的当前选项过滤、虚拟焦点与原站英文提交；不扩大到全部dropdown。
- 新增区域内／区域外与Escape恢复回归，先失败后通过。核心／扩展15文件102测试、扩展类型、修改代码与测试的Biome、构建及包检查通过，6726词条、仅storage；未重跑全仓verify。
- 最终ZIP覆盖已加载隔离目录，管理页重载后首次读取仍为旧版本，稍后重读确认0.1.23，CoE页已刷新。自造普通符文法器物等86；制作条件中文“闪电抗性”有一项候选，ArrowDown后INPUT仍聚焦、原站editing为1，Enter提交英文。
- 中文查询结果身份78、78_1001，与直接英文查询的身份及顺序一致。点击78选入普通闪电抗性条件，真实按键6及Tab后等阶值为6，候选面板数量0。截图已审阅，未覆盖所有窄屏或全部条件。
- 选择原站蜕变石后计算从初始化走到完成，显示成功率5.2%、约20次；这是原站输出证据，不是对概率算法或游戏实际成功率的独立验证。结果区仍有Running method setup、Success chance、Expected costs、Confidence等英文，后续需补译。
- 浏览器已确认关闭。此项补齐制作条件的查询→选择→填等阶→原站计算链，不等于多分支模拟、全部材料、真实IME或全部装备已验收。


## 0.1.24 计算结果与累计成功率说明（2026-09-25）

- 公开DOM核对：计算结果位于calculationsZone，进度为完整“1 of 1”文本，次数为“1 out of 14 tries”，耗时由Executed in、独立b数字节点、seconds组成。仅该上下文增加动态翻译，保留数字字符串和耗时子节点；12条人工UI译文补齐计算阶段及提示。
- 原站Confidence提示明确使用1 - (1 - P) ^ N，P为单次成功概率、N为尝试次数，因此译为“累计成功率”，解释为指定次数内至少成功一次，不使用统计置信区间含义。原tooltip属性未改写，仅翻译显示出来的文本节点，公式保持。
- 新回归先失败后通过，覆盖带千位分隔次数、重新计算更新、耗时节点身份、移出上下文及关闭恢复。15文件103测试、扩展类型、Biome42文件、构建与包检查通过，6738词条、仅storage。未重跑全仓verify。
- 最终ZIP解包到加载目录，管理页重载确认0.1.24并刷新原站计算页。物等100普通符文法器，闪电抗性不限等阶、蜕变石：原站单次结果7.28%，约14次，累计64.566%，阶段与耗时中文显示。此为原站输出比对，不是独立算法正确性证明。
- 实际悬浮提示已中文显示且公式相同，截图已审阅。真实按键把尝试次数改为30，累计输出89.174%；通过真实popup停用翻译，结果说明恢复英文，累计89.174及最新耗时2.517保留。浏览器已确认关闭。
- 不从该例推广为全部材料／计算方式／模式通过；双语、所有窄屏、复杂条件路由与其余完整门槛仍待核对。


### 0.1.24 两步骤条件重试与导出对照（2026-09-25，4767c04）

- 同一最终ZIP启动隔离会话，English／PoE2普通符文法器、物等100。中文搜索闪电抗性→选择78→选择蜕变石→原站“创建模拟流程”，导出确认第一步动作及结束条件78、failure=restart。
- 原站复制步骤确认后未关闭对话框，步骤仍1；同会话通过popup停用翻译后再次点击原站Proceed，仍open且步骤1。此为停用对照，不是全新无扩展环境，不据此归因为原站或扩展。关闭对话框、恢复翻译，使用“添加步骤”继续。
- 新增第二步：制作方式→通货→增幅石，拖入画布并连接第一步。两步结束条件均为闪电抗性78，第一步结束priority=0，通往第二步的直接连线priority=1；第二步无后续路线时重新开始。第一次连续配置后导出显示第二步无条件结束，未运行；重新选条件、检查编辑状态、随后另次操作应用，导出才包含78。原站异步保存时序尚未深入归因。
- 固定items、目标10，导出确认后运行。原站显示运行次数88、已执行步骤168、已完成10、目标100%。第一步执行87次，其中结束6、进入第二步81；第二步执行81次，其中结束4。这证明两条成功路径及失败重试实际发生；运行次数88与首步87的口径差异未解释，不据此改动原站统计。
- 首件及随后9次“下一个”逐件检查都含目标闪电抗性；数值依次24、11、16、26、14、7、26、29、23、26，范围均保留。再点一次回到首件（护盾54、闪电抗性24、成本0.341），确认完成一轮浏览。结果截图已审阅，中文词缀叠加与用户步骤名称共存。
- 清除本任务生成的结果回到编辑态，分别启用／停用翻译执行原生Export Simulation，通过真实Ctrl+V取得两份文本，均1406字符且逐字一致。自造配置保存在fixtures/two-step-lightning-route.json，仅格式化缩进；没有复制同行数据文件、私人物品或完整页面。
- 浏览器已确认关闭。本轮只提交验收记录与自造配置，不修改运行代码、不重跑历史103项测试。独立多出口条件组、显式回边、复制异常、真实IME、更多装备及其余完整门槛仍待验收。


### 复制步骤异常的全新无扩展对照（2026-09-25）

- 针对上轮复制确认未关闭，另开全新会话且不加载扩展；管理页extensions-item为0，浏览器标识为HeadlessChrome/151.0.0.0。Beta English／PoE2进入Simulator，新增一个空步骤，点击标题→Copy→Proceed。
- 对话框仍open，步骤数量1；页面error事件捕获Uncaught TypeError: Cannot read properties of undefined (reading 'advancedRouter')，来自原站package_poe2.js?v=1790166276第1行。这排除了扩展是该次异常的必要原因，但没有确定原站缺失对象的更深原因，也未推广到所有浏览器。
- 完整最小复现保存在upstream-issues.md，尚未对外发送。扩展不修改原站复制实现；可继续使用已验证的新增步骤路径。浏览器已确认关闭。
- 本轮只有诊断和文档变更，未重跑历史测试；快速配置后的条件保存时序仍待独立调查。


### 0.1.25 标签筛选（2026-09-25）

- 最终 ZIP 在 Chrome 扩展管理页确认版本 0.1.25；新版英文 PoE2 符文法器制作页的 20 个正向、20 个排除标签显示中文，词缀 `.modTag` 复用相同映射。
- 实际点击“闪电”与“非闪电”，69 个词缀组分别有 5／64 个不带原站 `filter` 标记。停用后英文 Lightning／Non-Lightning 重新操作，全部 69 组的 group、filter 状态、search 属性逐项相同；停用即时恢复英文且当前过滤状态不变。
- 译名为人工 UI 表；用户提供的高级文本支持伤害、元素、冰霜、闪电、抗性、魔力、暴击、召唤生物、宝石等用语。Caster 暂用“施法”，尚未核对国服独立标签的真机显示。不翻译未知特殊标签。
- 本轮未验证所有基底／标签组合，不把当前对照扩大为全部制作行为验收。真实输入法、多出口流程与更广装备导入仍待推进。


### 0.1.26 条件候选截断修复（2026-09-25）

- 修正 0.1.21 记录的“先截取再过滤”限制：核心可接收当前控件的候选判断函数，在排序／每域 50 项截断前执行；普通搜索及精确身份语义保持不变。仍不承诺穷尽所有词典缺失项。
- 构造 60 条无关短译名挡住当前有效词条的回归，修改前零候选、修改后可选择并提交英文。另验证范围内仍有 50 项上限，临时范围不会污染后续普通搜索。
- 最终 ZIP 解包启动独立浏览器，新版英文 PoE2 符文法器的计算条件输入“抗性”得到 7 个候选；通过实际 option 控件选择闪电抗性，输入变为 `#% to Lightning Resistance`，原站匹配身份为 `78`／`78_1001`。未在实站构造人为词典，截断缺陷证据来自回归测试。
- 本轮没有真实系统输入法验收，没有扩大装备导入或流程运行范围。


### 0.1.27 单流程导入与纠错引导（2026-09-25）

- 先在 0.1.26 确认单个流程误用“全部导入”会被原站拒绝，并按提示找到 New Simulation → Import a Simulation。该拒绝是入口格式差异，不是扩展故障。
- 0.1.27 最终 ZIP 独立会话确认错误标题、分段引导与“导入一个流程”入口为中文；依照提示导入 `fixtures/two-step-lightning-route.json` 成功。
- 实际点击“导出模拟流程”，通过真实 Ctrl+V 读取导出；解析后的 JSON 与样本逐字段完全相同，包含两个步骤、原 ID、连线、结束条件、起始物品和运行配置。测试只恢复配置，本轮没有重新运行随机制作，也没有验证全部批量导入格式。
- 107 项相关测试、类型、格式、构建及包检查通过；导入文案的回归保护强调节点和关闭恢复。本轮没有新增权限或数据源。


### 0.1.28 导入错误提示（2026-09-25）

- 以自造 `{invalid` 和 `{}` 在原站分别核对 Invalid JSON／Invalid Dataset；新增人工中文译文及真实 textarea 的多行占位说明，沿既有 UI 来源，不新增网络能力。
- 最终 ZIP 独立浏览器验证占位说明、两类错误均为中文；实际点击“返回”后输入分别仍为 `{invalid`／`{}`，扩展不修改提交数据或原站校验。
- 构造回归复现 0.1.27 导入区独立句号被附加右引号的问题；移除分段引号拼接，保留强调节点与原文恢复。该标点问题的复现来自测试，不宣称实站已有此特定错误句。
- 108 项相关测试、类型、格式、构建及包检查通过。未验证 Pastebin／Gist 远程导入行为；新增占位译文只转述原站能力。


### 0.1.29 保存、刷新与批量备份（2026-09-25）

- 0.1.28 会话中导入自造两步骤样本，保存为 `Runed Focus 中文回归 2026-09-25`。列表名称原样显示。刷新后原站保留当前流程；重新导出仅 name、id、lastModified 与未保存样本不同，其余字段一致。
- 第一次仍在保存列表时调用“导出模拟流程”失败，随后读到旧剪贴板；该次结果作废。返回流程图、成功点击导出后重新粘贴，才形成上述证据。
- “全部导出”包含 current 与 saved。另一独立会话导入前保存行数为0；原站覆盖确认后 saved 中唯一保存项逐字段一致，但 current 仍为目标会话的空流程。批量整体相等断言失败，因此不能声称自动恢复当前编辑状态。
- 单击名称也不会载入。经快照找到保存行的“载入”按钮，实际点击后单流程导出与保存前逐字段完全一致（包括名称、ID、两步骤、条件、连线和起始装备）。此前把单击名称当载入的检查已被此证据纠正。
- 0.1.29 最终 ZIP 独立会话确认“流程数据导入／这将替换当前数据，是否继续？／此操作无法撤销”均中文；关闭二次确认后保存行数仍为0。没有覆盖用户已有数据，所有会话均为本轮自造。
- 108 项相关测试、类型、格式、构建及包检查通过。本轮未验证多个保存项、冲突合并、跨游戏数据或浏览器重启；不将单项往返外推为完整备份兼容性。


### 0.1.30 新版介绍页再核对（2026-09-25）

- 在 `/whats-new` 英文 PoE2 模式检查可见内容。首次全新会话默认 PoE1，扩展未启用；切换 PoE2 后返回介绍页，既有译文生效。此条件与既有支持边界一致。
- 补充7条人工界面译文，材料／基底名来自既有国服目录；保险箱经已登记 PoE2DB 简中页交叉核对，来源详见 data-sources.md。
- 最终 ZIP 独立会话确认装备状态、瓦尔注能装置、四种溶剂、特殊首饰和分区标题中文。桌面 viewport=1266、scrollWidth=1251，截图观察长句正常换行，无横向溢出；不外推移动端布局。
- 108项相关测试、类型、格式、构建及包检查通过。尚未翻译 Distilled Emotions、部分 synth／augment／elevated 说明、特殊符文池简称与 PoE1 专属段落；未将当前页面宣称为完整汉化。


### 0.1.31 启动设置时序（2026-09-25）

- 检查发现内容脚本原来在初始设置读取完成后才订阅变化。延迟读取的构造测试确认读取期间尚无监听；修复为先订阅，读取期间事件值优先，初始化完成后再挂载界面功能。
- 新测试覆盖旧启用快照晚于新停用事件返回、随后重新开启双语，以及没有并发事件时初始开启／关闭两种情况。111 项相关测试、类型、格式、构建与包检查通过。
- 最终 ZIP 管理页确认0.1.31；实站正常启动显示中文。实际 popup 停用后刷新，首页按钮恢复 Import an item／Simulator；重新开启并勾选双语，原页面变为“导入装备 · Import an item／流程模拟 · Simulator”。
- 实站只验证正常设置与刷新链；微小时序窗口由确定性回归覆盖，未声称在真实 Chrome 中稳定复现该竞态。未新增权限或依赖。


### 0.1.32 丝质之袍胸甲导入（2026-09-25）

- 对照完整计划，继续任务五的防具结构覆盖。使用自造稀有丝质之袍、物等86、前缀护盾+40(36-41)／等阶6、后缀闪电抗性+18(16-20)%／等阶6，先原生英文导入，再以最终 ZIP 中文预览填入走完整导入链。
- 两次原生导出字段一致：基底 `Metadata/Items/Armours/BodyArmours/FourBodyInt5`，`LocalIncreasedEnergyShield5` 值40、`LightningResist3` 值18，l=86、r=rare、q=0、空插槽和无特殊词缀标记。这证明本样本中英文导入等价，不证明游戏里能生成输入的任意等阶／范围组合；原站按自身规则解析。
- 最终包另外验证 magic 双词缀与 normal 空词缀，各自通过中文预览→填入→继续→原生导出；基底、物等、稀有度和词缀值符合预期。三个样本都没有手填护盾总值、品质、需求或插槽区块，不能外推这些附加区块的真机格式。
- 新回归先被原档案拒绝；增加档案后又发现 `Item Class: 胸甲` 未映射，修复独立文本类别别名为 Body Armours。未修改旧制作引擎或材料资格。跨类别、品质、插槽、异常等阶仍拒绝。
- 173 项相关测试（扩展、术语核心及 item-core 类别／解析／导出）和类型、格式、构建、包检查通过；剩余基底、首饰、权杖、多行词缀等继续保留。


### 0.1.33 普通品质导入（2026-09-25）

- 三种基底的自造普通英文品质20样本均经原站导入导出确认 q=20、qt=null、l=86、m=[]；分别为 FourFocus9、FourHelmetInt1、FourBodyInt5。
- 扩展允许单条普通品质整数0–20%，保留 augmented 标记；重复、负数、小数、超20以及催化品质仍拒绝。回归包含0、1、20与非法结构，修改前失败、修改后通过。
- 最终 ZIP 中文完整预览／填入／原站导入链：魔法细枝头冠 q=10、稀有丝质之袍 q=20，各有 LocalIncreasedEnergyShield5 值40；普通符文法器重新单独核对 q=15、FourFocus9、m=[]，三者qt=null且l=86。
- 连续自动操作曾在控件重建期间找不到导出按钮；一次普通法器读到旧胸甲剪贴板，作废该次结果。最后以临时剪贴板标记替换旧内容、分阶段真实导出和粘贴，确认法器基底及不同品质15。不能只核对品质和词缀数量就认定导入成功。
- 113项相关测试、类型、格式、构建与包检查通过。自造样本不代表完整游戏真机格式；催化、特殊品质和镶嵌仍待推进。没有修改旧制作引擎的品质运算。


### 0.1.34 空插槽导入（2026-09-25）

- 英文对照：普通丝质之袍 `Sockets: S S` 原生导出 ns=2、s=[]；普通细枝头冠／符文法器 `Sockets: S` 导出 ns=1、s=[]。基底分别为 FourBodyInt5、FourHelmetInt1、FourFocus9，物等86。法器首次复制仍是临时标记，待异步复制完成后读取原生JSON，不使用旧剪贴板作为证据。
- 档案分别开放最多2／1／1个空S孔，单行声明；这是已验收范围，不宣称游戏孔位上限。拒绝重复行、连接格式、其他孔位类型和镶嵌效果，不修改旧制作规则。
- 最终 ZIP 中文完整预览→填入→原站导入→导出：普通法器单孔，魔法头盔单孔，稀有胸甲双孔；均 q=10、l=86、s=[]，后两者 LocalIncreasedEnergyShield5 值40，基底和稀有度逐项核对。
- 首次最终包检查的临时粘贴框被原站侧栏覆盖，仅调整测试框位置后完成；不修改原站业务节点。复制前写入测试标记并等待有效JSON，避免旧剪贴板假阳性。
- 114项相关测试通过；修正非空断言告警后另跑15项导入测试、类型、格式和最终构建／包检查均通过。真实国服空孔文本、已镶嵌物和特殊孔位仍待进一步覆盖。


### 0.1.35 分类选择与导航（2026-09-25）

- 观察初始15个分组及14组对应的可见类型项，新增89条装备分类／分型译文；不翻译未核实的 Tablet、Waystone 及其分型。仅显示层变化，不改 value、URL 或选择事件。
- 英文选择 Body Armour → Body Armours (INT)，原站直接载入默认基底，URL group=1／class=3／item=3416。最终 ZIP 中文选择“胸甲 → 胸甲（智慧）”，三个身份值完全一致；截图显示导航“胸甲／胸甲（智慧）／符文羽饰之衣”。
- 初始分组名称中文显示，已选导航中文显示；选择完成后隐藏的classSelector内容恢复英文，符合既有隐藏区域规则，不将隐藏节点当作漏译。
- 多次切换分组后原站固定页头曾遮挡按钮，Control+Home未解决；回到初始页面后正常点击完成对照。该布局现象不计入本轮修复。桌面width=1251、viewport=1266，无横向溢出。
- 114项相关测试、类型、格式、构建及包检查通过，6755→6844条词条。只对胸甲智慧路径做选后身份对照，不把它扩大为全部89条的完整功能验收。


### 0.1.36 搜索控件状态变化（2026-09-25）

- 合成 DOM 回归先复现四种失败：候选出现后输入变为只读、禁用、移出搜索区域，以及稳定候选所在条件框被设置 hidden 类。
- 搜索资格排除只读与禁用控件（含 fieldset 继承禁用）；点击前重新核对领域与父容器，观察器监测影响已核实选择器的 class/id/type/readonly/disabled 属性。状态恢复后仍可重新输入查询。
- 118项核心与扩展测试及扩展类型检查通过。本轮是自动化边界回归，未新增真实 CoE 浏览器验收，不代表已验证原站一定会触发上述每种状态，也不替代真实中文输入法验收。

- 补查 fieldset 继承禁用时，happy-dom 的 `:disabled` 仅检查元素自身属性，导致该额外测试失败；核对其选择器实现后移除不适用于该模拟环境的断言。隔离 Chrome 151 空白页实际验证 `<fieldset disabled><legend><input></legend><input></fieldset>`：普通 input 匹配 `:disabled` 为 true，首个 legend 内 input 为 false。此项仅证明原生选择器语义，不是扩展端到端验收。构建、打包和格式检查通过，权限仍仅 storage。


### 0.1.37 Data 装备卡属性（2026-09-25）

- 从新版 Data 可见分类与结果卡核对，沿0.1.35人工类别对应关系补27条词典记录。装备卡 `.item .property` 的 Energy Shield／Runic Ward／Focus／`, Int` 分段增加限定上下文译文；数字不重写，关键词ID不改。实际版本0.1.37，词典6871条，仅storage权限。
- Chrome 151隔离会话以最终ZIP解包目录安装，PoE2／English：Data 输入“符文法器”并选择 Runed Focus，查询结果稳定后为两张卡。图片身份 FourFocus9／FourFocus9Verisium 与0.1.36基线一致；能量护盾42，第二张符文结界32，两者物品等级100、需求等级45／智慧64；关键词ID仍 EnergyShield／EnergyShield／Ward。
- 从真实 popup 关闭翻译：查询仍 Runed Focus，两张卡的英文标题、Focus、Energy Shield、Runic Ward、`, Int` 恢复，所有数值／图片路径相同，辅助节点为0。
- 旧包会话尝试点击分类59（Focus）时先被底部广告挡住，滚动居中后点击返回成功，但稳定URL与筛选反馈未见新增类别条件。因此不将这次操作计为类别过滤验收；本轮证明查询结果显示与恢复，不证明全部分类筛选／排序。
- 新回归先失败，后验证分段翻译、数值及ID保留、移出属性区域恢复、撤掉property类恢复、关闭恢复。119项核心／扩展测试及类型检查通过，构建和ZIP检查通过；未验收真实IME或其他需求属性排列。


### 0.1.38 Data 筛选对照与搜索清空（2026-09-25）

- 新建无扩展／0.1.37两会话，分别真实点击Focus／法器：URL均itemCategorySelector=59，选项均selected，51张结果卡的图片身份、直接数值文本节点、关键词ID和顺序逐项相同。需求标签有意翻译，不能用包含标签的整段textContent比较数字。原站数据包含其他用途条目，51不代表当前游戏可制作基底数量。
- 叠加英文Runed Focus／中文“符文法器”候选查询，URL参数一致，两张图片身份FourFocus9／FourFocus9Verisium相同。上一轮类别未生效原因未确定，不能据新结果推断旧操作成功。
- 真实复现0.1.37首击X失败：mousedown落在empty按钮；focusout移除提示后按钮从x=395.46／height=65变为x=236.33／height=42，mouseup和click落到外层input容器，查询保留。第二次X才清空并保留分类59、恢复51张卡。无扩展一次X即可。
- 修复普通搜索提示布局：视口定位，按输入框边界设置宽度和位置，下方空间不足时向上展开，滚动／resize重定位，停止时移除监听。条件下拉框仍沿用保持输入焦点的现有布局。
- 首次绝对定位试验被原站输入父容器overflow:hidden裁掉，未通过；最终ZIP改为fixed，Chrome151新会话成功选择候选。一次X清空，mousedown／focusout／mouseup／click全程按钮x=259.67／y=198／height=42不变，click落在empty。
- 最终包420×800视口：输入框x=20／bottom=450，候选x=20／top=450／width=271.67／bottom=630.52，截图已查看且真实候选点击成功。仅该Data窄屏样本，不等于全站响应式验收。
- 筛选反馈Searching是当前查询标签，限定filterFeedback译为“搜索词”；Clear all译为“清空全部筛选”，其他区域保留原有语义。120项相关测试、类型、构建及ZIP检查通过，6871条词条，仅storage权限。真实IME、完整模式和所有搜索框视觉回归仍未完成。

- 最终包点击筛选反馈内原站按钮后，URL回到mode=items、查询为空、结果卡为0并显示中文空筛选提示。原站label使该按钮的可访问名称成为“筛选设置”，按可见“清空全部筛选”名称定位失败后改用已观察DOM定位正常点击；未修改原站可访问名称。


### 0.1.39 蓝玉戒指基底属性与普通后缀（2026-09-25）

- 扩展文本资格新增Sapphire Ring／戒指，必须且仅有一组简单`{ 基底属性 }`、一行冰霜抗性完整数值范围；普通装备无显式词缀，魔法／稀有另支持冰霜抗性后缀（完整高级头／等阶／范围）。同一翻译模板的implicit和explicit分别去重，组内重复仍拒绝。其他基底不自动继承。
- 基底头由既有文本模块转换为`{ Implicit Modifier }`，含标签或额外语义且不能完整转换的头不放行。基底缺失、重复、错分组、缺范围、值越出输入范围，以及首饰品质、空孔位仍阻止填入。没有修改旧做装资格或制作引擎。
- Chrome151独立无扩展会话：自造英文普通／魔法／稀有Sapphire Ring，l=86，基底冰霜抗性25(20-30)；后两者另加后缀18(16-20)、Tier6。原站导出均i=Metadata/Items/Rings/FourRing4、ip=[["25"]]、q=0、qt=null、ns=null、s=[]；普通m=[]，魔法／稀有m=[ColdResist3,v=[18]]，r分别正确。
- 最终0.1.39 ZIP独立解包实装：分别粘贴对应中文样本→预览→填入英文→原站继续→导出→真实Ctrl+V取证；三个导出的上述字段分别与英文基线一致。每次复制前先写入不同标记，避免读取上一件装备。未以只生成英文文本代替原站结果。
- 中文预览保留基底与后缀组，允许二者共用冰霜抗性文本模板；模板来源id的explicit前缀不是制作来源身份。原站可自行规范化Tier等信息，本轮只证明这三件自造样本的转换与导出一致，不证明输入范围／等阶属于国服实际可掉落组合，真实国服戒指样本仍待补充。
- 两项新增回归含正常链及拒绝矩阵，最初正向用例失败，实现后122项相关测试通过；类型、格式、构建和ZIP检查通过。词典仍6871条，权限仅storage，无新增来源或生产依赖。


### 0.1.40 带标签的基底属性头（2026-09-25）

- 共享文本模块识别简单基底属性头及逗号分隔标签，转换头为Implicit Modifier并保留标签文字。新增knownImplicitHeader文本接口；未知前置／后置限定词、空标签或额外破折号段不通过结构检查，增效段完整保留，不因解析出的tags为空而被吞掉。
- 扩展仅对已开放的蓝玉戒指接受元素／冰霜／抗性或Elemental／Cold／Resistance标签；未知及不匹配标签继续阻止填入。其他装备不会因此新增导入资格，原独立制作桥接仍保持既有拒绝语义（文本回归有覆盖）。
- 无扩展英文基线：自造普通Sapphire Ring，l86，`{ Implicit Modifier — Elemental, Cold, Resistance }`，抗性25(20-30)，原站导出FourRing4、r=normal、ip=[["25"]]、m=[]。
- Chrome151最终0.1.40 ZIP独立解包实装，中文`{ 基底属性 — 元素, 冰霜, 抗性 }`预览保留标签，填入／继续／导出完整链成功。真实Ctrl+V读取JSON：i=Metadata/Items/Rings/FourRing4、l=86、r=normal、ip=[["25"]]、m=[]、q=0、qt=null、ns=null、s=[]，与英文基线一致；复制前独立标记排除旧剪贴板。
- 本轮中文样本为自造，不声称该基底的国服高级头实际必定含上述全部标签；用户腰带样本只提供带标签结构参考，不据此放行腰带。复杂基底属性、品质和其他首饰仍待扩展。
- 初始核心导出和扩展正向回归均失败，修复后18文件177项相关测试通过（包含item-core解析／导出）；扩展类型、格式、构建及包检查通过，6871词条，仅storage权限。无新依赖和来源。


### 0.1.41 装备预览自适应布局（2026-09-25）

- 0.1.40实装420×800：原站导入弹窗宽563.33，双栏预览x=24／278且各宽270，既超出视口又彼此重叠。原站原文textarea固定内容宽500，textareas原有content-box使width100%加上padding后超出网格。
- 面板自身文本框改border-box和纵向缩放，网格按可用宽度自动切单／双栏；只在noticeDialog内存在扩展导入面板时设置弹窗宽760、max-width为视口减24，以及原文输入框宽度约束和flex内容层扩展。原站no-max-width覆盖初版max-width规则，最终限定规则使用!important；样式随面板销毁，不持久写原站style属性。
- 最终0.1.41 ZIP重新实装Chrome151：桌面弹窗宽760，两栏同一y、x=277／639、各宽350；420×800弹窗宽396、clientWidth=scrollWidth=377，两栏同x=36、不同y、各宽332.67。最终截图已查看，窄屏真实点击“填入英文到原站导入框”成功，输入内容正确且按钮禁用。
- 真实popup停用：导入面板数量0，原站dialog maxWidth恢复none，已经填入的英文文本未改变。本轮未点击原站继续，不把布局验收报告为新的原站装备结果验收；普通蓝玉戒指文本使用既有已验收结构。
- 初步迭代曾仅消除文本框重叠、仍有原文输入框横向溢出；后续又发现桌面flex内容层不展开，均修正后才记录最终结果。超长文本、全部诊断滚动定位及其他尺寸仍待覆盖。
- 123项核心／扩展回归、类型、格式、构建及ZIP检查通过；纯布局调整以真实浏览器为主，不新增复述CSS实现的测试。词典6871条，权限仍仅storage，无新依赖。


### 0.1.41 长文本诊断补验（2026-09-25）

- 使用既有最终ZIP解包目录实装，Chrome151、PoE2／English；本轮未修改运行时代码或版本。自造固定样本见`fixtures/long-import-missing-range.txt`，共69行，第7–66行为60条备注，第69行冰霜抗性缺少高级范围。
- 原文输入框主动滚到顶部，再真实点击“定位第69行”：选中文本为“冰霜抗性 +25%”；scrollTop=916、scrollHeight=1262、clientHeight=346，选中末行确实处于可见底部，截图已查看。不是只验证selectionStart／selectionEnd。
- 将末行改为“冰霜抗性 +25(20-30)%”后，旧定位按钮和填入按钮立即禁用，提示“原文已改变，请重新预览”。重新预览后可填入；原文预览仍69行，提交框变为8行英文装备文本，60条备注全部不进入提交文本。本轮不点击原站继续，不重复宣称新的装备结果验收。
- 窄屏420×800，把每条备注追加“，这是仅用于检查换行和定位的自造说明”12次，源文本共13,880字符／69行。从输入顶部点击同一诊断，仍选中末行；scrollTop=11716、scrollHeight=12062、clientHeight=346，末行可见。dialog clientWidth=scrollWidth=377，无横向溢出；最终截图已查看。
- 已证明这两件自造缺范围样本的末行定位、可见性和修改失效链；不推广为所有诊断类型、任意长度或所有系统输入法已验收。未添加人为滚动逻辑，继续使用Chrome原生焦点／选择行为。


### 0.1.41 三步互斥出口补验（2026-09-25）

- 沿既有最终ZIP实装，Chrome151、PoE2／English。从自造两步流程经“新建模拟流程→导入一个流程”导入，再通过实际UI添加第三步；保持“不执行操作”，无有效路线时结束。新步骤先位于simulatorStepList，必须真实拖入simulatorCanvas后才建立出口；标题点击只打开步骤编辑，不等于选择连线目标。
- 第一条新增路线从Step1到Step3，条件组and，普通冰霜抗性id77；原Step1→Step2改为not同一条件，两条互斥。选择过程为中文“冰霜抗性”→英文查询→原站普通Suffix选项；另有精华77_1001，本轮未选。保留Step1／Step2原有闪电抗性78结束条件。
- 原生导出确认：Step1结束优先级0；至Step2为not77、优先级1；至Step3为and77、优先级2；Step3 action=null、failure=end。自造配置已保存`fixtures/three-step-cold-lightning-route.json`，只格式化JSON，不含同行规则库或私人物品。
- 固定目标10件实际完成：原站总计显示35轮、66步、10件；步骤表Step1执行34次，直接结束2件，去Step2共28次，去Step3共4次；Step2结束4件，Step3结束4件。原站总轮数35与Step1动作数34分别记录，不自行解释为相同指标。
- 通过原站“下一个”逐项读取10件保存装备，顺序为冷／电／电／电／冷／冷／电／电／电／冷；4件带冰霜抗性、6件带闪电抗性，与结束路径计数对应。本次随机结果不是固定概率基线，重跑不要求得到同样计数。
- 清除本任务生成的结果回到编辑态，导出与运行前逐字相同；真实popup停用翻译后再次原生Export Simulation并Ctrl+V读取，两份均1990字符且逐字一致。每次先复制独立标记，防止旧剪贴板误判。临时取证框曾覆盖清除按钮，仅移动该测试框后完成正常点击。
- 条件候选需等待原站异步更新并重新获取快照；第一次按旧定位方式选择失败，使用当前可见选项后成功。未强行点击隐藏选项，也未用页面执行函数修改流程状态。
- 浏览器errors普通输出曾只有空条目；改用JSON后确认多条来自原站AdZone.resizeRun，报null.getBoundingClientRect，堆栈package_poe2.js?v=1790166276:1:48285。本轮未隔离其原因，不声称无浏览器异常，也不据堆栈断言扩展完全无关；目标流程运行和导出核对已成功。
- 本轮仅文档与自造样本变更，运行时代码和版本不变。补齐两个互斥独立出口的编辑／运行／导出证据，不代表嵌套多条件组、显式回边、全部优先级交互或全流程验收完成。


### 0.1.41 条件占位符显示补验（2026-09-25）

- 已打包扩展的独立会话，PoE2／English。进入副手法器制作页并打开计算器条件候选，普通冰霜抗性选项value=77、search=`suffix+#% to cold resistance`；精华选项value=77_1001单独保留，没有合并身份。
- 候选中的原站 `.stat.textContent` 仍为 `+#% to Cold Resistance`，相邻扩展host的open Shadow DOM为“冰霜抗性 +#%”。选择普通77后，已选条件继续显示该中文，host有布局矩形，最终截图已检查。保留英文不是漏译；只读outerHTML或不包含Shadow DOM的快照会遗漏中文。
- 直接选择未过滤长列表时曾被固定页头遮挡，未强行点击；在原站条件搜索框输入Cold Resistance并触发键盘检索，缩小到普通／精华两项后，通过真实点击完成选择。未据此认定原站滚动问题已修复。
- 本轮仅核对条件显示，不运行计算或新增制作概率结论；核心与词缀显示层共30项测试通过。运行时代码、词典及版本未改变，无需修复已正常工作的占位符匹配。


### 0.1.42 首页功能说明（2026-09-25）

- 原站公开DOM中六张介绍卡片位于`#homeFeatures`。补齐14段标题／说明的人工简体译文，仅在该容器使用；现有标题译法沿用。图片内英文不属于文本节点，本轮不改图。
- 两项新增集成测试先失败后通过，覆盖图标节点身份、源文换行恢复、区域外同文不译、可编辑文本保护、动态挂载和移动后恢复最新英文。相关核心／扩展共17文件125项测试通过，扩展类型、格式、构建及ZIP检查通过。词典仍6871条，权限仅storage，无新来源或生产依赖。
- 最终0.1.42 ZIP独立解包实装Chrome151，PoE2／English：六张卡片标题／说明均为预期中文，桌面截图已查看。通过原生悬浮第一张卡片读取其中文说明；不改原站展示动画。
- 真实popup取消启用后，六张卡片全部恢复完整英文，12个图片节点与关闭前逐一为同一对象。本轮未新增全首页、图片内文字或窄屏排版的覆盖承诺；原站操作说明区仍有待翻译片段。


### 0.1.43 首页操作说明（2026-09-25）

- 根据公开DOM核对六条说明：鼠标悬停后左击应用制作方式、按住Alt切换词缀说明、按住Shift固定提示框并查看更深详情、Ctrl+Z撤销、左击词缀池增删当前物品词缀、右键菜单。翻译仅作用于instructions区域的已知文本片段，不改写图片、快捷键字母或节点顺序。
- 新增整段合成DOM回归先失败后通过，检查六句拼接含义、区域外Hold不翻译、图标节点保留及停用后innerHTML逐字恢复。18文件126项相关测试、扩展类型／格式／构建／ZIP检查通过。词典6871条，仍仅storage权限。
- 最终0.1.43 ZIP独立解包加载Chrome151，PoE2／English；六条提示均显示中文，桌面截图已查看，无图标与文字重叠。真实popup停用后全部恢复英文，六个图片与Hover／Z两个键帽节点逐一保持同一对象，Hover文字恢复原文。
- 本轮验证说明显示与恢复，没有重新执行六种制作操作，不把提示翻译当作快捷键业务行为新增验收；窄屏和中英对照下的完整排版仍待补验。


### 0.1.44 操作说明换行与对照空白（2026-09-25）

- 0.1.43最终包，420×800：中文模式说明区clientWidth=scrollWidth=361，但横向flex把“悬停／应用”等压成竖排；中英对照clientWidth=361、scrollWidth=409，截图确认溢出。说明行改普通文字流、键帽inline-block不拆行、图标垂直居中；只针对instructions直系说明行，启用时挂载style、停用时删除，不写原站inline style。
- 初次布局修复后又发现中英对照去掉原文两端空白，导致相邻片段粘连。新增回归先失败，文字层改保留原始空白；使用替换回调，文本中的替换语法字符不作为替换指令解释。关闭仍恢复原文。
- 最终ZIP独立解包实装Chrome151／PoE2／English：420×800中文与中英对照均clientWidth=scrollWidth=361；1280×900中英对照均为962。窄屏截图已查看，短语按文字行换行，键鼠图标保留；切换对照后样式数量为1。
- 窄屏中英对照搜索“符文法器”出现两个候选，真实点击普通符文法器后原站输入为Runed Focus。本轮只补候选可点击与英文提交，不替代既有原站结果身份对照验收。
- 真实popup停用后布局style数量0，说明行computed display恢复flex，六个图片仍为同一节点，说明恢复英文，已提交Runed Focus不变。全页另有原站横向溢出，本轮仅修复说明区，不声称全站移动端兼容。
- 18文件127项核心／扩展测试、类型、格式、构建及ZIP检查通过；纯布局不新增复述CSS的测试，实际渲染为验收依据。6871词条、仅storage权限，无新增依赖或来源。


### 0.1.45 法器双行复合前缀（2026-09-25）

- 根据原站导入输出核对Runed Focus的能量护盾百分比／魔力上限双行前缀。仅放行前缀、两行顺序分别为已登记身份4015621042／1050105434的组合；普通词缀数量按组计数。没有把任意两条合法单行词缀拼接当作已验收复合词缀。
- 英文自造基线l86、magic：20%护盾与10魔力，初始输入Tier1及范围16–20／8–10。原站规范化为Serene、Tier5、范围14–20／9–16；原生导出为FourFocus9，唯一m={k:LocalIncreasedEnergyShieldAndMana2,v:[20,10],f:[],p:null}。这说明本扩展并不验证游戏词缀实际等阶或生成区间；不能从导入成功反推自造输入为真实国服掉落。
- 最终0.1.45 ZIP独立解包实装Chrome151，中文固定样本见`fixtures/focus-hybrid-prefix-zh-CN.txt`，采用原站显示范围和Tier5。预览中仍只有一个Prefix Modifier头和两行属性；真实点击填入／继续／导出后，JSON与英文基线相同：i=Metadata/Items/Armours/Focii/FourFocus9、l86、r=magic、m上述单一复合词缀、q0、qt=null、ns=null、s=[]、ip=[]。词缀名和标签中文保留，原站按属性识别。
- 两次原生导出均以独立标记排除旧剪贴板。中文结果第一次过早粘贴得到标记，未计为成功；再次触发复制、下一次工具调用再Ctrl+V才得到有效JSON。未读取同行执行源码或规则库。
- 新增正向与拒绝矩阵先失败后通过；缺行、错后缀、未核对组合、第三行、缺范围、其他类别、异常等阶、稀有装备另一个前缀重复魔力均拒绝。18文件129项相关测试、类型、格式、构建／ZIP检查通过；最后测试格式调整及稀有重复检查后单文件20项再次通过。6871词条、storage权限，无新依赖和来源。
- 实站只验证上述魔法样本，不宣称所有复合前缀、跨词缀相同属性、不同排列或更多装备结构完成；完整路线仍待推进。


### 0.1.46 复合与独立魔力同时存在（2026-09-25）

- 原站英文自造稀有Runed Focus、l86：复合前缀20%护盾／10魔力，另一个普通前缀48魔力。高级描述分别显示两组，简要描述合计58魔力；原生导出m为LocalIncreasedEnergyShieldAndMana2:[20,10]与IncreasedMana4:[48]，不是一个合并词缀。
- 将重复检查区分为已验收法器复合来源、普通显式来源与基底属性来源；只因已核对组合可共享魔力身份，不放开同类重复。0.1.45拒绝这一合法组合的回归按新证据更新，同时补正向两头／两数值保持、两个复合组、两个普通魔力、魔法前缀超限、错后缀及同组重复拒绝矩阵。新正向测试先失败后通过。
- 最终0.1.46 ZIP独立解包实装Chrome151，PoE2／English。中文样本见`fixtures/focus-overlapping-mana-zh-CN.txt`，真实预览／填入／继续／导出，Ctrl+V读取的完整JSON与英文基线一致：FourFocus9、l86、r=rare、两个上述m条目，q0、qt=null、ns=null、s=[]、ip=[]。复制前独立标记，复制与粘贴分两次工具调用。
- 转换保留10和48各自范围及高级头，不把原站显示总值58写回导入文本。样本为自造，不宣称真实国服词缀名与掉落组合已真机验收。其他复合组与跨来源属性组合仍未开放。
- 18文件131项相关测试、类型／格式／构建／ZIP检查通过；6871词条，仅storage权限，无新来源和生产依赖。


### 0.1.47 条件标签、空余词缀搜索与克隆恢复（2026-09-25）

- 原站条件DOM使用单数Prefix／Suffix／Corrupted Implicit及Essence来源标签；Meta对应数量及空余位置等特殊条件。补8条人工UI译文，词典6879条；不改li的value或英文search属性。
- 初版实装中，中文“空余前缀位”映射Open Prefix后，原站只显示该选项；选中值special:open-prefix，显示中文正常。但popup停用时，候选恢复MetaOpen Prefix、已选current却残留中文，确认原站复制了翻译后节点，不将该次验收算成功。
- 新增最小克隆回归复现失败，文字层对.dropdown .current新增文本只从同一下拉框li.selected的已知翻译状态查找唯一原文。不做全词典中文反译，不改变机器身份；歧义／没有已知原文时不猜测。
- 修复后的最终ZIP独立解包实装Chrome151：通过中文查询、ArrowDown／Enter选候选、真实点击原站特殊条件，current为“特殊条件空余前缀位”，selected值special:open-prefix。真实popup停用后current恢复MetaOpen Prefix、值仍special:open-prefix、search仍metaopen prefix。原站URL中的reqs也保留该机器身份；扩展不主动改URL。
- 一次CLI按角色定位误打开背包，立即关闭并改用新快照引用进入导入；没有保存、删除或修改背包。本轮未运行概率计算，不声称特殊条件业务计算新增验收。
- 19文件133项相关测试、类型／格式／构建／ZIP检查通过；新增集成测试使用实际UI词典，覆盖标签、英文属性、图标和停用恢复，以及中文特殊条件候选。权限仍仅storage，无新来源或生产依赖。


### 0.1.48 新版介绍页术语与模式复验（2026-09-25）

- 对用户指定`/whats-new`重新核对残留英文，沿已有国服词条2081918629的“增幅器”补充影响其数值的词缀说明。Distilled Emotions、Chronomancy等池简称、synth／elevated说明和PoE1专属段落仍保留英文，不能声称页面完整汉化。
- 最终ZIP独立解包实装Chrome151：PoE2／English的新说明为中文，page-labels样式1份；真实点击PoE1后，标题Calculator、新说明英文原句，data-poe2-l10n节点0；再点击PoE2，译文恢复、样式仍1份。此次模式复验不代表所有页面或原站语言矩阵均重跑。
- 真实popup启用中英对照，新增行显示中文与完整英文，clientWidth=scrollWidth=361；桌面三列截图已查看，可正常换行。本轮未专门覆盖窄屏及所有介绍卡片尺寸。
- 19文件133项相关测试、类型／格式／构建／ZIP检查通过。6880词条，权限仍仅storage；单条文案复用既有显示及模式回归，无新增复述文案的测试。

### 0.1.49 自制图标与最终包（2026-09-25）

- 最终ZIP解压后加载Chrome：管理页显示PoE2中文助手0.1.49及原创深色金色「中」字图标；管理卡片图片自然尺寸48×48，截图已实际查看。
- 在扩展popup页面逐一调用Image.decode验证16、32、48、128四个PNG，全部成功，宽高与声明一致。未新增权限或公开资源。
- 核心／扩展19文件135项测试通过；类型检查、变更JS/JSON格式检查、双入口构建、ZIP检查通过。图标检查限定本地路径、文件存在、PNG头和尺寸，不声称静态检查是完整图片解码；解码证据来自上述Chrome实测。
- 图标规格依据[Chrome manifest icons文档](https://developer.chrome.com/docs/extensions/reference/manifest/icons)，采用PNG，包含16/32/48/128像素。图形自行绘制，无CoE素材或第三方字体。
- 本轮未重新验收全部CoE页面，也未执行全仓verify；现有预览范围和待验收项不变。

### 0.1.50 环锁腰带原站兼容性核对（2026-09-25）

- 来源为用户提供的环锁腰带结构；入库fixture改为测试名称并移除交易备注。词缀文本沿用已有国服词典；共享解析器已有`implicit.stat_1416292992`的“咒符位”别名，无需追加机器繁简转换。
- 无扩展的新版PoE2 English页面：导入完整英文测试腰带，唯一原站诊断为`Could not find type for modifier 'Has 2(1-3) Charm Slots'`；改成词典单数`Charm Slot`仍报同类错误。没有点击带丢失风险的继续按钮。
- 单独导入普通Mail Belt、物等79、药剂充能降低14(15-10)%成功。原生Export经真实Ctrl+C标记／点击导出／Ctrl+V得到自造物品JSON：`i=Metadata/Items/Belts/FourBelt7`、`r=normal`、`l=79`、`ip=[["14"]]`、`m=[]`、`p={}`、`s=[]`。
- 同一普通样本新增独立`Charm Slots: 2`属性行后无报错，但原生完整JSON与上一个样本逐字一致，仍仅含上述药剂固有值；页面未显示咒符位。说明“导入无报错”不足以证明数据保留，不能把移动／删除咒符行作为兼容方案。
- 原站可见词缀池将样本两条召唤生物属性列在Genesis Tree前／后缀中。这只确认页面分类，不证明高级导入后的来源身份、游戏生成规则或实际交易装备来源；两条词缀的完整原生导出仍未验收。
- 0.1.50增加按基底／类别／唯一词缀ID定位的兼容性说明，保留原文与转换全文。未支持基底不再套用空档案反复报词缀未识别或数量错误；已支持基底仍执行原有逐组检查。仍保留未验收资格限制。未扩大腰带的可提交范围。
- 核心／扩展20文件137项测试、类型检查、格式检查、构建和ZIP检查通过。未执行全仓verify，权杖与更多类别继续待做。

- 最终ZIP实站验收：该样本只显示咒符位兼容性和支持范围两项提示，英文预览保留两条固有属性、六条显式词缀；填入按钮禁用，原生输入仍是原始中文。第13行定位按钮选中咒符位原文。

### 权杖原生导入审计（2026-09-25；运行时仍为0.1.50）

无扩展原站完整识别用户权杖对应的四组显式词缀，复合后缀保存为同一身份的两个数值；但技能当前等级12或1均显示成13，两份完整原生导出逐字一致。不能将其认定为当前技能等级已保留。详见[权杖导入核对](sceptre-import-audit.md)及其原生JSON／中文fixture。新增回归后21文件139项测试通过，本轮没有新增可提交类别，也未重发ZIP。

### 0.1.51 条件类别翻译与步骤名保护（2026-09-25）

- 新增22条实际DOM界面译文：路线优先顺序，装备属性／状态／汇总属性类别，护甲、能量护盾等属性，以及亵渎词缀与伤害／抗性汇总条件。沿用已登记人工UI词典；未修改原站数字、候选value或search属性，未知术语仍保留英文。
- 新增保护范围为步骤编辑器中路线列表的非end目标标题，避免用户步骤名碰巧与词典相同而被翻译。测试覆盖译名、中文候选、英文筛选键、用户标题及停用恢复。
- 最终ZIP实站导入自造流程，将第二步标题设为Property：第一步编辑器显示“路线优先顺序／结束／Property”，用户名称未变。
- 同一最终包在条件输入“能量护盾”，扩展候选含“能量护盾→Energy Shield”；选择该候选后原站筛选结果含“装备属性能量护盾”，`value=special:property:ENERGYSHIELD`、`search=propertyenergy shield`保持英文。最后点击该原生选项被站点footer广告遮挡，未成功选中，也未保存该属性条件；不将候选显示证据当成原生选择成功。
- 140项核心／扩展测试、类型、格式、双入口构建及ZIP检查通过；6902条词条，仅storage权限。本轮未执行全仓verify。

### 两条件组补验记录（2026-09-25；编辑／运行使用0.1.50）

- 通过原生界面将第一步结束条件改为or组，含普通闪电78、冰霜77；新增not组含普通混沌80。中文输入经扩展候选转成英文查询，再选择原站普通后缀。第一次混沌选择后导出为空组，重新等待筛选稳定、按value选中并检查当前标签，才成功保存80；未据首次结果宣称成功。
- 完整原生导出1647字符，已保存`fixtures/two-group-resistance-route.json`，只格式化JSON。两组matches均为null。配置成功证明运算符／条件身份保存，不证明“or”按钮单独决定组内任一匹配或所有多组逻辑语义。
- 该配置运行完成10件，原站总计84轮、166步；第一步动作83次，全部去第二步，直接结束0件；第二步结束10件。第一步为什么没有直接结束尚未完成原因隔离。不能将此记录写成预期的复合逻辑已通过。
- 后续尝试在第一组匹配数量框fill为1，保存后的原生导出仍为null；本轮没有证明该数字控件编辑成功。下一轮应先核对原生change／keyup交互和英文无扩展基线，再解释多组匹配语义。不得直接改流程对象绕过UI。
- 尚未完成任意深度嵌套、显式回边及完整启停导出等价验收，阶段清单继续保持未完成。

### 0.1.52 匹配数量键盘输入与组运算符译文（2026-09-25）

- 无扩展英文原站导入此前的两组样本，实际点击第一组matchCount，Ctrl+A、键盘1、Tab，再Apply changes。原生完整导出中matches变为字符串`"1"`，证明上轮fill值未保存不是扩展翻译造成的丢数；尚未读取原站内部监听实现，不推断单个事件就是全部根因。
- 英文基线运行完成10件：总计35轮60步；Step1执行34次、直接结束8件、转Step2共26次；Step2结束2件。与原先matches=null时第一步未结束形成配置差异证据，不把随机计数当概率结论。
- 修正And／Or／Not为“与（AND）／或（OR）／非（NOT）”，Matches为“组内匹配数量”；新版介绍页对应说明同步使用运算符名称，避免“满足任一”让人误以为已经设定组内数量1。未改动组type、matches、元素ID或业务事件。
- 最终ZIP实装0.1.52，重新导入原始matches=null样本，重复同一键盘操作。实际界面显示新译文；保存后的完整原生导出1646字符，与无扩展英文基线逐字一致，matches同为`"1"`。
- 最终中文版实际运行完成10件：总计34轮58步；Step1执行33次、直接结束8件、转Step2共25次；Step2结束2件。随机运行无需与英文基线轮数相等。本轮没有逐件核对全部结果，也没有证明任意组合的完整真值表。
- 成功保存的原生配置见`fixtures/two-group-match-one-route.json`，只格式化JSON；保留原来的null样本供对照。141项核心／扩展测试、类型、格式、构建及ZIP检查通过，6902条词条，仅storage权限。未执行全仓verify。
- 此轮补齐真实键盘保存和英文／中文原生导出等价证据；更深层嵌套、显式回边、全部模式及真实IME仍未完成，不能据此关闭整个模拟器验收。

### 0.1.53 显式回边、起点提示与属性候选（2026-09-25）

- 在0.1.52最终包上，从既有两步流程通过原生UI将两步动作改为none，点击第二步连线按钮，再点击第一步目标按钮建立回边。原生导出确认Step1→Step2、Step2→Step1，action均null、startingStep=null、noStopOnLoop=false，见`fixtures/explicit-backedge-no-start.json`。原站同时重新编号部分priority，保留导出原值，不假设编辑无副作用。
- 首次运行因循环内没有自动可识别起点而报`Could not find starting step (please select one)`，实际执行0步。关闭提示、清除结果后通过原生起点选择器选Step1，再运行确认回边确实执行；约7.1秒观察到15,802,825步、0件，原站没有自动终止该无动作循环。
- 随即点击原生“停止模拟”；即时快照仍显示运行，后续检查已切为“清除模拟结果／返回流程”，最终计数63,296,614步、29.6秒。手动停止已确认。时间包含工具调用和观察延迟，不是精确停止延迟测量。浏览器随后关闭；未禁用循环防护，不能据配置中的false就承诺所有回边都能自动终止。本轮未用无扩展会话隔离自动停止行为的原因。
- 0.1.53补齐5条实际提示：发生错误、无法自动确定起始步骤、选择起始步骤、自动识别、暂无物品数据。最终ZIP重新导入无起点回边样本，实际错误弹窗显示中文，选择器标题和自动识别按钮亦为中文；不再运行无出口循环。
- 最终包中用中文能量护盾→Energy Shield进入原站筛选。首次点击仍落在滚动列表可视区域以外；检查DOM后正常向下滚动dropdown的ul，成功选中`special:property:ENERGYSHIELD`，显示“装备属性能量护盾”。不修改广告、footer或站点z-index。
- 原生导出确认startingStep为Step1 UUID、第二步回到第一步，第一步结束条件同时含闪电78与能量护盾属性ID。实际popup停用翻译后导出与启用时逐字一致，均1577字符；完整样本见`fixtures/explicit-backedge-property.json`。
- 数值边界未通过：选中属性但未键盘编辑时首次导出没有该元素；实际键盘输入1后元素进入导出，但tier仍为null。停用扩展后重新打开原生条件，用键盘输入40、Tab、Apply changes，输入框确实显示40，导出仍为null。只确认身份保存与启停等价，不宣称属性阈值持久化成功；仍需更完整的无扩展基线／事件核对。
- 142项核心／扩展测试、类型、格式、构建和ZIP检查通过，6907词条，仅storage权限。更深层分组、属性数值及全部回边矩阵仍未验收；没有执行全仓verify。


### 0.1.54 Data 材料搜索与数值控件隔离（2026-09-25）

- 无扩展新会话导入`explicit-backedge-property.json`，打开第一步结束条件，不运行该无出口回边。能量护盾数字框用真实键盘Ctrl+A、40、Tab后显示40，Apply changes后原生导出仍为tier:null，重新打开为空。普通闪电词缀同样用2、Tab保存失败；再用ArrowUp两次、Enter、Tab仍未保存。由此确认这条导入编辑流程在完全不加载扩展时也存在问题，不能归因于中文翻译；未查明原站根因，不泛化到所有数字控件（组内匹配数量已有独立成功记录）。
- 英文原站`/data?mode=items`导航显示POE 2 selected。`#dataItemSearchInput input`可通过真实键盘查询Exalted Orb，不必先选标签；fill后若尚未产生keyup会保留原有空态。原生查询返回9件，包括普通／高级／完美崇高石及部分PoE1通货。只确认原站可检索，不代表这些条目均可在PoE2制作。
- 扩展此前将此框限制为base领域，虽已具备材料译名却没有候选。本轮仅对该Data框合并既有item名称领域，精确候选优先、总计最多50项；制作页仍限制基底、词缀框仍限制词缀。不新增词典来源或制作规则。
- 最终0.1.54 ZIP解压后加载独立Chrome会话。在Data输入“崇高石”出现普通／高级／完美三项候选；实际点击“崇高石 → Exalted Orb”后原生input.value为Exalted Orb，返回9件。九行图片URL和英文效果说明的有序数组与无扩展英文查询逐字一致，前三件名称已显示简体中文；其余未知名称和长效果说明保留英文。
- 新增材料搜索与制作基底领域隔离回归先失败后通过。核心／扩展21文件143项测试通过，类型、格式、构建和ZIP检查通过；6907词条，仅storage权限。未执行全仓verify，也未完成真实IME或全部材料种类验收。


### 0.1.55 新版介绍页剩余操作说明（2026-09-25）

- 重新检查用户指定的`https://beta.craftofexile.com/whats-new?game=poe2`可见DOM，补充6条人工译文：Elevated词缀的等阶0显示、合成／腐化的基底词缀覆盖、化石优化器、剩余时间与组合处理速度、并行工作线程数、Heist首饰词缀数量上限。这里只翻译原站的功能介绍，未验证或复现其制作规则。
- Elevated保留英文；Synthesis与Heist保留英文对照以便查找。PoE1专属内容仍保留在原站所属章节，未改成PoE2功能说明。Distilled Emotions及特殊符文池简称等未确认国服译名仍未补写，不声称介绍页完整汉化。
- 最终ZIP独立解包加载，Chrome管理页确认0.1.55；6条文字实际显示简体，化石优化器下的嵌套两项说明保持结构，当前桌面视口没有整页横向溢出。从真实popup取消“启用简体中文”后，六条全部恢复英文，扩展owned标记为0。
- 本轮只增补词典文案，未改变运行逻辑、权限或依赖。21文件143项既有核心／扩展测试、类型、词典格式、构建与ZIP检查通过；词条6913，仅storage权限。未新增逐条复述译文的测试，显示与撤销通过最终包实际验收；未执行全仓verify。


### 0.1.56 跨领域候选排序（2026-09-25）

- 检查实际6913条词典：基底1543条、item名称771条，两领域没有同英文名称重复；“符文”匹配649条基底、134条item名称。0.1.54的合并逻辑先放基底再放材料，只有精确匹配优先，统一截断50项会将该查询的材料全部挤掉。
- 核心search新增向后兼容的领域数组参数：先限定领域与accept范围，再沿原有完整名称优先、匹配名称长度、ID排序，最后统一截断50项。Data物品框使用base与item；其他控件的单领域调用不变。没有按领域配额硬塞候选，也没有放宽到所有词条。
- 扩展回归先复现55个长基底挤掉短材料名称，再验证短材料进入首位、总数50及键盘焦点与选择。核心补充跨领域完整名称优先、外部领域排除、重复／乱序领域不影响结果、accept先筛选和空领域数组。
- 最终0.1.56 ZIP独立加载。在Data输入“符文”显示50项，前6项为短基底，第7项行家符文、第8项身躯符文，其他材料也可见。真实按8次ArrowDown聚焦“身躯符文 → Body Rune”，Enter提交后原生input.value为Body Rune，原站返回身躯／次级身躯／高级身躯／完美身躯符文4项；中文查询提示保留。此处没有将真实键盘自动化冒充中文输入法验收。
- 21文件145项测试、核心与扩展类型检查、格式、构建和ZIP检查通过；6913词条，仅storage权限。未执行全仓verify。


### 0.1.57 丝绸便鞋高级文本导入（2026-09-25）

- 新建完全不加载扩展的英文PoE2会话，用自造稀有Silk Slippers高级文本导入：物等86、品质20%、一个空S孔、护盾40(36–41)、闪电抗性18(16–20)。首次将护盾写为T6，原站显示T3；修正为T3重新导入。两次原生JSON相同，说明原站会依据识别结果显示等阶，不能将输入等阶照抄视为验收。
- 原生导出为基底`Metadata/Items/Armours/Boots/FourBootsInt4`，`LocalIncreasedEnergyShield5=[40]`、`LightningResist3=[18]`，`ns=1`、`q=20`、`l=86`、`r=rare`。显示能量护盾91；未将此样本的属性计算规律泛化为本扩展制作引擎。
- 扩展新增该基底独立档案，只开放已核对的护盾上限前缀／闪电抗性后缀、现有0–20普通品质验证及一个空孔；不继承其他法器词缀，不扩展移动速度或其他靴子基底。先以被拒绝的样本复现失败，再增加允许档案；测试包含错类别、双孔、品质21、缺范围和前后缀混淆的拒绝。
- 最终0.1.57 ZIP独立会话从首页“导入装备”粘贴国服样本，经双栏预览、填入英文、原站继续，原生Export输出与无扩展正确等阶英文基线逐字一致。两边均先用独立剪贴板标记，再点击原生Export并通过真实Ctrl+V读取，不使用旧剪贴板冒充导出。英文预览保留中文随机名与词缀标题，不影响原站数值身份识别。
- 样本入库为`fixtures/silk-slippers-zh-CN.txt`与`silk-slippers-native.json`，后者仅格式化原生导出。本轮实站验证的是稀有双词缀样本；普通／魔法的组合由既有统一稀有度规则处理，尚未为此新基底单独跑完稀有度矩阵，不宣称所有靴子覆盖。
- 核心／扩展21文件146项回归、类型、格式、构建与ZIP检查通过；6913词条，仅storage权限，无新依赖或来源。未执行全仓verify。


### 0.1.58 无括号范围的固定移动速度（2026-09-25）

- 无扩展英文PoE2会话逐一导入自造稀有Silk Slippers、物等86的单前缀文本，使用不带括号范围的10/15/20/25/30/35% increased Movement Speed；原站分别显示T6/T5/T4/T3/T2/T1。最初20%搭配T3时原站仍显示T4，故新档案校验固定值与等阶组合，而不照抄未经核对的等阶。
- 复用已有国服术语身份`explicit.stat_2250533757`，只在丝绸便鞋普通前缀中允许上述六档固定值无范围。来源仍为已登记词典与公开UI实测，没有读取原站执行代码或数据文件。等阶错配、21或负数、非固定范围、后缀分组、其他防具均不能通过；护盾等原有词缀仍要求范围。保留输入数值，不补造`20(20-20)`。
- 6条缺陷回归先失败再通过，另覆盖上述拒绝边界。最终ZIP实装国服三词缀样本：护盾40(36–41)、20%移动速度、闪电抗性18(16–20)，加品质20与单空孔。双栏预览保留无范围移速，实际填入原站并继续成功。
- 原生Export的完整JSON与无扩展英文三词缀样本逐字一致：`LocalIncreasedEnergyShield5=[40]`、`MovementVelocity3=[20]`、`LightningResist3=[18]`，基底、物等、品质与孔位也一致。每次先复制独立标记，再原生Export、真实Ctrl+V；样本见`silk-slippers-speed-zh-CN.txt`与`silk-slippers-speed-native.json`。六档均有英文原生UI验证，完整中文导出对照本轮只针对20%组合；未宣称所有等级／装备／特殊移速都可导入。
- 核心／扩展21文件153项回归通过；随后测试模板格式整理后导入30项再次通过。类型、格式、构建与ZIP检查通过，6913词条、仅storage权限；未执行全仓verify。


### 0.1.58 后续全仓门禁核对（2026-09-25）

- 默认`pnpm verify`在项目检查前触发自动安装，因全局缓存SQLite无法打开而退出。临时`pnpm_config_verify_deps_before_run=warn`保留“工作区结构自上次安装后变化”的警告，不自动安装也不修改配置；此环境不是全新锁文件安装验证。
- 全部六包typecheck通过；全仓Biome首次检出8个扩展原生JSON样本的数组排版问题。只对这8个文件运行格式化，再逐文件解析并序列化，与格式化前及Git HEAD比较均完全一致，包含对象键序、数组顺序和全部值。
- 修复后全仓Biome检查1090文件通过。本记录仅确认类型与格式两道门禁；后续完整测试与构建结果须另列，不能据此称`verify`全部通过。扩展运行版本仍为0.1.58，无运行逻辑或包内容变化。
- 新增`validation.md`说明全仓／局部验证的范围、样本JSON必须纳入格式检查、原生逐字导出证据与入库排版的区别，以及临时包管理器诊断命令的限制。


### 0.1.58 全仓失败隔离与安装核对（2026-09-25）

- 上述完整测试已终止：578文件中577通过、1失败；4839项中4835通过、3失败、1跳过，耗时481.90秒。唯一失败文件`packages/dict-builder/src/syncCraftOptional.test.ts`的三项均报`spawnSync node EPERM`；在沙箱外原样独立运行该文件，三项全部通过（788ms）。旧制作UI此前的15秒超时本轮未复现，不继续将它列作已复现的当前失败。
- 唯一跳过项是`packages/build-core/src/format/roundtrip.test.ts`在未提供本地真实.build样本时的既有skipIf；未新增跳过、放宽超时或改测试断言。此轮分段结果不能合并宣称一次完整verify成功。
- 独立`pnpm build`、`pnpm dict:check`、`pnpm craft:check`均正常退出。构建仍有旧制作站大于500kB的分包提示；词典审计计数zh-CN=13、zh-TW=17及目录缺失声明原样保留，未放宽门槛。仅验证旧站回归，不恢复其功能开发或部署。
- 在独立工作树运行`pnpm install --frozen-lockfile`成功，输出锁文件已是最新、无需重新解析、Already up to date，未更改Git跟踪文件。没有全局安装或升级pnpm。随后在允许Node子进程的环境启动无临时选项的标准`pnpm verify`，其最终结果另行记录；本段不提前宣称通过，也不把已有node_modules的安装核对称为全新环境安装。


### 0.1.58 标准门禁通过与真实IME（2026-09-25）

- 锁文件安装核对后，在允许Node子进程的环境原样执行`pnpm verify`，退出码0。全部六包类型检查、1090文件格式检查、578测试文件、4838测试通过；1项本地真实.build样本缺失按既有skipIf跳过。测试耗时487.58秒，随后全仓构建、dict:check、craft:check依次成功。未使用临时依赖检查选项、未调大超时、未跳过失败用例。旧站大分包提示及数据审计计数保留，不等于零警告或CI远端通过。
- 用0.1.58最终ZIP在隔离有界面Chrome、Fcitx5 5.1.7拼音中完成Data真实IME确认与取消路径。系统输入fuwen时预编辑、无候选；空格确认符文后才出现候选；系统方向键选身躯符文、回车后原站Body Rune查询返回4件。再次输入fu后Escape清空预编辑、无候选。环境、事件标记限制和清理见ime-audit.md；不将这一结果外推Windows或全部控件。
- 本轮未修改扩展运行逻辑和版本；现有ZIP仍为0.1.58。文档记录标准门禁与真实IME的新证据，保留CI、其他平台及完整交互矩阵的待验收项。
- 标准`pnpm extension:package`亦正常退出，完成构建与产物检查。重新生成的content.js和dictionary.json与本轮真实IME使用的最终ZIP解包文件逐字相同；没有用旧逻辑包冒充本轮源码结果。


### 0.1.58 制作页真实IME追加核对（2026-09-25）

- 新隔离Fcitx5/X11会话完成“符文法器”基底组词、输入法选词、英文查询及进入制作页；完成“闪电抗性”词缀组词、确认、英文查询，原站显示普通78与精华78_1001两行。两框预编辑阶段均无扩展候选，不通过fill或合成composition伪造中文。
- 计算器条件框真实组词后可用虚拟选择保持输入焦点，再提交英文。普通78首次点击没有选中，重试成功；新增条件的精华78_1001首次点击成功。保留未解释的首击异常，未将其归因或称完整条件交互无问题。过程与视口证据见ime-audit.md。
- 本轮仅更新实测记录，无源码与版本变化；没有重复运行上一轮已通过的全仓测试，也没有扩大数据来源或恢复旧做装网站。文档差异与全仓格式检查通过。


### 0.1.58 条件点击自动化失效隔离（2026-09-25）

- 无IME输入同样复现首击未保存：视口高837，自动化点击y=843.79，pointerdown目标HTML，随后失焦关闭列表。滚动使选项完整可见后，y=461.67命中LI并保存普通条件78。确认本次失败来自自动化点击位置，不修改扩展运行逻辑。
- 同一会话在设置停用扩展后，原生英文检索与完整可见的精华选项点击成功，最终条件身份78、78_1001均保留。不是全新无扩展环境；历史首击缺少事件记录，不能逐一追认根因。详细证据及剩余矩阵见ime-audit.md。
- 验收规范增加视口位置、事件目标和实际chosen身份检查。本轮仅更新文档，版本仍为0.1.58，不重复执行此前已通过的全仓测试。


### 0.1.58 多标签设置同步与窄窗语言恢复（2026-09-25）

使用最终ZIP解包目录、独立Chrome会话，同时打开英文PoE2首页与`/whats-new?game=poe2`，第三个标签打开扩展popup。未改写站点存储或调用站点内部函数；通过原生控件操作。

| 步骤 | 实际结果 |
| --- | --- |
| 首页输入符文法器，保留未提交查询 | 输入值符文法器，扩展面板1个、候选2个 |
| popup关闭扩展，依次检查两页 | 两页`data-poe2-l10n`辅助节点均为0；标题恢复Key Features／Usage及Calculator／Emulator等英文；输入仍为符文法器 |
| popup重新启用并打开中英对照 | 两页标题变为主要功能 · Key Features、计算器 · Calculator等；首页重新聚焦后面板1个、候选2个，没有叠加重复面板 |
| 首页Enter提交 | 原生输入为Runed Focus，候选按钮0个、查询提示1个；原站展示两组制作／详细信息／百科操作 |
| 重载新版说明页 | 稳定后保持中英对照，原站模式PoE2、语言us；只证明页面重载，不等于浏览器重启或扩展升级 |
| 新版说明页通过菜单切到Deutsch | 原站语言de，扩展语言提示1个；关闭提示后为0且语言仍de，不自动改回英文 |
| 390×844视口重载德语页 | 提示重新出现且仅1个，辅助节点总数1；边界left=16、right=374、top=716、bottom≈812.67，截图确认提示文字与关闭按钮完整可见 |
| 原生菜单切回English | 语言us、提示0个，九个说明标题恢复中英对照；另一标签仍保留Runed Focus输入及双语标题 |

原站窄窗口页面本身出现水平滚动，未据此判定扩展造成溢出。菜单导航与重载会暂时重建文档，证据取稳定后的状态；一次管理页过早读取和一次重复声明的取证脚本失败均未计为产品失败。没有验证进程重启、升级、所有语言或所有页面；没有将可见节点唯一性推断为内部监听器数量证明。会话已关闭。

本轮仅更新验收记录，无运行代码、词典或版本变化；格式检查与差异检查通过，不重复此前已通过的全仓测试。


### 0.1.59 设置页与头部设置弹窗（2026-09-25）

- 原站`/settings?game=poe2`实测显示布局、显式词缀显示、筛选效果、权重计算、汇总属性和限制开关等仍为英文。本轮新增设置上下文，仅匹配该页main及公开`#settingsZone`容器；短词Active在此译为“显示”，不改变其他页面的译法。现有输入／可编辑文本保护继续生效。
- 原站设置丢失警告实际由文本、红色LOST标签、后续条件三段构成；首次整句映射未命中，已用失败回归复现并按分段修复。最终包显示中文警告，红色span原样保留；没有以重写innerHTML合并节点。
- 最终ZIP独立解包加载，设置页四个分类标题为全局设置、价格设置、制作设置、自定义价格；SHIFT说明、旧版内容可能加载较慢、最大词缀数量限制等条件均保留。可见站点版本为Forbidden Rites - 0.5.5.3，未改译该版本名。Chaos／Exalted／Divine缩写继续英文，不声称设置页零英文。
- 紧凑模式经原站选项从0切为1，再恢复0，标签中文、value身份未变；该操作在本轮首次0.1.59候选包完成，随后仅修复分段警告。最终包另外核对默认值0、双语显示和关闭恢复；四个分类、Active及带换行的完整英文警告均恢复，辅助节点为0。
- 最终包返回首页，通过悬停打开头部设置弹窗，制作设置及所有本轮所列短标签显示中英对照。没有执行设置导入／导出或改写价格；价格输入保护由合成DOM回归验证，未据此声称全部原站设置操作均已验收。
- 先失败后通过的三项回归涵盖设置边界、选项身份／输入保护、动态弹窗、离开页面、双语与原始警告恢复。核心及扩展22文件156项通过，扩展类型检查、全仓Biome1092文件及构建／包检查通过；仅storage权限、词典6913条。版本更新触发pnpm缓存访问受限后，在允许缓存访问的环境按锁文件安装核对成功，再执行标准打包成功；锁文件及生产依赖未变。本轮未重跑全仓verify，0.1.58的全仓结果仍是历史证据。


### 0.1.60 自定义价格搜索领域修复（2026-09-25）

- 0.1.59设置页实测：`#customPriceSearchHolder`内复用`#searchInput`，被既有词缀选择器误认；输入“崇高石”出现试炼献祭词缀，不能得到材料候选。原站英文Exalted Orb查询正常，稳定后可见三条材料行。以最小失败回归复现，新增更具体的容器匹配，优先归入item领域；其余制作页`#searchInput`仍为stat。
- 0.1.60最终ZIP独立解包，中文候选恰为崇高石、高级崇高石、完美崇高石；Enter选择精确项后原生值Exalted Orb，原站结果身份依次为`CurrencyAddModToRare`、`CurrencyAddModToRare2`、`CurrencyAddModToRare3`（均位于Metadata/Items/Currency/）。手输英文和同会话停用扩展后的英文查询，三条身份及顺序一致；停用后恢复英文显示、扩展辅助节点0。
- 原站筛选有延迟，采集稳定后的结果。清空按钮首次点击清空查询，537行全部恢复、候选面板0。最初字段基线包含了搜索框本身，因此整体比较为不同；逐字段隔离确认唯一变化是无item／league的文本搜索值，2148个带item身份的价格输入及赛区／物品属性均未变。没有设置价格或调用保存接口。
- 回归同时验证价格材料领域、英文提交、价格数值保护及搜索框移出价格容器后恢复词缀用途。核心与扩展22文件157项、扩展类型检查、全仓格式及差异检查通过；直接运行现有构建与打包脚本，0.1.60包检查通过，仍为6913词条和storage单一权限。未新增依赖或重跑全仓verify，完整平台／IME矩阵仍待验收。


### 0.1.60 模拟器组层级与增删往返（2026-09-25）

- 最终ZIP独立会话导入本仓自造`two-group-match-one-route.json`。原生导入框为`#simulatorImporterInput`；第一次宽泛textarea选择器命中隐藏框，未导入，不算产品失败。明确定位可见框后成功。
- 第一阶段尝试从步骤编辑器路线优先顺序的标题进入条件编辑，未进入；真实入口为流程图步骤卡的`.links .end`。结束条件编辑器中两个requirementGroup直接隶属于同一个groupsZone；点击“添加条件组”新增第三个并列AND组，三组内部的requirementGroup数量均为0。当前可见控件只有外层添加组、组内条件条目和移除按钮，未找到新增子组入口。此证据不证明上游绝对不支持其他形式嵌套，也不能将并列多组称为任意深度嵌套。
- 首次删除空组工具返回Done，但原生导出1690字符仍含第三个空AND组，未计删除成功。该配置完成10件（37轮、64步）；没有以这次运行证明空组语义或原配置往返。
- 模拟后“返回流程”进入结果流程视图，链接编辑入口布局框为零；工具报页头遮挡。原生“清除模拟结果”后才恢复编辑入口。重新展开结束条件，将删除按钮滚入视口，边界y≈714.33至744.33、视口高1277；点击后立即确认组数从3变2，再应用更改。
- 重新复制独立标记、点击原生导出、真实Ctrl+V读取，导出1646字符，与原样本解析后全字段相等、差异顶层键为空；没有忽略时间戳或业务字段。恢复后的流程再次运行完成10件（37轮、65步、0.1秒）。随机计数不作为概率结论。
- 本轮新增的是中文启用状态下组增删／保存／运行的证据；未新跑停用对照，原样本英文／中文逐字等价证据仍见0.1.52记录。没有逐件审计全部终态词缀，也没有验证完整布尔真值表、任意深度嵌套或新回边结构。未改运行代码和版本；文档格式及差异检查通过，未重复源码测试。隔离会话已关闭。


### 0.1.61 模拟器连线提示与Shadow DOM核对（2026-09-25）

- 核对上一轮普通DOM记录中看似空白的词缀宿主：0.1.60已选条件78／77／80的Shadow Root分别包含闪电／冰霜／混沌抗性`+#%`，每项宿主数量1，原站文本仍为英文模板。`outerHTML`不含Shadow DOM，因此不能据空span认定缺译；未修改既有词缀层逻辑。
- 实际悬停发现结束、重新开始、新增连线三项提示仍英文；提示层位于main内。新增三个已核实人工UI译文，不改tooltip属性、事件、条件ID或流程数据。另两次针对当前不可见状态图标的悬停未命中，不将旧提示误记为对应图标证据，也未据此增加未验收文案。
- 最终0.1.61 ZIP独立解包，导入自造两组流程，依次悬停显示“结束条件”“重新开始条件”“新增连线”；原站属性仍为End condition／Restart condition／New link。通过popup停用后重新悬停，三条均恢复英文，辅助节点0。没有运行或更改流程。
- 新增先失败后通过的动态提示回归，覆盖节点新增、提示更新、保留原站属性及停用恢复最新英文。核心与扩展22文件158项、扩展类型、全仓格式和构建／打包检查通过；词典6916条、仅storage权限。本轮使用现有工具直接构建，未重跑全仓verify。隔离会话已关闭。


### 0.1.61 浏览器进程重启与设置持久化（2026-09-25）

- 用新的临时持久Chrome配置加载最终ZIP；在真实popup先开启双语，再停用扩展。关闭浏览器后，用不同会话名启动新进程，复用同一临时配置及扩展目录，扩展ID一致。没有使用cookies／localStorage恢复快照替代浏览器配置，也未读写个人浏览器资料。
- 新进程的popup中，enabled仍为false，bilingual为true且控件禁用；真实`/whats-new?game=poe2`九个标题均英文，扩展辅助节点0。重新启用后立即恢复已保存的双语偏好，九个标题均显示中英对照。
- 两个进程均正常关闭。此证据涵盖Linux Chrome同一目录、同一版本的正常关闭与重启；不等同于崩溃恢复、Windows浏览器或扩展升级／更换目录。
- 同轮补齐更新日志0.1.49—0.1.58：十个版本及日期逐一对照对应提交中的manifest和Git日期；0.1.0—0.1.61共62个条目连续、无重复、降序正确，最新版本与package一致。未改运行版本或发布新包。


### 0.1.61 当前源码全仓门禁（2026-09-25）

- `pnpm install --frozen-lockfile`完成现有依赖核对，锁文件已最新、未变更依赖。随后原样运行标准`pnpm verify`，退出码0；六包类型检查、1092文件格式检查、579测试文件通过，4843项通过、1项跳过，测试耗时493.18秒。跳过仍是缺少本地真实.build样本的既有skipIf，未新增跳过或放宽超时。
- 全仓构建、dict:check、craft:check依次完成。旧制作站大于500kB构建提示、词典审计zh-CN=13／zh-TW=17及既有目录未解析声明保留，不报告零警告或旧站已恢复开发。
- 随后标准`pnpm extension:check`通过：0.1.61、6916条词条、仅storage。全仓构建所得content.js与dictionary.json和本轮重启验收使用的最终ZIP解包文件逐字一致。现有交付ZIP副本也逐字一致。
- 仓库级frontend-design与webapp-testing共8个文件的SHA-256均与vendor-lock一致；两个技能只保留在仓库，扩展ZIP不含技能源码。本轮没有全局安装、没有发布商店或更改CI凭证；远端CI仍未验收。


### 0.1.60→0.1.61 固定目录升级（2026-09-25）

- 全新临时持久Chrome配置加载0.1.60，管理页版本确认后启用双语，真实新版介绍页九个标题为中英对照。将0.1.61最终ZIP覆盖解压到相同临时目录，通过Chrome原生Reload操作升级，没有改扩展身份或直接注入设置。
- 首次尝试未在管理页开启开发者模式：命令行加载的扩展在Reload后变为DISABLED，disableReasons中unsupportedDeveloperExtension=true，原popup标签关闭。该次不是升级成功，也不是已证实的扩展代码缺陷。按安装说明开启隔离配置的Developer mode后，再次Reload成功；管理页同一ID、版本0.1.61、状态ENABLED、runtimeErrors为空。
- 重新打开popup，两项设置均保持true；刷新原CoE标签后九个标题仍为双语。随后真实关闭扩展，九个标题均恢复英文、辅助节点0，说明新版刷新后的设置监听有效。
- 已在安装说明明确开发者模式、版本号核对、重新打开popup及刷新CoE标签。此证据仅覆盖Linux Chrome同目录、此次两个版本之间的手工升级，不代表更换目录、商店自动升级或Windows路径已验收。会话已关闭，未改个人Chrome配置。
- 本轮仅文档更新，版本仍为0.1.61；全仓格式及差异检查通过，不重复上一轮已完成的全仓verify。


## 0.1.62：翠玉项链的三种稀有度

先在无扩展英文新版PoE2导入自造Jade Amulet，原站显示敏捷固有属性12(10-15)、敏捷后缀24(21-24)T4、闪电抗性18(16-20)T6。初始试探文本把敏捷写作T5，原站显示T4；修正为T4后重新导入并取得正式基线，未把初始输入当作等阶证据。

原生导出基底为`Metadata/Items/Amulets/FourAmulet4`，固有属性`ip:[["12"]]`；稀有样本显式属性为`Dexterity5:[24]`及`LightningResist3:[18]`，等级86、品质0、无孔。普通样本只含固有属性；魔法样本另含敏捷后缀。身份来自原站自身Export，未读取执行代码或规则库。

最终0.1.62 ZIP独立解包加载后，中文高级文本逐一经过预览、填入、原生继续按钮，三种结构的Export均与对应无扩展英文原始JSON逐字一致。每次先复制独立标记再点击原生导出并粘贴读取，避免沿用剪贴板旧值。稀有样本三条属性的中文显示与数值也已核对。

范围仅为这个基底的已登记属性，不代表所有项链。固有属性要求完整范围，标签只接受属性／Attribute；品质、催化品质、孔位、腐化、未知词缀、重复后缀及错误前后缀归属阻止填入。新增回归先失败后通过，核心／扩展22文件161测试通过；本轮未重跑全仓verify，最近完整记录仍为0.1.61。


## 0.1.63：已打开设置页面的同步

发现弹窗此前只读取一次设置，其他窗口修改后仍显示旧值，后续保存可能覆盖新偏好。新增本机storage事件订阅；保存期间保持锁定，失败恢复最近收到的已保存值。两个新增回归先失败后通过，覆盖外部更新后的保存及保存失败时的外部更新。

最终ZIP独立解包并加载真实Chrome，版本0.1.63。用两个独立标签打开扩展自身popup.html：第二页开启双语，第一页立即显示双语已选；第一页关闭翻译，第二页同步关闭且双语保留、禁用；第二页再启用，第一页同步启用且双语仍选中。这里证明的是同一扩展文档的真实storage事件与控件行为，不声称自动化同时打开了两个工具栏弹窗。

核心／扩展22文件163测试、类型、格式、构建和包检查通过，仍仅storage权限。未重复完整全仓verify；保存故障由单测注入，未声称真实Chrome发生过存储故障。


## 0.1.64：中文搜索提示的描述关联

候选操作说明与未命中提示通过aria-describedby关联到当前输入框，保留原站原有描述；取消、更新、提交或停用清理时按属性归属恢复。原站后续自行改写属性时不覆盖。新增两项回归先失败后通过，相关核心／扩展22文件165测试、类型、格式、构建及包检查通过。

最终ZIP独立解包实装新版PoE2首页：输入“符文法器”出现2个候选，输入框引用的描述节点含方向键、Enter、Escape说明；按ArrowDown再Escape，焦点回到输入框，候选为0，描述属性恢复为不存在，中文输入不变。输入“绝无此词测试”时0候选，描述引用正确指向未命中说明。此处为真实Chrome DOM与键盘验收，不代表已完成NVDA、VoiceOver或其他读屏软件的朗读验收。没有新增权限或依赖，全仓verify本轮未重跑。


## 0.1.65：数据页常用类别漏译

在新版Data页公开DOM核对itemCategorySelector及itemClassSelector，新增预兆、技能宝石、辅助宝石、引路石和石板的9条单复数类别映射，仅在两处列表项生效。国服译名沿用已登记的材料名称及交易词缀：stat_2777224821（引路石）、stat_752930724（技能宝石）、stat_664024640（辅助宝石）、stat_3138466258（石板）；没有采集原站规则库。原始标签代码、未知类别、物品详情长说明仍有英文，不把本轮当作Data全量翻译完成。

最终ZIP0.1.65实装，五类显示分别为预兆50、技能宝石456、辅助宝石625、石板8、引路石16；原始value仍为70、16、17、68、26。预兆选择70后，另开无扩展英文会话使用同一原生筛选，实际50个结果节点的图片URL标识按序完全一致。这里只证明该筛选结果集合一致，不代表所有物品字段已逐一验收。此前尝试比较普通链接时两边都是0个，已弃用该空证据，改用非空结果节点。

新增两项回归先失败后通过，覆盖身份、计数、范围隔离、动态更新、双语与关闭恢复；核心／扩展23文件167测试通过。类型、格式、构建和包检查通过，最终格式调整后新增测试再次通过；全仓verify本轮未重跑。


## 0.1.66：预兆用途整句对照

先读取无扩展新版Data的公开预兆用途，发现关键词span将句子分割，原逐文本节点翻译造成中英混杂。对`.item.currency .modifier`保留完整原文及原生关键词结构，沿既有Shadow DOM词缀对照层显示11条人工整句译文：左右崇高、移除两词缀、左右剥离、同质化／催化崇高、腐烂、光明与左右死灵说明。未知整句保留英文，三条巫妖专名句本轮未开放；不等同于制作规则验证。

最终0.1.66 ZIP独立解包实装，11条中文均正确出现。50个结果实际含49个用途说明节点，这49段innerHTML与无扩展英文基线逐字一致，包括关键词span及其id；初始把结果数当说明数的50断言失败，改按实际结构检查。对Catalyst的悬停未获得可见tooltip证据，本轮只证明结构与原文保留，未宣称实际悬停已通过。

新增回归先失败后通过，覆盖英文原文、关键词节点身份与点击监听、未知动态更新撤除旧译文；缓存区分材料说明与词缀。核心／扩展23文件168项通过，类型、格式、构建及包检查通过，无新权限或依赖。全仓verify未重跑，完整页面和关键词交互验收仍待补齐。


## 0.1.66补验：材料关键词与窄视口布局

本轮未修改运行时代码。新建无扩展会话和加载最终ZIP的会话，均打开PoE2 Data预兆类别70，分别对Catalyst及左旋死灵说明的Desecration关键词滚动居中、真实hover、真实click。每次用elementFromPoint核对中心命中Catalyst／Abyssalify原节点，两边均无导航变化；可见dialog、role=dialog及tooltip相关节点均为空，快照未出现新交互。由此取得这两个目标的原站／扩展行为一致证据，不把“没看到提示”推断为所有关键词本来不支持提示，也不声称已经验证提示内容。

对最终包全部11个中文Shadow DOM宿主读取几何边界：桌面父文本容器宽375、中文宿主365；390×844视口下父容器约270.14、宿主约260.14。11段均在各自父容器左右边界内，scrollWidth未超过clientWidth，未发现中文段落横向溢出。这是DOM几何检查，未替代完整视觉／读屏验收。

390视口整页仍可横向滚动：无扩展原站body及根宽417，扩展为400。原站基线本身也溢出，不能把整页宽度直接归咎于新增说明；本轮未修改第三方站点布局。已关闭两个验收会话。仅更新验证文档并运行git diff --check，未重复源码测试或构建。


## 0.1.67：扩大用途说明并核对限制

新增26条完整匹配的预兆说明，总计37条。覆盖混沌、点金、富豪、瓦尔、崇高、神圣、机会石及完美／腐化精华的已读取用途，以及低血恢复、经验损失、赌博购买、估价、神龛、保险箱和一次揭示重选。Low Life沿国服词缀译作低血；只翻译公开文案，不据此验证游戏机制。错误估价未解释成必定获利，最低词缀等级未改写为最低等阶。

最终ZIP独立解包重启浏览器后，49段原始说明innerHTML与无扩展基线完全一致，37条中文对照实际出现、12段保持英文，所有37条保留在物品栏中激活的前提。另核对低血两类恢复、75%经验损失、50%免金币且不消耗、引路石排除怪物群规模以及机会石不摧毁。新增四项回归先失败后通过，改变75→100、once→twice或删除否定条件的未知句拒绝匹配。

核心／扩展24文件172项通过，类型通过。格式检查首次发现导入分隔问题，修正后无错误，四项新增测试重跑通过并重建最终包；包检查仍6916词条及storage唯一权限。用途人工句位于适配器，不计入术语词条数。全仓verify未重跑，完整覆盖与剩余说明仍未完成。


## 0.1.67后续术语核对线索

在现有国服primary词缀表（trade2 poe.game.qq.com，游戏快照0.5.5，抓取2026-09-11）中找到此前未核实的专名：`explicit.stat_3418580811|28`乌拉曼、`|24`阿曼娜姆、`|26`古加尔；`|22`梅德维德、`|21`沃拉娜、`|23`欧罗什。另有`explicit.stat_3815617979`阿兹莫里之灵和`explicit.stat_1352729973`盗贼流放者；乌崔德见既有gray物品表的Uhtred's Chalice→乌崔德圣杯。

这是后续译文的可追溯线索，尚未加入用途整句匹配。同一primary专名句把Desecration写为“渎灵”，当前界面人工表使用“亵渎”，需要按词条语义和版本单独审查，不能只凭孤立行全局替换。Monster Effectiveness未在本次词缀表查询中命中；Sanctified Staff只是基底名，不能用“神圣长杖”证明Sanctify制作状态的对应译名。本轮不修改运行时术语或制作机制。


## 0.1.67标准全仓验证（2026-09-25）

在干净源码基线31f54c8执行`pnpm install --frozen-lockfile && pnpm verify`，退出码0；六包类型检查、Biome1098文件、581个测试文件全部通过，4857项通过、1项跳过，测试耗时465.94秒。跳过仍为build-core roundtrip的本地真实样本缺失分支，不是新加跳过。全部构建、dict:check和craft:check通过；静态站大块体积警告、zh-CN13／zh-TW17既有审计项及目录未解析声明保留。旧制作网站只做回归，没有恢复开发。

另执行标准`pnpm extension:check`通过：0.1.67、6916词条、仅storage权限。已验收ZIP的content.js、dictionary.json、manifest.json与本次全仓重建逐字节一致，交付副本与原ZIP一致；该ZIP SHA-256为`cd90047bc976962d4e65895eb226432fef9e3aed3412bc69592125650bf88add`。ZIP字节哈希用于本次产物，不承诺重新打包的时间戳相同。

仓库级frontend-design、webapp-testing两套技能及许可证的8个文件均与vendor-lock哈希匹配，未做全局安装，ZIP不含技能源码。安装说明开头遗留的0.1.62已修正为0.1.67。全仓通过不替代完整浏览器矩阵、装备覆盖或尚受凭证限制的远端CI验收。
