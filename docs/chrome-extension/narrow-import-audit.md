# 导入面板窄窗口验收（2026-09-26）

使用已交付0.1.99独立加载的扩展，Linux Chrome151.0.0.0，新版CoE的PoE2／English。实际设置浏览器视口为320×640，测试自造错误攻击弓、既有长文本诊断样本及含交易备注的可导入攻击弓；未通过页面脚本注入译文。截图均已查看。本轮没有发现需修改运行代码的缺陷，版本保持不变。

## 实际结果

| 操作 | 证据 |
| --- | --- |
| 320宽度显示错误双范围样本 | 原文／英文对照纵向排列；弹窗clientWidth与scrollWidth均277，扩展区域两者均257，无横向溢出。诊断可阅读，原生关闭按钮在可见区域。 |
| 长文本定位 | 使用fixtures/long-import-missing-range.txt，先把输入框scrollTop置0，再点击实际诊断“定位第69行”。selectionStart=911，选中“冰霜抗性 +25%”；scrollTop变为952，clientHeight=346、scrollHeight=1298，截图确认选中行在输入框可见区域。 |
| 窄窗口填入 | 实际点击填入，原站输入以Item Class: Bows开头，交易备注未进入英文文本。按钮能通过弹窗滚动访问。 |
| 恢复原文 | 实际点击恢复，原始中文、换行及~b/o备注完整还原；弹窗与扩展区域仍无横向溢出。恢复和填入按钮进入预期禁用状态，等待重新预览。 |
| 恢复1280×800视口 | 重新预览后两列顶部y相同，宽度均350，x分别284与646；对照恢复并排，不滞留窄窗排版。 |

长文本首次脚本误用第68行，而实际诊断是第69行；定位调用未命中，不算功能失败。读取实际诊断标签后在同一会话继续操作并取得上述结果。

## 范围说明

这是桌面Chrome的窄视口、滚动和真实按钮交互验收，不是手机触摸、浏览器缩放200%、Windows输入法或屏幕阅读器测试。原站底层页面自身可能有窄窗限制，本轮只核对已打开导入弹窗与扩展区域。输入框和弹窗各自有滚动条，未将双滚动结构宣称为已优化或消除。

本轮只更新验收文档，未修改运行代码、样本、依赖或安装包，未重复执行代码测试／构建。全仓Biome及diff检查用于文档变更；最近运行验证分别为0.1.99相关250项、0.1.95全仓4913项通过／1项既有样本跳过，均是历史基线，不冒充本轮执行结果。
