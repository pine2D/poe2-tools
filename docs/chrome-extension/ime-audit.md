# 真实拼音输入验收

日期：2026-09-25。扩展0.1.58最终ZIP；Chrome for Testing 151.0.0.0，有界面X11；Fcitx5 5.1.7拼音。页面为新版CoE的英文PoE2 Data物品搜索与制作页。

本记录包含Linux Fcitx5在Data、制作基底、词缀与条件输入中的分项证据。条件的首次原生点击曾未生效；后续复现确认自动化点击落在视口外，复查结果见末节；不证明Windows微软拼音、其他输入法或完整IME矩阵已验收。

## 环境与方法

- 使用本机已有Fcitx5、拼音模块、GTK输入模块、Xvfb和libXtst；没有安装新软件或升级全局工具。
- 在临时目录放置独立XDG配置、数据、缓存与运行目录，通过独立D-Bus和Xvfb启动浏览器与输入法。配置参考[Fcitx官方设置说明](https://fcitx-im.org/wiki/Setup_Fcitx_5)，只对测试进程设置环境变量，不修改用户的配置。
- 首次Fcitx会话受继承的Wayland环境影响退出，该次不计入验收。终止确认后，移除测试进程的Wayland变量并关闭其Wayland模块，使用X11与keep选项成功建立会话。
- 浏览器自动化负责定位输入框和读取结果；拼音字符、空格、方向键、回车与取消键由libXtst向独立X显示发送系统按键，交给真实Fcitx5处理。没有用fill写入中文，没有调用dispatchEvent制造composition事件。
- 在页面window捕获阶段只记录自造查询的事件类型、data、key、isTrusted与输入框值，不读取或改写CoE内部业务对象。浏览器命令使用与启动时相同的XDG运行目录，避免连接同名的空白测试实例。

## 已验证路径

| 步骤 | 实际结果 |
| --- | --- |
| 聚焦Data物品框，系统输入`fuwen` | 输入框预编辑值为`fu wen`；扩展候选按钮0个 |
| 系统空格确认首个拼音候选 | 输入框变为“符文”，出现50个扩展查询候选；前6项为短基底，第7项行家符文，第8项身躯符文 |
| 系统Down八次 | 焦点位于“身躯符文 → Body Rune”按钮 |
| 系统Return | 输入值变为`Body Rune`；原站返回身躯、次级身躯、高级身躯、完美身躯符文4项；保留中文查询提示 |
| 选中英文查询后删除，再系统输入`fu` | 预编辑值为`fu`，扩展候选0个 |
| 组词中系统Escape | 输入值清空，扩展候选仍0个；没有提交半成品英文或中文候选 |

事件记录显示compositionstart、compositionupdate、拼音input和keyup的isTrusted为true。确认与取消时观察到的compositionend为false；没有查明该结束事件标记的来源，不宣称所有事件都带可信标记。真实IME证据来自已运行的Fcitx5及系统按键、可信预编辑事件与实际中文确认结果的组合，而非单独依赖compositionend。

确认“符文”后没有立即替换为英文，仍由用户选择候选触发原站查询。取消时没有遗留扩展候选。尚未覆盖输入法翻页、混合输入、跨输入框切换、重载、长时间连续输入和其他平台。

## 清理

完成后关闭该隔离浏览器和本轮误连产生的空白实例；通过独立D-Bus的`fcitx5-remote -e`结束输入法，启动脚本退出并清理其Xvfb。测试进程正常退出，未改变用户输入法配置。临时目录中的自造输入记录不作为产品数据随扩展打包。


## 制作页追加核对

同日新建隔离浏览器与输入法会话，仍使用0.1.58最终ZIP，未改变运行逻辑。

### 基底搜索

- `#searchItemInput input`通过真实拼音输入`fuwenfaqi`时，预编辑为`fu wen fa qi`，扩展候选0个，compositionstart与input为可信事件。
- 输入法整句首选为“符文发起”，扩展未找到候选，也未将未知词猜测替换。重新分词输入`fuwen`、空格，再输入`faqi`；实际输入法候选窗显示第三项“法器”，系统数字3选择后形成“符文法器”。未用脚本写入目标中文。
- 扩展显示普通Runed Focus与Runeforged Runed Focus两项；系统Return选择唯一精确项，原生输入变为Runed Focus，原站展示两张基底结果卡。点击普通基底的“制作”后进入符文法器制作页。

### 制作词缀搜索

- 页面导航重建文档，首次立即定位词缀框未成功，该次不计入验收；等控件挂载后重新聚焦，并在新文档重新安装只读事件记录。
- `#searchInput input`真实输入`shandiankangxing`期间预编辑为`shan dian kang xing`、扩展候选0个，compositionstart为可信事件。空格确认成“闪电抗性”后出现候选。
- 系统Down、Return选择基础闪电抗性，原生输入为`#% to Lightning Resistance`；原站可见普通与精华两行，公开group身份分别为78和78_1001、search均保留英文。没有把所有中文近似候选视为当前基底可制作的词缀。

### 计算器条件输入与点击复查

- 在原生条件下拉框真实拼音输入同一词组，确认前无扩展候选；确认后仅有当前原站选项能匹配的基础闪电抗性候选。
- 系统Down后焦点仍为INPUT，aria-activedescendant指向扩展虚拟选中项；Return提交英文模板后，原生下拉框保持editing，提供78和78_1001两个选项。
- 第一次点击原生78后标签为空，没有记为选中成功。重新打开并再次点击后生成chosen条件，当前标签正确，li.selected身份为78。随后在新增空白条件重复真实IME流程，首次点击精华78_1001成功，两条chosen条件分别保留78与78_1001。
- 视口高度837时，第二轮原生选项列表top约825.8、bottom约919.8，需要滚动才完整可见；但这一轮首次点击成功。当时第一轮失效原因未确认，未归因于输入法、布局或扩展；后续隔离复查见下节。更完整的首次点击稳定性与窄屏矩阵仍待核对。

本轮没有执行制作材料操作或模拟运行，不能将搜索与条件选择成功代替完整制作链验收。结束后关闭隔离Chrome，并通过独立D-Bus退出Fcitx5，启动脚本正常退出并清理Xvfb。


### 首次点击失效的隔离复查（2026-09-25）

- 最终0.1.58包、1050×837视口，以普通英文Runed Focus导入后，在条件框直接输入“闪电抗性”并点击扩展候选，不使用IME，仍复现原生选项点击未保存。只读记录pointerdown、mousedown、blur、focusout、mouseup与click，不改写原站处理函数。
- 失败时普通选项78的top为820.79、高46，自动化点击中心y=843.79，超过视口高837；pointerdown与mousedown实际目标为HTML，随后输入框失焦、列表关闭。工具返回Done不代表命中词缀。这次失败是自动化点击位置失效，不能据此判定扩展或原站选择逻辑故障。
- 页面向下滚动320后重新展开，选项top为438.67、bottom为484.67；点击中心y=461.67命中LI，原站chosen条件的li.selected为78。没有修改生产代码。
- 通过扩展设置停用翻译与搜索，在新增条件原生输入Lightning Resistance，扩展候选面板数量为0。精华选项78_1001位于571.67至607.67，点击y=589.67命中选项内SPAN；最终两条chosen条件身份为78与78_1001。这是同一浏览器的停用对照，不称为未安装扩展的独立环境。
- 上一节历史首击没有事件目标记录，不能倒推每一次失败均出于同因；本次已复现并解释相同症状，因此不再把该自动化失败单列为已证实的产品缺陷。完整窄屏、Windows IME及首次点击矩阵仍待验收。隔离浏览器已关闭。
