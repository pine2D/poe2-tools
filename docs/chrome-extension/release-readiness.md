# 核心覆盖与发布验收

2026-09-26，起点28e4d22／0.1.110。用户授权按两轮建议实施；沿用现有扩展架构，不增加站点、制作规则或生产依赖。

## 第一轮：核心覆盖

已发现：词典已有盾牌、箭袋、弩、单手／双手武器等类别，但装备输出未复用这些译名；护甲与闪避值已能解析，却未翻译。按字段补齐，不按基底开通。继续保留真实歧义、未知标题和损坏输入提示；催化品质仅处理明确描述符，不推算效果。

- [x] 复用现有词典补类别与面板字段，数字及范围原样保留。
- [x] 覆盖中英文、全角标点、未知／歧义字段及既有样本回归。

## 第二轮：发布验收

- [x] 实装包核对新版介绍、首页与Data搜索、制作页导入和POB复制。
- [x] 核对关闭恢复、重新启用、刷新及同目录升级。
- [x] 运行集成门禁、核对包内容与来源，交付单一候选版本。

Windows Chrome真实输入法仍须Windows环境验证，Linux自动化不能替代。远端推送／CI现有审批限制单独记录，不以本地通过声称远端通过。未达到上述证据前保持预览版，不为版本号提前宣称1.0。


## 本轮证据

候选0.1.111，字段转换仅在扩展内补充；没有修改item-core、旧制作站、独立词典来源或浏览器权限。类别使用既有UI词典；旧样本回归加载同一份UI表，不保留原先硬编码项链补丁。只有已知面板字段能消除对应未知行诊断，其他原文继续保留提示。

17项字段回归包括类别、护甲／闪避／格挡、英文催化品质、全角标题、未知／歧义字段、台服同形词。初次13项失败；独立审查另发现核心已知类别可绕过词典歧义，增加失败回归并修复；补台服法杖→Wands回归，防止被国服Staves覆盖。催化品质仅透传英文明确描述符，不将中文猜译当作国服真机格式。

最终ZIP独立实装，Chrome管理页确认0.1.111：

| 流程 | 观察结果 |
| --- | --- |
| 0.1.110→0.1.111同目录升级 | 开发者模式开启后原生Reload生效；启用与双语偏好保留。先前未开启开发者模式的点击未生效，已通过管理页版本核对纠正。后续语言隔离修正单独打包实装，未重复升版本。 |
| 新版介绍 | 独立会话访问用户指定whats-new，选择PoE2后正文、标题与导航显示简体。旧验收会话连续CDP导航超时，已关闭；不据此推断站点或扩展缺陷。 |
| 首页中文搜索 | 水晶法器显示明确中英候选，Enter回填Crystal Focus。历史基底结果完整对照见兼容性记录，本轮不扩大为全词库验收。 |
| Data材料搜索 | 崇高石→Exalted Orb，异步结果实际显示崇高石、高级及完美崇高石等原站结果。 |
| 制作词缀搜索 | 闪电抗性给出多候选，选择对应英文模板后原站输入为#% to Lightning Resistance；关闭／启用不改变该值。 |
| 通用字段导入 | 自造普通陈旧塔盾，类别／稀有度全角冒号，护甲123、格挡25%、物等86。英文预览保留输入值，原站接受。 |
| 原生POB对照 | 同一盾牌直接英文导入与中文转换导入、关闭扩展后三份POB字节相同。原站均重算护甲为145、格挡为26%；这是原站行为，不声称面板输入值原样导入。 |
| 关闭／启用／刷新 | popup关闭后扩展标记节点0，英文按钮恢复，英文查询和装备值保留；重新开启双语及刷新后正常显示，装备仍在。 |

这些是代表流程验收，不是所有制作机制、全部文案、所有装备格式的保证。原站计算和特殊导入问题仍以安装说明为准。两个关键边界仍明确保留：Windows真实输入法尚未验收；远端推送与CI现有审批限制未解决，本轮不重试同一被拒动作。


## 验证过程记录

首次标准pnpm verify完成596个测试文件：594通过，2文件各有1项超过原定5000ms，5007通过／2失败／1跳过。失败为旧RuneforgedArmour和extendedInfluenceBones中的重计算用例，没有断言不匹配；这两个文件单独复跑8项全部通过。期间曾并行运行浏览器和局部测试，可能存在资源竞争，但未据此认定根因。未改旧代码、测试阈值、隔离或跳过逻辑。关闭验收浏览器后重新执行原始完整门禁，最终结果在下方补录。

最终完整门禁：pnpm verify退出0，六包类型、全仓Biome1163文件通过；596测试文件全部通过，5009项通过／1项跳过，测试耗时490.45秒。跳过仍为既有build-core私有真实.build样本缺失；旧站500kB构建提示、CN13／TW17词典审计项及制作目录未解析映射／未知权重仍保留。未新增跳过或放宽超时。

全仓构建后重打包检查通过：0.1.111、6933词条、6来源、仅storage权限。最终12文件与dist及实际安装验收目录逐字相同，交付副本亦相同；六项来源哈希匹配。ZIP SHA-256：`06f79503cae50e359267d1c4e0dfd2ce9fd2d088013f62963048acd785cebc4e`。交付副本位于项目dist/poe2-extension-0.1.111.zip。

本轮两阶段本地工作完成；版本保持开发预览，Windows真实输入法和远端CI尚未验收。后续优先修实际使用反馈，不再扩建准入名单或重复已通过的无变化矩阵。
