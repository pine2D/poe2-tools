# 搜索键盘与辅助属性实站验收（2026-09-26）

扩展0.1.103最终解压包，Linux Chrome151，CoE Beta的PoE2／English。仅通过公开页面输入与真实按键操作，读取DOM属性作断言；没有向原站注入辅助属性或更改业务选项。本轮补实站证据，不修改运行代码或升级版本。

## 基底搜索

记录原站基底输入框初始全部aria-*与role属性。输入“完全不存在的装备测试”，候选按钮为0；aria-describedby引用实际存在的未找到术语说明。按Escape后提示节点及描述引用移除，全部属性与初始值相同，焦点仍在输入框，中文查询保留。

Tab离开再聚焦，未命中说明重新出现。改为“符文法器”，ArrowDown将焦点交给候选按钮；Escape归还输入焦点并恢复属性。再次离开并聚焦，ArrowDown／Enter提交Runed Focus，原站实际结果出现，焦点回到输入框、属性恢复。清空后扩展提示和描述引用均无残留。不把工具点击完成响应当作结果出现的证据。

## 模拟器条件搜索

原站导入自造two-step-lightning-route.json，打开Step 1结束条件中的既有闪电抗性下拉框。记录输入框初始aria-*与role。输入“完全不存在的条件测试”，选项为0、描述ID对应真实说明；按Enter后查询仍为原中文，没有自动提交任意候选。Escape撤下面板并恢复全部属性、保留输入焦点。

输入“闪电抗性”后ArrowDown保持输入焦点；aria-activedescendant对应实际aria-selected=true选项，且该选项所属listbox ID等于输入框aria-controls。Escape后活动选项节点移除、全部属性恢复。本次未应用条件改动，也未把这一辅助属性检查当成完整流程保存验收。

## 范围与验证

没有读屏软件播报、Windows输入法或工具栏popup生命周期证据；DOM引用正确不等于实际读屏已验收。输入框用途改变、原站追加说明等分支已有自动回归，本轮未伪造实站DOM来代替原站行为。当前结果为基底搜索和模拟器条件两个已描述路径，不外推所有搜索框。

本轮执行全仓Biome及git diff检查；无代码修改，未重复代码测试或打包。0.1.103实现时260项相关测试、构建与包检查，以及0.1.101全仓基线，分别保留原来的版本边界。
