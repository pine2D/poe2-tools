# 条件搜索空结果验收（2026-09-26）

扩展0.1.100，Linux Chrome151.0.0.0，新版CoE的PoE2／English。沿既有公共DOM人工UI来源，仅翻译计算器和模拟器条件下拉框的`li.no_result`完整提示No results for this search，译为“没有符合当前搜索的条件。”。通用词典未命中提示继续由搜索控制器独立显示，不将两种情况合并。

## 实站证据

用自造稀有粗制弓原生JSON进入制作页计算器。在0.1.99会话中，原生条件输入zz-no-condition-765并触发keyup，no_result实际可见且仍英文。最终0.1.100独立加载后执行相同操作，提示中文、查询值保持英文测试词。随后输入Dexterity，空结果行隐藏，原生候选重新可见；全部候选的value/search数组在操作前后相同。再输入未收录中文，扩展显示独立“未找到术语”提示，没有将其替换为本次原站提示。

最初误点击空白条件的隐藏.current节点，工具报告固定页头覆盖；检查布局发现该节点为display:none、零尺寸，真正可见入口为.placeholder。修正验收入口后正常完成，没有改站点节点、强制触发内部方法或绕过实际点击。

本轮实站只覆盖计算器，模拟器使用同一类结构的DOM回归；不据此声称模拟器该提示已经实站验收。也未扩大到任意下拉框或所有错误消息。

## 回归与打包

两项回归先失败后修复，分别覆盖计算器和模拟器：隐藏行不改写、显现后翻译、双语、输入值和业务属性不变、区外原文保持、用途类移除／重新加入及关闭恢复。相应class边界纳入观察器，确保原站复用节点时正确恢复。30文件252项相关测试、扩展类型、全仓Biome、构建及包检查通过，未重跑全仓verify，最近全仓基线仍0.1.95。

只改显示层，不修改原站过滤逻辑、词缀数据或查询提交。最终ZIP12文件与dist、实装目录及交付副本一致，6933词条、6项来源、仅storage权限，无新依赖或网络请求。
