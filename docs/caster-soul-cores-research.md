# 施法武器魂核

核对日期：2026-09-24。沿用已登记 MIT PoB2 固定提交 ce566eac45ea8a86477f513c7ee65a1ebe60014e 的 ModRunes、ModScalability，当前制作目录标注0.5.5快照；本轮没有将其更新为当前客户端版本，也未新增抓取或运行时请求。

## 接入范围

固定目录共有10个对应分支：以下5种魂核各有 wand 与 staff 两种身份，完整记录和逐值缩放元数据在 casterSoulCoreData.ts 中核对。不能借同名材料开放攻击长杖、法器或其他部位。

| 魂核 | 主效果 |
| --- | --- |
| Opiloti's Soul Core of Assault | 获得狂怒球时，50%几率额外获得一个狂怒球 |
| Guatelitzi's Soul Core of Endurance | 获得耐力球时，50%几率额外获得一个耐力球 |
| Xopec's Soul Core of Power | 获得暴击球时，50%几率额外获得一个暴击球 |
| Xipocado's Soul Core of Dominion | 召唤生物命令技能伤害提高40% |
| Soul Core of Vizoma | 法术暴击几率提高30% |

上表为机制说明，不作为各服官方物品译名表。每种每件限一，同孔覆盖先移除旧材料；已有超限的观察与修复沿用既有规则。触发条件与召唤生物作用对象完整保留，不转成常驻伤害或角色最终收益。

魂核沿通用君王增效缩放，30%增效时上述50、40、30分别成为65、52、39。Runeseeker 的75%只影响 Rune，不能参与 SoulCore 缩放；同件混合符文与魂核分别派生。Vizoma 的法术暴击进入已有合计，能与 Vision Rune 合并显示；其余条件行逐条核对，不能因数量相同就通过来源验证。

## 流程与版本

共享施法镶嵌身份、缩放和限量核对，旧 Rune 包装入口继续严格拒绝魂核。v127 日期为2026-09-24，覆盖当前、导入声明、完整未来和未执行指引；旧版本拒绝新能力，空 v127 保存不降版，同名报价不触发升级。网页实际镶嵌后保存必须带符文目录与缩放两个指纹。

搜索及导入后继续制作、覆盖、撤销重做、三语回读与项目恢复均在本轮范围。国服真实魂核复制格式、角色最终收益及游戏随机权重仍未验证；不将合成文本通过视作真机通过。

## 对照工具

2026-09-24 读取用户指定的 [CoE 正式 PoE2 入口](https://www.craftofexile.com/?game=poe2)。该页面明确推荐 beta 新站，并介绍新版把计算器与装备操作放在同一视图。本项目据此继续检查导入／搜索、操作、目标和历史能否连贯完成；没有读取或移植同行执行代码、数值文件及权重。正式站说明自身权重经过推算，也不能作为本项目获得当前权重或再分发授权的证据。

## 本轮验证

中断后重新运行，长杖初始8项失败；扩展至两部位后核心及既有回归85项通过，v127项目两项失败随后由门禁接入修复。增效隔离、限量、暴击合计、类型伪造与网页保存等3文件31项专项通过。独立审查逐项确认10条固定记录与5条缩放声明和目录一致，未发现阻断问题。新增测试夹具的字面量类型已修正，短杖搜索混合镶嵌、合成中文长杖导入后覆盖、撤销保存与完整未来恢复重做及320px布局均经真实浏览器验证，无浏览器错误。浏览器和服务已关闭后开始完整验证；类型与lint通过；完整测试552文件通过、5文件失败（4677项通过、7项失败），其中1项为旧基础魂核测试把施法候选固定为零，已限定回无限量基础分支；另6项为5秒超时。5个失败文件按原阈值串行复现后32项通过、2项仍超时（腐化指引约5.2秒、手套雕像版本矩阵约5.4秒）；旧基础魂核断言修正后通过。命令级15秒全量重跑期间出现容量路线约20至26秒超时，主机CPU等待压力约35%，因此停止该次运行（exit130），未将其记为通过。随后从383537b导出独立源码到临时目录、复用已安装依赖直接调用Vitest，3个慢测试文件串行仍8项超时、20项通过（154秒），确认基线也存在该环境下的超时。全部初始失败文件及新增魂核测试随后串行通过：8文件65项，命令级15秒阈值，耗时88.40秒；未修改默认测试配置，也不将这次局部通过称为默认全量验证通过。

类型、lint、发布构建及词典／制作目录校验随后通过，发布资源为 craft-a52QOulA.js。默认全量运行的未通过项及基线复现均保留，远端默认CI和新版本生产验收待推送后核对。
