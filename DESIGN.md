# PoE2 Tools 界面约定

面向阅读英文构筑、核对国服术语的 PoE2 玩家。界面以完成操作为主：导入、核对、下载；扩展则是启用翻译、对照术语、预览并填入装备文本。

## 现有视觉系统

网站使用 React 与普通 CSS，颜色以 `apps/build-l10n/src/styles/tokens.css` 为准，支持系统、浅色、深色主题。以冷灰工作台、蓝色主操作和清楚的文字层级为基础。中文使用本机中文字体，不为装饰引入字体请求。装备稀有度和游戏标记色保留其语义。

扩展弹窗沿用深蓝底、暖金标题与浅蓝反馈；注入面板使用限定在 `data-poe2-l10n` 下的样式，不能改变原站其他控件。扩展源码目前位于本仓库 `feat/coe-chrome-extension` 工作树，当前主工作区只有规划文档。

## 细节与交互

- 同组操作靠近，独立信息用留白和分隔线区分；避免给每段文字套卡片。
- 桌面常用按钮至少 40px 高，小屏触控入口至少 44px。可点击设置用整个标签作为点击区域。
- 标题平衡换行，说明文字自然换行；动态数字使用等宽数字。
- 主操作与辅助操作区分明确，禁用、加载和失败必须有文字反馈。焦点可见，键盘顺序与视觉顺序一致。
- 高频操作即时响应。延续现有短过渡，不引入入场动画；尊重减少动态效果与系统高对比设置。
- 小屏导航可以换行，不依靠缩小中文文字塞进单行。对照文本保持完整，不截断装备原文。

## 本仓库的设计工作流

结合 Impeccable 的 polish / adapt / harden 和 make-interfaces-feel-better 的细节审查：

1. 读取产品约束、当前组件、样式、调用方和已有测试，确认真实任务与目标工作树。
2. 浏览器查看主要任务路径及桌面、小屏、主题、空态和失败状态；按阻断问题、层级布局、细节顺序处理。
3. 直接使用已有 CSS 与语义 token；不因设计任务增加生产依赖或重启已搁置的制作引擎。
4. 一次集中检查，一次必要修正。运行关联回归与构建，并记录实际浏览器证据及未验收部分。
5. 本地改版与线上发布分开说明；未经授权不推送或部署。

技能安装位置及固定版本见 `docs/design-skills.md`。这些技能是开发工具，不进入网站或扩展产物。
