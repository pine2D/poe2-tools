# Changelog

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。
版本号对应仓库整体（根 `package.json`），`apps/build-l10n` 与之同步；`build-core` / `dict-builder` 是私有内部包，不单独计版本。仓库私有，暂不提供版本比较链接。

## [Unreleased]

### Added

- 五种绑定符文可开放 Chronomancy、Marksman、Berserking、Soul、Decay 专属词缀池，保留部位、物等和冲突限制；贯通中文高级文本导入、目标搜索与准备路线、条件指引、费用及 v93 完整未来保存。Thrud 显式增效、多影响／特殊容量共存、骨骼揭示及绑定萃取组合仍待核实。

- 条件指引支持复制规则：保留嵌套条件、目标引用与阶段去向，将副本插入原规则之后并定位编辑；遵守十二条上限，可随项目保存恢复，不增加装备历史或材料费用。

- 条件指引新增亵渎词缀数量范围，可分别按未揭示占位与已揭示亵渎词缀继续制作或停止；随候选、回响、确认和撤销重算，网页编辑与 v92 完整未来保存、恢复、沿用方案共用同一规则。

- 腐烂预兆支持标准装备与普通珠宝的多槽揭示，保留破裂词缀，逐槽普通候选三选一及独立深渊回响；接通网页预览、剩余槽位、完整目标路线、材料费用及 v91 保存恢复。特殊容量及未核实骨骼组合继续限制。

- 未揭示亵渎阶段支持常规崇高追加：保留隐藏占位，检查剩余揭示候选，贯通三档及既有方向／强效配置、目标指引、路线、费用与 v90 完整未来恢复；固定候选、珠宝、特殊容量及未核实组合继续限制。

- 完美无限精华支持普通项链上的力量、敏捷、智慧三种百分比结果，分别选择演练结果与移除对象；贯通目标准备路线、条件指引、材料费用、中文导入及 v89 完整历史恢复，不推定等概率。

- 制作步骤清单可包含尚未执行的后续计划，逐步核对选定结果，并分别列出已消费材料、计划材料及新增费用；复制与下载跟随当前范围，未来步骤无效时拒绝导出不完整计划。

- 纯 Astrid 双工艺目标接通未来镶嵌、准备路线和 v87/v88 恢复；目标编辑按拟修改组合重新选择真实容量来源，避免 Astrid 与 Serle 的不同历史互相遮蔽，也不拼接未曾共存的容量。

- 七组目标路线支持补齐 Serle 容量：撤销至镶嵌前时，利用已核对历史生成打孔、镶嵌及后续制作步骤，沿用材料费用和目标保护；骨骼、精华、合金建议同步支持未来目标组合，实际操作仍遵守当前容量。

- 新增 Serle 稀有装备后缀容量：单枚已核对符文支持第四后缀与七组总量，贯通中文导入、神圣重掷、亵渎占位、七组目标、条件指引和 v88 完整历史；绑定孔不可覆盖，混合孔萃取仅返还非绑定材料。普通／魔法及全绑定萃取消费仍标为未核实。

- 新增 Astrid 单枚容量符文：已支持的普通武器、防具与副手可制作第二组精华／合金工艺，仍核对部位、来源、冲突及前后缀空位；贯通中文导入、容量提示、目标建议、费用、萃取及 v87 完整历史。已有双工艺时覆盖容量来源仍标为未核实，原词缀不被删除。

- 新增 Protection／Nourishment 两种限量防具符文：条件比例按来源增效，触发周期与持续时间保持不变；已有超限孔位可核对和替换修复，明确提示外部装备未知。镶嵌、费用、条件指引、三语文本、清单及完整未来历史保存为 v86 项目。

- 新增 Masterwork Rune 原孔升级：已核对家族的高级符文可升级为完美档，预览／取消不计费，应用只消耗升级材料；贯通条件指引、萃取结果、制作清单与 v85 完整项目恢复。低档升级、腐化装备和特殊孔位仍待核实。

- 新增四档重生防具符文与巩固之结界符文：生命再生按来源内部精度逐枚增效，本地结界提高与品质分开计算；贯通镶嵌覆盖、费用、锻造、条件指引和 v84 完整项目恢复。

- 新增四档结界及四档充能防具符文：普通底子可通过符文获得结界，最大值与再生提高分别计算；贯通镶嵌覆盖、恐惧增效、三语回读、费用、条件指引和 v83 全历史恢复。面板显示防御项新增或消失的差值，未知状态保持未知。

- 普通防具新增符文锻造预览与应用：375 条已核对配方按实际 Verisium 数量计费，保留词缀、品质、孔位及破裂身份；贯通条件指引、撤销重做、中文步骤清单与 v82 完整项目恢复。固有属性替换掷值尚未核实的配方暂不执行。

- 建立独立符文锻造配方目录及来源校验：记录 376 条基底转换关系和 33 条未解析行，区分固有属性保留与替换；遵守灰区发布开关，供后续转换动作接入。

- 已有普通符文锻造防具可进入制作演练，新增符文结界面板和数值条件；品质、本地结界合金、固有范围与完整历史共同参与估算。v81 保存锻造起点和未执行结界指引，旧版本严格拒绝新能力；特殊跨词缀池基底与 Verisium 锻造转换另待接入。

- 新增七种防具符文演练：三档物理荆棘，以及闪电荆棘、召唤生物承伤转换、减益消退和感电效果降低。贯通已有孔导入、镶入覆盖、恐惧增效、三语回读、材料费用及条件指引；v80 保存完整历史，旧项目拒绝夹带新能力。

- 增加可复制、下载的中文制作步骤清单：按当前历史位置回放起点、每步材料和选定结果，记录移除／破裂对象、孔位、技能升级、亵渎候选及萃取返还；费用与缺价沿用工作台报价，撤销步骤和未应用草稿不计入。完整项目仍保存为 `.craft.json`。

- 支持已核对的高催化品质起点及导入：普通戒指／项链 40%、裂隙戒指 60%，移除裂隙精华词缀后保留已有品质；当前提升上限与已有量分开显示，贯通制作、有效值、消费、导出回读和 v79 项目恢复。

- 腐化收尾接入条件指引：可按未腐化、仅一次腐化和二重腐化状态分流，指定瓦尔石或建筑师宝珠后选择原有全部演练结果；复用预览、费用、阶段和 v78 项目恢复，旧实际腐化历史继续兼容。

- 增加萃取石演练：预览并记录摧毁装备后返还的已核对镶嵌物，重复符文按实际孔数计数；支持腐化装备、条件指引、一颗材料消费、撤销重做与 v77 完整项目恢复。返还清单与历史支出分别保留，不推定出售收益；未知或尚未支持的绑定镶嵌来源继续拒绝。

- 普通施法武器新增完美溶剂演练：将单一固有技能的装备等级升至 20，保留原角色显示观察，最高级未知时需主动声明；贯通费用、撤销重做、技能等级条件指引和 v76 完整项目恢复。升级后暂用项目保存，游戏风格文本导出明确提示尚未支持，避免丢失结果或误写角色等级。

- 增加四种抗性溶剂的转换关系查询，区分装备、普通珠宝与范围珠宝，支持两服材料名称及属性搜索；独立灰区表可整体停用。支持一次转换全部适用抗性、逐实例选值、费用和 v75 完整回放；同类目标保留独立数值与破裂要求，旧项目保持原规则门禁。破裂整件消费和被转换工艺标记仍待核对。

- 珠宝在未固定揭示候选时可补液态工艺并保留未揭示占位，贯通目标路线、条件动作、费用及 v72 保存恢复；明确标示占位移除和已固定候选后的再加工仍待核对。

- 普通与失落珠宝接入颅骨、定向亵渎、回响揭示和目标指引；新增 44 条独立珠宝亵渎属性，复用费用、撤销及 v71 项目恢复，按珠宝当前容量核对移除结果。真实概率与未揭示期间交错制作仍待扩展。

- 珠宝接入瓦尔无变化、独立强化、顺序重选及建筑师成功／摧毁，贯通品质与工艺保留、费用、撤销、三语导入导出和 v70 全历史恢复；随机增减词缀与真实概率仍待扩展。

- 四类失落珠宝接入独立的 160 条范围词缀查询、常规制作、目标路线与高级文本导入导出；保留小型／核心天赋完整语义和两服译名，隔离普通珠宝池。半径随词缀派生，支持预览、历史比较及项目恢复。v68 接入 13 种远古液态情感的 46 个保证结果，支持超大半径、范围抗性、转换及增容，并贯通目标路线、报价和恢复。v69 增加已有催化品质导入、搜索声明、精炼材料效果比较与保存恢复；缺少范围缩放资料时不猜有效目标。旧项目保持原支持边界。

- 演练增加技能等级词缀贡献，按原文适用范围分列有效加成，显示预览与本步变化；未知掷值或增效资料保持未知，不推导角色最终等级，也不改写装备授予技能。

- 普通抗性有效值目标可自动考虑君王合金，支持从空白基底准备牺牲词缀、共同倍率下的数值条件及神圣联合重掷；建议显示实际达成与上限失配风险，沿现有演练保存和恢复。

- 合金接入指定结果替换、目标与路线建议、条件指引、材料计数及 v66 项目恢复；君王增效同步反映在抗性、有效值目标、武器镶嵌物与高级文本中。低物等、同组替换和预兆组合仍保留核对边界。

- 增加可按基底查询的合金保证属性目录：13 种材料、132 条对应，支持两服名称、属性搜索和缺失提示；独立灰区表按需加载并可整体停用。合金制作与查询共用独立关系表。

- 催化崇高支持与定向、强效组成两枚或三枚配置，接通双组选择、品质消费、目标损失、独立材料费用与 v65 完整项目恢复；不推断催化权重或成功率。

- 基础混沌支持消减与左右旋消抹双预兆：限定侧内最低等级、保留并列风险，贯通指定结果、目标建议、路线、独立材料费用及 v64 项目／条件指引恢复。

- 建筑师宝珠新增指定追加强化，支持二重腐化的两组属性、催化贡献、数值比较、三语导入导出及 v63 项目恢复；保留品质与孔内物，贯通取消、费用和撤销。组与标签交互按目录建模，仍待真机核对，不提供随机概率。

- 建筑师宝珠增加指定摧毁结果，贯通预览、取消、费用、撤销与 v62 项目恢复；摧毁后停止制作、估算和装备文本导出。英文二重腐化标记可识别；成功分支由后续 v63 接入，真实概率尚未开放。

- 瓦尔重选新增普通魔法／稀有装备的 1–3 次顺序替换预演，逐次检查合法候选、保留品质与孔内物，整次只计一颗瓦尔石；三语输出、比较及 v61 项目恢复贯通。顺序模型仍待真机核对，破裂／工艺／亵渎交互与真实概率暂未开放。

- 普通腐化强化接入独立属性层：可指定属性与基础数值，保留同组显式词缀，支持三语高级文本、催化抗性贡献、武器／防御估算、比较、费用及 v60 项目回放；特殊腐化及真实概率仍未开放。

- 增加瓦尔石两类指定结果预演与腐化状态：腐化后可继续基础符文／魂核镶嵌，普通制作停用；来源核对、三语导出、比较、费用及 v59 项目恢复贯通。完整随机腐化仍待接入。

- 接入七种基础魂核的十四条部位效果，支持恐惧增效、抗性／攻速及弩装填估算、三语来源核对、费用和 v58 项目回放；修正需求转换魂核误显示钢铁防御提示的问题。

- 普通武器符文新增 87 条效果分支，攻击武器现支持 55 种、法杖及施法长杖各 40 种；物理点伤接入武器估算与停止条件，三语来源核对、覆盖费用、撤销和项目回放沿 v57 保存。

- 制作目标新增“从当前装备提取”：勾选已核对词缀、可选精确基础数值与已有破裂要求，预览后替换显式目标，沿现有建议、路线、条件指引及收藏方案复用。

- 防具普通符文扩展为 12 族 48 种，新增生命、魔力、魔力再生、晕眩门槛、药剂回复与三属性；导入贡献核对、恐惧精华增效、覆盖费用和条件指引沿 v56 完整回放。

- 条件制作指引支持普通／催化品质范围及指定催化类型，可在催化品质消费后转入后续规则；缺失品质不视为零，比较预览不影响判断，嵌套条件和历史恢复沿 v55 保存。

- 戒指／项链新增单枚催化崇高预兆，支持三档崇高消耗已有催化品质，提示显式与固有目标的有效数值损失，并贯通草稿、费用、条件指引和 v54 保存回放。催化标签不作为保证结果，真实权重仍未知。

- 裂隙精华制作后的催化预览支持已核对的最大品质属性，普通戒指／项链预览上限为 40%，裂隙戒指为 60%；移除与历史恢复后重新计算。仅为只读估算，尚未接入逐颗催化或提高后的实际品质状态。

- 普通珠宝基底目录接通常规词缀查询，新增液态材料与保证属性的中英文检索、双侧工艺和来源限制说明。制作区同步支持按保证属性查材料，装备尚不满足使用条件时也能查看已核实效果；修正普通珠宝仍被提示“词缀池尚未收录”的入口问题。

- 多步制作路线接入液态情感保证属性、临时增容与最终增效，可准备稀有度和填充组、清理已有工艺，并联合规划神圣石与有效数值目标。路线显示结果侧别、跨方向移除风险和费用，第一步可进入草稿，沿 v53 保存回放。

- 普通珠宝新增强效的液化凶残两侧 40–60% 增效，累计支持 13 种液态材料、50 个映射。增效与催化品质相加，贯通神圣石、破裂保护、有效目标、抗性条件、三语高级文本与 v53 项目回放；基础掷值保持独立，国服原生百分比头待验收。

- 普通珠宝支持强效的液化轻蔑双侧结果及额外词缀位；移除增容后保留第三条同侧词缀，新增容量、条件空位、费用、三语往返与 v52 项目恢复共用规则。液态制作累计支持 12 种材料、42 个映射。

- 珠宝制作新增强效的液化悲哀三色条件效果和钻石珠宝的液化孤独工艺，支持无掷值预览、工艺专属目标、条件指引、费用及 v51 项目恢复；无名称工艺可通过高级文本往返。工艺数据与普通生成池明确隔离。

- 装备工坊新增基础液态情感制作：十种材料覆盖红玉、蓝玉、翠绿珠宝的 30 个已核实映射，支持移除目标风险提示、结果预览、条件指引、材料费用、撤销重做及 v50 项目保存恢复。强效、范围与钻石珠宝交互仍标明未支持。

- 演练收藏新增“沿用方案”：预览后将目标和条件流程用于当前装备，保留制作历史与报价，校验跨基底目标兼容性，并从当前步骤重启分阶段指引。

- 新增装备抗性合计面板及条件指引，按固有、显式、当前符文和已核对催化品质统计四抗与元素合计；提供制作前后对比，缺失掷值或孔位保持未知，支持 v49 项目恢复。

- 条件指引新增装备面板范围，可按武器 DPS、攻速、暴击率、装填时间或三项防御继续、转向及停止；显示当前估算与未知原因，支持嵌套条件、撤销和 v48 项目恢复。

- 新增祝福预兆与神圣石搭配：仅重掷固有属性，保留显式词缀原值；接入目标建议、路线、条件指引、费用与 v47 项目校验。
- 新增命名演练收藏：多份完整项目独立保存和恢复，同名不覆盖，损坏记录隔离；支持跨标签刷新、容量提示，并保留原快速保存入口。
- 流程画布改为专注视图，支持模态内阶段管理、键盘返回与焦点恢复；保留缩放和阶段选择，关闭清除待连线，常规演练页面减少占用。
- 分阶段指引新增可编辑流程画布：选择输出与目标阶段连线，查看判断路径、回连及自环，创建停止规则并定位编辑；支持键盘、缩放和滚动，复用现有项目与实际材料历史。

- 条件制作指引支持嵌套全部、任一与取反，可保留已有条件分组、解包和编辑；未知孔位不会因取反误判，完整目标引用校验、制作回放与 v46 项目恢复贯通。

- 阶段工作规则可配置无法执行时的去向，显示预检原因并沿备用路线继续判断；与纯跳转共用循环检测，历史与费用只计实际制作。v45 保存转向并拒绝旧版注入。

- 条件指引新增不消耗材料的纯判断跳转、可解释路径和零消费循环检测；实际制作成功后保持最终阶段，v44 项目恢复重算路径并拒绝旧版注入。

- 条件指引支持分阶段流程、入口选择和回连；按实际操作身份推进，撤销及项目恢复重算阶段，修改规则或目标从当前装备重启。v43 保存流程起点并校验旧版本注入。

- 条件指引可检查指定目标组至少满足或尚不足 N 组，沿用各组数值、品质、替代档位及破裂要求。删除被引用目标时明确暂停并保留可修复配置，v42 项目保存规则并拒绝旧版注入。

- 条件指引支持巧匠石与符文，按已有孔数和空孔数范围继续制作；第一空孔逐次填充、指定孔位明确覆盖，草稿聚焦并准确显示被替换材料。v41 项目保存配置并校验未操作规则的镶嵌来源。

- 条件制作指引支持精华、骨骼施加与揭示、破裂步骤，新增词缀组数及亵渎阶段判断；待揭示占位参与空位和破裂门槛，完整目标仍须揭示完成。特殊结果复用原有选择、草稿和费用流程，v40 项目在未操作时也校验规则材料的数据指纹。

- 新增可编辑的条件制作指引：按第一条匹配规则选择通货、预兆或停止，支持稀有度、目标达成及前后缀空位的联合条件。操作后、撤销和恢复时按当前历史重算，提供步骤上限与非法动作原因，v39 项目保存规则；明确无预兆的规则和建议不会继承手工旧选择。

- 制作目标增加“至少 N 组显式目标”，联动普通、精华、骨骼建议和多步路线的停止条件；同组替代只计一次，固有条件与指定破裂组始终必选。部分模式验证包含必选组的合法组合，v38 项目保存数量条件，修改条件会取消旧搜索。

- 显式与固有属性目标支持按品质后有效值判断，并贯通常规、精华、骨骼建议及路线。来源精度不唯一时要求全部可能结果满足阈值；数值反解跳过无内部逆像的显示空档，无法达到的条件不生成误导建议。v37 项目保存目标口径及缩放来源指纹，神圣重掷保留不可缩放尾注。

- 已有催化品质接入高级文本导入、搜索起点声明、基础通货、估算显示及 v36 项目回放；未知中文类型需核对，保存基础值与来源指纹，三语出口保留英文品质标题。实际逐颗施加及消费继续核实。

- 高级装备文本支持 `当前值(固定基础值)`：解析与中英对照保留单个数值及基础注释。与目录精确一致的固定属性可导入制作、三语导出和项目回放；增效值不同则明确提示待还原，不能按普通基础值重复计算。

- 制作目录新增 2843 条属性缩放声明与固定来源指纹，催化预览优先使用内部精度并保留固定条件。显示值对应多个内部值时明确提示结果不唯一；每行展示计算依据，不以粗网格替代已知精度。

- 戒指、项链与四类普通珠宝增加 13 类催化剂效果预览，使用独立中文材料名，比较固有与显式属性并随制作、撤销和恢复重算。普通上限 20%、当前裂隙戒指 40%；缺数值、未核对的固定数字或不可缩放属性逐行说明。按逐行依据估算，实际施加及材料消耗仍待接入。

- 手套／鞋的 60% 恐惧精华接入普通符文增效：精华制作、同孔数值比较、防御估算、三语导入导出、撤销和 v35 项目回放贯通；按固定 PoB 快照逐枚取整，真机待验收。

- 制作路线支持按已应用报价优先搜索，保留较长但更便宜的路径；缺价不当零，改价取消旧搜索，目标保护及完整回放继续生效。

- 装备工坊增加自填材料报价与起点成本，统一实际材料计数，预览路线新增及累计费用；缺报价、零成本和计价单位明确区分，报价随 v34 项目保存恢复。
- 装备工坊支持强效崇高的双词缀新增、左右旋配对及三档崇高；逐条核对空位、冲突和等级，目标路线与历史共用结果，组合材料分别计费。v33 项目严格拒绝旧版注入新配置。
- 装备工坊支持四类普通珠宝的搜索、导入、两前两后制作、目标路线和破裂演练；新增 160 条固定来源珠宝词缀，独立记录排除审计与 v32 项目指纹。三服珠宝使用说明不再阻止导入，特殊品质、范围及增容交互仍待支持。
- 装备工坊新增光明预兆配基础剥离，可移除已揭示亵渎组后重新亵渎；目标路线、来源保护、历史与两项材料费用共用现有引擎。v31 项目保存完整循环，旧项目不能注入光明操作。
- 装备工坊新增消减预兆配基础混沌的演练、目标建议及路线：按未破裂词缀的目录出现等级筛选，显示并列风险和独立材料费用。v30 项目保留预兆历史，旧项目不能注入新操作。
- 装备目标新增“要求此组破裂”，支持同组替代档位、独立数值条件及保存恢复。多步示例按实际合法池准备四词缀、先调整数值后破裂，展示随机候选与材料；默认保留已满足的身份和数值。v29 项目保留要求，旧版本禁止注入新字段。
- 装备工坊新增稀有装备破裂演练与已有破裂组导入：预览目标和锁定结果、独立计费、撤销恢复及三语导出共用历史。后续神圣与移除保护锁定组，目标指导识别不能再调整的数值；v28 项目保留独立工艺标记及待揭示回响选项，旧项目禁止注入新状态。指定结果不代表游戏可指定或已知成功率。

- 装备工坊可识别并继续演练已有 Genesis Tree 专属施法／召唤属性，支持数值目标与 v27 项目恢复；新增候选仍遵守各制作方式的资格，尚未实现 Genesis Tree 生成。

- 装备工坊支持深渊回响预兆：首次固定候选时启用并计费，可重选一次并保留两组选项，贯通目标指导、预览、历史和 v26 项目恢复。远古骨骼／指定巫妖搭配暂未验证，首组选回依据作者说明、待真机确认。
- 装备工坊支持两枚方向与三枚巫妖骨骼预兆，可组合一方向与一巫妖；配置贯通候选、目标指导、预览、费用与历史，v25 项目保存并严格回放。同族不足三项暂不支持，风险与示例仅覆盖已支持结果，不推断游戏抽样或消费。

- 装备工坊支持亵渎专属目标，以及普通、精华和固有条件的联合骨骼路线；施加骨骼、固定三候选、选择揭示沿原历史与费用逐步应用。未揭示项目可以继续分析和规划，固定候选不被自动改写；v24 拒绝旧项目注入新专属目标。

- 装备工坊可对普通数值固有属性及咒符栏设置上下限，与显式词缀目标联合提供神圣建议、有限路线和达成/损失对照；仅固有目标也可规划，v23 项目保存完整条件并核对目录身份。旧腰带范围及缺范围来源不被目标自动升级。

- 装备工坊支持当前目录 15 种腰带基底的搜索起点与中文导入，咒符栏实值及范围贯通神圣、目标建议、精华、骨骼、比较和三语导出；v22 项目保留旧范围并完整回放。缺少高级范围的原文仍可继续其他制作，神圣明确受限；咒符与传奇保持只读。

- 装备工坊接入九种常规骨骼、未揭示占位与三选一指定结果演练：满词缀移除、等级与部位限制、固定候选、目标风险、费用和撤销恢复共用制作历史；v21 项目保存全部步骤。未揭示期间混合加工及真实候选抽取概率仍待扩展。
- 装备工坊支持已揭示亵渎稀有装备导入后继续常规通货与精华演练，保留独立来源、整组比较、三语结果和 v20 项目恢复；新增 199 条固定来源专属记录并与普通生成池隔离。工艺与亵渎各限一组，旧项目不能夹带新状态；骨骼施加与三选一的后续支持见本节新增项。

- 装备制作结果支持简体中文、繁体中文与英文文本预览、复制和下载；中文基底与属性采用独立词典并核对反查，保留混合组、数值范围和技能最高等级，未译或有歧义的内容保留英文并说明。切换语言不影响制作历史，词典加载失败可重试或切换英文。

- 装备工坊新增全部目标达成的多步示例路线，支持现有通货、精华、预兆、替代档位与数值条件；默认保留起点已达成目标，展示每步风险和预计材料，复用原有草稿、历史与保存恢复。路线按需在浏览器后台计算，可取消并丢弃过期结果，不将有限搜索视为成功率或最优成本。

- 装备工坊新增普通攻击武器本地面板估算与制作前后变化，涵盖各系伤害、武器 DPS、攻速、暴击和弩装填时间；支持武器起点品质核对，沿用当前词缀、符文和制作历史，不混入角色技能加成。
- 装备文本解析补充中英文武器伤害、暴击、攻击每秒与装填属性行，保留原始面板数值用于对照，避免把这些行误当未知词缀；支持简繁各自标题及多系元素端点。

- 装备工坊扩展普通武器、法杖和施法长杖的已有孔核对、巧匠石打孔、元素／钢铁符文镶入及替换；区分长棍与施法长杖，以及同名符文的武器／施法效果。v19 保存完整孔位与来源，旧版本不能夹带新增武器操作。

- 装备工坊支持招架、架盾等不带等级的授予技能：按现有官方译名核对导入与目录展示，保留无等级形态并支持英文导出回读；v18 严格恢复技能来源，合法旧空白基底仍可升级。

- 装备工坊新增当前演练结果的英文装备文本预览、复制和下载，保留词缀分组、工艺标记、品质、技能及当前符文效果；只导出已应用状态，不复制原交易备注或旧面板。规范普通词缀头可省略未知阶级后重新核对，CoE 转接仍要求已验证格式。

- 制作目标新增精华示例准备路线：从普通／魔法装备通过蜕变、富豪逐步进入可用精华状态，显示指定结果与预计材料，只预览首步并随实际应用重新计算建议；有限搜索不宣称穷尽路线。

- 制作目标新增精华方案入口：支持保证档位与替代档位、符合数值条件的演练预览，区分选定移除损失与整个池的随机风险，并推荐确实收窄移除池的结晶预兆；复用现有费用和v17历史。

- 装备工坊支持左旋／右旋结晶预兆搭配完美与腐化精华，按侧别筛选合法移除组与目标风险，分别记录精华和预兆费用；v17 项目保存完整搭配，恢复拒绝旧版精华预兆字段。

- 装备工坊支持完美／腐化精华的稀有装备替换演练，按前后缀容量列出可移除组，提示已选目标及替代档位的损失风险；精华独有属性可设为目标。新增有限技能等级互斥校验，v16保存移除结果、全历史回放并限制旧版本新增能力。
- 符文效果显示按目录 stat hash 精确消歧，修复钢铁效果因不同词典语境同英文而回退英文的问题；简繁译文独立，未命中仍保留英文。
- 装备工坊支持前三档精华保证属性制作，魔法升稀有并加入一组工艺词缀；支持数值核对、目标提示、后续常规通货及统一费用与历史。项目 v15 校验精华来源指纹并拒绝旧版本注入，完美／腐化、未解析效果与低物等交互继续标明边界。
- 装备导入保留工艺、亵渎与破裂属性尾注及破裂物品旗标，中英文对照和目录身份匹配均保留来源；唯一可核验工艺组可进入演练；已揭示亵渎与破裂的后续支持见本节新增项，未验证转接仍拒绝。
- 基底详情新增精华与保证属性查询：82 种材料按装备类别连接固定词缀，支持独立简繁名称和中英文属性搜索；未解析效果明确保留。
- 装备工坊支持六枚定向预兆搭配基础崇高、剥离与混沌；候选及目标建议按配置筛选，混沌只限定移除侧。历史分别记录通货和预兆消耗，项目 v14 严格保存恢复并拒绝旧规则注入。
- 制作目标支持同组可接受档位：组内任一达成、各档独立数值条件，建议识别实际档位的损失与神圣风险；项目 v13 保存替代项，旧版项目保持精确目标语义。
- 装备工坊支持带等级授予技能的中文导入与最高等级保留，普通显式制作、撤销和保存恢复沿用技能来源；搜索起点可声明已有技能等级。项目 v12 严格核对来源和旧规则，角色最终等级与神圣交互仍待验证。
- 装备工坊将普通打孔、16 种护甲符文及防御估算扩展到法器、盾牌和小圆盾；允许核对两孔起点，普通巧匠石上限一孔。项目 v11 保留合法旧项目，拒绝旧版本夹带副手新操作。
- 装备工坊支持四档钢铁符文（台服鍛鐵符文），与本地防御词缀相加后进入品质乘区；新增符文贡献分解、覆盖导致的防御下降提示。中文原文可核对合计防御符文效果，替换后不双算；项目v10严格回放，旧规则不能冒用新镶嵌，v9品质来源校验保持严格。

- 装备工坊新增起点品质读取与独立核对，以及护甲、闪避、能量护盾的 PoB 快照估算、公式分解和操作前后差值；未知品质、孔位与未支持效果显示原因。规则升级 v9，严格保存品质来源并核对旧项目升级；护甲片动作与游戏取整仍待核实。

- 装备工坊支持独立 `(rune)` 效果行的中英对照、候选选择与三系抗性总和核对；普通前后缀不参与符文贡献，替换后不双算。项目升级 v8，保存起点确认译法并在恢复时重验，旧 v2–v7 不能冒用符文来源字段。
- 装备工坊新增导入防具孔位核对：缺行默认未知，S 仅确定孔数，每孔显式选择空孔或已支持符文；起点已有符文不收费。独立保存核对信息并重查原文，旧 v2–v6 项目不能冒用新声明。
- 装备解析支持独立护甲、闪避面板行及英文 Requires 需求行；导入演练拦截未对应的授予技能，避免未建模技能被忽略。
- 装备工坊新增巧匠石打孔，可从零孔防具连续打孔、镶嵌，展示孔数变化并按普通上限检查全部已有孔。旧规则不能冒用新打孔步骤。
- 符文名称接入三服官方静态 ID 对齐数据，简体与繁体各 771 个名称独立生成；完整覆盖已支持的 12 种符文，修正 Artificer’s Orb 为“巧匠石”，区分技能辅助孔使用的工匠石。
- 装备工坊新增已有空孔设置，以及头盔、胸甲、手套、鞋的 12 种抗性符文镶嵌与覆盖。独立镶嵌效果接入前后对比、耗材与混合制作历史，项目校验符文来源指纹。未核对的复制孔位不再被忽略后进入制作。
- 装备演练接入蜕变、增幅、富豪、崇高、混沌的高级与完美版本，按最低词缀等级及整族最高可用档位例外计算候选；界面提供通货层级、例外说明及具体版本的建议、历史和消耗，拒绝冒用旧规则的新操作。
- 装备制作目标可设置数值上下限，区分档位已存在与数值已达标，保留未知值并提供神圣风险提示；条件随项目保存恢复。完整草稿与历史步骤可展开前后对比，按整组显示新增、移除及显式/固有数值变化。
- 装备演练支持具体数值编辑和范围试掷，神圣可同时重掷普通显式与固有属性并保持词缀档位；结果随历史、撤销重做和项目保存恢复。范围试掷明确标注显示精度、独立等概率的模型假设，技能等级范围暂不参与。旧 v2 项目经来源核对可升级为 v3。
- 装备演练支持中英文搜索目标词缀，按精确档位跟踪达成情况，提示物等、冲突和可开始的基础通货步骤；明确显示移除或重置导致的目标损失。目标随项目保存恢复，建议只打开待确认草稿。
- 装备演练新增混沌与剥离，先按完整词缀组移除，混沌再按变化后的状态选择新增词缀；支持演练项目的本机保存及 `.craft.json` 导入导出，恢复原始装备、操作历史与撤销位置，并拒绝损坏、跨版本或无法合法回放的项目。
- 装备工坊加入蜕变、增幅、富豪、点金、崇高的指定结果演练，可从已知普通装备导入或空白基底开始，逐步选择合法词缀并撤销、重做，统计当前路线的通货枚数。只读和未知特殊状态不能进入普通通货演练。
- 装备目录支持同名基底变体，保留固有属性、基础面板、需求与来源差异；导入可按完整固有属性自动定位，证据不足时保持待选，避免将双玉戒指等装备的不同变体混为一项。
- 装备工坊新增中英文基底搜索、物品等级筛选、词缀范围对照和已有冲突组提示；导入词缀按完整分组及范围对应生成目录，歧义保留待核对。目录固定 PoB2 数据来源及哈希，提供独立重建与校验命令。
- 新增“装备工坊”：支持粘贴简体中文高级装备文本，按词缀组展示中英对照并保存到本机；咒符和传奇装备仅供对照。已验收的符文法器常规稀有词缀组合可转接 CoE，未知内容保留原文并阻止完整转接。
- `build-l10n` 新增装备、技能、天赋内容切换及“中英对照 / 译文”阅读方式；阅读方式不影响下载内容。
- `build-l10n` 待核对纳入基底名、传奇名未收录，支持跨区定位；编号词缀命中数仍独立统计，自由备注不算失败。

### Changed

- 精华枚举与已有工艺授权在单次查询内复用词缀身份索引，空目标来源检查直接返回；减少路线回放和项目校验中的重复目录扫描，保留每次查询的来源及重复身份验证。

- 制作核心改为直接保留独立目标的条件归属，新旧入口共用单条数值校验，消除按词缀类型反查目标的结构限制；实际溶剂转换与合法同类目标继续推进。

- 制作核心增加 v74 独立目标项目读写与旧项目升级，保留失联引用和全部历史；撤销后当前数值暂时无法对应时仍可保存目标。目标、替代档位及失联类型共同核对来源，新存储与编辑入口拒绝会被静默改写的字段。工作台各入口统一使用 v74，旧项目仍可验证后升级。

- 制作核心接入独立目标编辑、建议、路线与条件指引：重排保留身份，删除重加不复用，失联引用须明确修复；目标达成损失与实际档位风险分开报告。工作台目标编辑、条件表单、流程画布及普通与特殊制作面板均使用独立目标；数值暂时无法对应时仍能调整目标，失联条件可明确修复。

- 制作核心增加独立 v73 严格读写入口，核对全部历史及来源，拒绝缺失或错误编号与配置补全；保存前检查会被 JSON 改写的字段。工作台新会话现已启用实例身份，普通与特殊制作选择对应实例。

- 制作核心增加旧项目历史的实例升级候选，先验证原始版本与全部步骤，再核对逐步语义一致性；保留撤销后的未来历史、三语来源及摧毁终止快照。快速恢复、项目文件、收藏及沿用方案统一升级旧项目，失败不降级重试；沿用方案保留接收装备的完整历史与报价，并按新基底核对来源。

- 目标提取核心与选择面板接入词缀实例定位，拒绝同类型重获后的过期选择；快照变化清除草稿，输出仍只复制制作条件，保持旧项目兼容。

- 制作核心增加独立词缀实例身份，新增、移除、神圣与破裂按具体实例处理；v72 项目仍拒绝实例字段，溶剂实际转换待后续接通。
- 精华、合金、液态情感、亵渎及瓦尔顺序替换接入词缀实例定位；候选预演不消费实例编号，实际替换或揭示才分配，孔位及独立腐化保留显式词缀身份。
- 前后对照与路线明细按实例区分词缀保留、转换和重新生成；同类型的新旧实例分别显示移除与新增，跨类型转换保留两侧属性，不推断数值增益。
- 目标分析要求同一词缀实例满足全部数值和破裂条件，建议及路线保留具体移除与掷值对象；搜索按装备实际状态去重，忽略纯编号差异。同类多目标编辑仍待接通。
- 制作核心建立独立目标定义与一对一分配：每个词缀最多计入一个目标，部分达成保留必选破裂目标；旧接口继续返回原类型 ID 与顺序。新目标结构仍使用现有资格校验，不扩大同类词缀的合法共存范围。

- `build-l10n` 重构为原灰阶工作台，统一深浅主题、中文排版、文件区与移动布局，移除金属装饰和覆盖率轨道。
- `build-l10n` 导出选项与主题收进设置，当前文件下载放在构筑标题旁，批量下载归入文件区；设置说明可见，Escape 可关闭并返回焦点。

### Fixed

- 修复四孔腐化装备的条件指引限制：孔数／空孔数范围可选 4，镶嵌动作可指定第四孔；实际孔位仍由制作引擎校验，保存恢复沿用 v78 腐化指引，既有第四孔操作历史继续兼容。

- 修复 Serle 扩容后的条件指引仍限制为六组词缀／三个空后缀的问题；七组与第四后缀条件可编辑、按当前容量匹配及保存恢复，未执行条件同样遵循 v88 来源与旧版本限制。

- 修复珠宝有效值目标路线的搜索退化：保留准备阶段的负优先级，限制多词缀新增时的填充排列，同时保留独立目标所需的数值选项。

- 词缀实例启用入口复核转换后的完整状态；修正保证制作与带预兆待揭示状态在实例模式下误报无可用候选的问题。

- 手动选择的催化比较类型与品质在催化消费、撤销后保留，可一键返回当前品质；默认比较继续跟随当前装备，比较选项不计入制作历史或材料费用。

- 修正未揭示珠宝液态目标仍提示“先完成揭示”的矛盾；尚未固定候选时按新增交错能力说明，其他操作及固定候选阶段保留限制。

- 减少亵渎路线的重复候选校验：固定选项按相关词缀族核对，预兆确认三项合法候选后结束；保留最低等级回退、完整候选展示及现有路线搜索上限。

- 修正君王抗性增效在目标、催化比较和神圣路线中的说明：统一有效值口径，正确标注未命中催化标签的工艺贡献，并提示受增效影响的破裂词缀有效值仍可能变化。

- 液态情感检查仅索引当前珠宝材料引用的词缀，减少连续路线规划的目录分配开销；保留来源与身份校验、候选顺序及搜索上限。

- 修复国服高级腰带文本“咒符位”无法对应交易词典“咒符栏”的问题；保留实际栏数、原范围和独立繁体模板。

- 修正骨骼路线的预计材料展示：未启用深渊回响的固定候选及选择揭示不计材料，仅包含免费步骤的路线显示无需新增材料。

- 普通与当前亵渎候选不再把 Genesis Tree 专用上下文标签当作默认制作资格；腰带和戒指的错误候选被移除，精华的精确保证映射保留。之前含此类不合法普通结果的项目会明确恢复失败。

- 修复基底目录刚加载完成时，初始化定位可能覆盖用户刚选择的基底；搜索与变体选择保持用户选择，新导入仍重新定位。

- 修复缺中文词典条目的已知英文基底，以及中文装备中保留英文授予技能时无法重新导入的问题。复用现有制作目录与英文语法，保留完整来源核对、旧版项目限制和名称歧义；目录加载后更新识别，不额外抓取或重置制作历史。
- `build-l10n` 移除当前文件后自动选中相邻文件，最后一份移除后回到导入入口。
- `build-l10n` 重复下载同一文件会重置提示计时，通知改为“已开始下载”；部分成功时显示实际导出与失败份数。
- `build-l10n` 修复 zip 生成的同名后缀与输入已有文件名冲突时可能覆盖内容的问题。

## [0.3.0] - 2026-09-11

### Added

- `build-l10n` 新增浅色主题：完整的浅色令牌集（表面、文本、品牌金、语义色与 14 个标记语法色逐个重算过 WCAG 对比度，不是深色值的镜像；正文档 ≥4.5:1、图形 ≥3:1，并有一条回归测试守着），顶栏可在「跟随系统 / 浅色 / 深色」三态间切换，选择记在浏览器本地，首帧之前定妆不会闪一下深色。
- `build-l10n` 新增样式死代码扫描脚本 `pnpm --filter @poe2-tools/build-l10n scan-css`：列出零引用的设计令牌与没有任何 `.tsx` 用到的类选择器。不接进 `pnpm verify`（正则匹配会误报），用法见 README。
- 新增 `docs/manual-qa-checklist.md`：Windows Chrome / 微信内置浏览器 / iOS Safari / Windows 高对比度 / 游戏内五组真机走查清单，每项写明看什么、怎么判。
- `build-l10n` 概览卡新增「仅看未命中」筛选：开启后只留有未命中的卡片，其余整块隐藏，被筛空的区块显式说一句「这一区没有未命中」。判据与覆盖率、未命中清单、轨上的缺口完全同源（只算编号行），屏幕上不会出现两个不一样的「未命中」数。
- `build-l10n` 新增两个快捷键：`N` 依次跳到下一处未命中（到底回绕），`F` 切换「仅看未命中」。键位在概览卡里常驻显示（一条未命中都没有时整条不出现，两个键位本来就没有可作用的对象；≤560 手机档没有实体键盘，键位提示整条隐藏），两枚按钮各带 `aria-keyshortcuts`；焦点在输入框里、带 Ctrl / Meta / Alt、处于中文输入法组合期、或是长按连发的重复按键，一律不触发。
- `build-l10n` 下载后给出落地引导条：写清实际文件名与「放进 Build Planner 目录后同名替换」，`role="status"` + `aria-live="polite"` 读屏可闻，4 秒自动消失也可手动关闭，`prefers-reduced-motion` 下不带位移动效。概览轨的每一格另带悬停提示（「戒指 2 · 第 3 行」），命中格也有 hover 反馈；侧栏迷你轨仍是纯装饰。

### Changed

- `build-l10n` 样式表由单个 2300 行的 `styles.css` 拆成 `src/styles/` 下按区分工的 12 个文件（令牌 / 基线 / 控件 / 骨架 / 侧栏 / 空态 / 覆盖率轨 / 概览 / 卡片 / 对照表 / 响应式 / 无障碍兜底），由 `main.tsx` 按级联顺序引入；规则一条都没有挪位，构建产物与拆分前一致。
- 内部清理（无行为变化）：每份文件的逐行行模型只解析一次，侧栏迷你轨与主区对照表共用同一份，两处的第 N 格与第 N 行必定是同一行。
- `build-l10n` 整段都是「原样」的字段（构筑说明、大部分天赋与支援宝石备注）退化成单列纯文本块，不再左右逐字重复一遍，也不再为此摆两条列头；状态由行尾一枚「原样」标签承担。双语模式、有传奇名注入、或含任何未命中的字段不退化。
- `build-l10n` 天赋区改为「名字 | 备注」同行两列（窄屏退回堆叠），同样内容的高度约为改版前的三分之一。
- `build-l10n` 侧栏拖放区的粘贴框收进折叠式「或粘贴内容 ⌄」，拖拽与「选择 .build 文件」两条主路径不再被一个常驻文本框压在下面。
- `build-l10n` 空态大标题由「流放之路 2 构筑汉化」改为「PoE2 构筑汉化」，与顶栏字标统一。hover / 焦点过渡由 120ms 放慢到 160ms，主区卡片进入时带一次淡入（逐张错开 40ms，最多错到第 4 张）；两者在 `prefers-reduced-motion` 下全部归零。

### Fixed

- `build-l10n` 备注里 `<rgb(r,g,b)>` 自定义颜色的自动提亮此前写死了深色底：浅色主题下会把暗色文字提亮成近白、完全不可见。现在一次算出深浅两套达标值，由样式按当前主题选用。
- `build-l10n` 同一行里「先有普通文字、后有编号词缀」时整行不进覆盖率轨（轨上少一格、未命中时清单里有而轨上没有缺口）。
- `build-l10n` 对照表的两条列头此前用纯拉丁字体承载中文，Windows 上会掉成宋体、与全站不同族，且字距给每个汉字加了尾空。
- `build-l10n` 词典状态徽章此前对读屏完全静默（三态切换不播报，而失败态是唯一需要用户动手的状态）；分段控件的可见焦点此前完全依赖 `:has()`，在微信内置浏览器一类老内核上键盘切换目标语言没有任何焦点提示。
- `build-l10n` 一个文件都没有时，禁用的「全部下载」按钮的悬停提示不再说「打包下载 0 个文件」。
- `build-l10n` 版本号形式的文本（`v0.5.5`）不再被当成数值高亮；「原样」状态不再给英文格挂一个零样式的类；命中行的序号格不再同时是 `aria-hidden` 与可聚焦元素。
- `build-l10n` ≤560 手机档：侧栏折叠成可展开抽屉（默认收起，首屏直接给概览卡与下载按钮，选中文件后自动收起并把焦点交还给抽屉开关），粘贴框字号抬到 16px 避免 iOS 聚焦时整页放大，高度改用 `100dvh`，顶栏与主区补上刘海屏安全区内边距，本期新增的折叠开关 / 筛选 / 关闭 / 抽屉四个控件一并进 44px 触控名单。390 / 360 / 320 三档走查横向溢出为 0。
- `build-l10n` 空态底部脚注的分隔竖线在换行后不再悬在行尾（改由伪元素生成）。
- `build-l10n` 筛选开着时换一份全命中的文件，不再闪过一帧「三区全是这一区没有未命中」；「仅看未命中」的按下态在 Windows 高对比度模式下补了一圈轮廓，不再只靠颜色表达。
- `build-l10n` 首帧定妆脚本在 `localStorage` 取值抛异常时（隐私模式、Chrome「阻止所有 Cookie」、部分 WebView）会硬回落深色，与 `useTheme` 的「跟随系统」不一致，系统是浅色的用户仍会看到首帧深色再跳浅色；系统探测已挪出 `try`，两边逐分支等价，并有一条测试直接执行 `index.html` 里那段脚本守着。
- `build-l10n` 选中态、概览卡四角刻痕、对照表中缝、未命中标签与清单条目边框、覆盖率轨的格顶高光、「或粘贴内容」虚线下划线等 19 处强调色此前是写死在组件规则里的深色档字面量，浅色主题下合成后只有 1.04–1.57:1、整层看不见（「仅看未命中」的按下态更是破了 WCAG 1.4.11 的 3:1）；现在收成 11 个语义令牌，浅色档逐个重算，凡承担状态或边界职责的一律 ≥3:1，并纳入主题门禁。
- `build-l10n` 浅色主题下拖选文本几乎看不出选中了哪一段（1.14:1），而「对着原文列复制英文词缀去查」是本站的主动线之一；选区改用实色底 + 反白文字，选区底对五档表面 5.3–7.3:1，选中的字 7.3:1，深色档观感不变。

## [0.2.0] - 2026-09-10

### Added

- 词缀匹配增加 reduced / increased 对调回退：交易站词典只登记 increased 形式，攻略里 `25% reduced Attribute Requirements` 这类写法现在按对调后的模板命中，译文表述词一并对调（zh-CN 提高→降低，zh-TW 增加→減少）；英文或译文里表述词不唯一时仍按未命中处理。
- `build-l10n` 顶栏新增词典状态徽章：就绪 / 加载中 / 失败三态各有图标与颜色，失败时徽章内有「重试」、主区错误卡有「重新加载词典」，不再只有页脚一行小字（补上此前唯一没有恢复路径的错误态）；词典失败又还没有文件时，错误卡下面照样给拖放区，页面不会一个入口都没有。
- `build-l10n` 首屏新增空态引导区：产品名 + 一句话说清工具做什么、三步说明「导出 → 核对 → 放回」、大号拖放区与「文件只在你的浏览器里解析，不会上传到任何服务器」的信任行。
- `build-l10n` 自托管 Cinzel 拉丁子集（SIL OFL 1.1，约 16 KB，随站发布、零第三方请求），用于英文眉标、三步罗马数字与覆盖率数字；许可与子集配方见 README「字体与许可」。
- `build-l10n` 新增内联 SVG 图标集（12 个，零依赖零请求），用于顶栏、拖放区、文件列表与状态徽章。
- `build-l10n` 概览卡新增覆盖率轨：一格一条编号行，实心矮格为命中、空心高格为未命中，点缺口或清单里的「定位」直接跳到那一行（键盘同样可达）；未命中清单从 `inventory_slots[4].additional_text` 这种 JSON 路径改成「戒指 2 · 第 3 行」的人话定位。侧栏每个文件行也带一条同源的迷你轨，多文件时一眼看出哪份缺口多。
- `build-l10n` 概览卡顶部新增「下载此文件」与「放进 Build Planner 目录，同名替换即可」，补上任务流最后一步此前的沉默。
- `build-l10n` 预览还原备注里的标记语法颜色（`<red>{…}`、`<rgb(r,g,b)>{…}` 等，支持嵌套与跨行），色值按「保色相、只提明度」校准到 WCAG AA；未知标签原样透传文字。对照表里的数值单独高亮：原文列比正文亮一档、译文列用金色，扫一行就能对上数字。

### Changed

- `build-l10n` 界面第一期改版：样式表重写为分层设计令牌（暖近黑表面 + 发丝金描边 + 语义色与品牌金分家），正文基准字号 14→15px、左英右中对照区 11.9→14px，顶栏字标由内部包名 `build-l10n` 改为「PoE2 构筑汉化」并加中文副标题，「全部下载」与文件行「下载」升为主按钮，对照区改为一块面板加一条中缝。站点补 `description`、OG 与 Twitter 卡片字段、内联 SVG favicon（不新增任何网络请求）。
- `build-l10n` 界面第二期 A：顶栏切成「品牌区 | 控制区 | 状态区」三段，目标语言从下拉框换成分段控件（两个选项直接可见），两个开关改自绘方框并用 `aria-describedby` 挂上一句人话说明；页脚取消，词典版本降级到侧栏脚注；文件列表改两行式卡片（状态图标 + 文件名 + 覆盖率 / 下载主按钮 + 移除幽灵按钮）。
- `build-l10n` 调色向设计稿收口：控件不再罩一圈去饱和卡其描边（带文字标签的控件改用发丝金边，只有粘贴框与拖放区这类「边框即唯一识别手段」的控件保留 ≥3:1 的功能性边界），页面底色补一层暖光晕与细斜纹。
- `build-l10n` 界面第二期 B：左英右中的整段预览改成逐行对照表（序号 / 原文 / 序号 / 译文四列，一条源行一行，左右天然同高），槽位与宝石卡加卡头三段式（中文名 + 稀有度标签 + 编号行计数 + `inventory_id` / `Lv 52–100`），区块标题改成英文眉标 + 中文标题 + 计数。译文列显式标注 `lang`，繁体（台服）第一次用上正确的字形，且与简体同走等宽栈、数字仍然对齐。
- `build-l10n` 三枚「下载」定主次：概览卡的「下载此文件」是主按钮（主场景是导入即下载），顶栏「全部下载」管多文件批量，侧栏文件行那枚降为次级幽灵按钮，视线不再被三枚同权按钮打散。
- `build-l10n` 覆盖率轨的格宽上限分档（概览轨 52px、侧栏迷你轨 22px），概览轨不再缩在中列左边一小截。
- 内部清理（无行为变化）：删掉 `apps/build-l10n` 里生产代码零引用的 `preview/lines.ts`，以及界面零消费的 `TranslatedFile.unmatched` 字段——未命中清单统一由 `preview/locate.ts` 从行模型里取。

### Fixed

- `build-l10n` 窄屏（≤900px）下侧栏被主区挤成一条缝、拖放区看不见：窄屏改为整页文档滚动，并把断点从 1 档补到 1280 / 900 / 560 三档；新增 `:focus-visible` 焦点环、`prefers-reduced-motion` 与 `forced-colors` 兜底；解析错误文案对比度从 3.2:1 提到 5.5:1。
- `build-l10n` 空态左右两句互相矛盾的文案（主区「选择左侧文件查看对照预览」与侧栏「还没有文件」并存，且窄屏单列下「左侧」是错的）：空态改为整屏引导区、侧栏折叠，结构上不可能再并存。
- `build-l10n` 目标语言分段控件在 `forced-colors` 下不再强制丢弃系统色回落：删掉 `.seg__item--on` 的 `forced-color-adjust: none`，退出强制配色后改由 Windows 高对比度主题接管文字颜色，避免金字在高对比度亮色主题下对比度只剩约 1.9:1。
- `build-l10n` 未命中只在英文列标记、译文列毫无提示（用户看右边那份「成品」时无从知道哪几行其实没翻）：现在左右两列同时标记，并有序号变叹号、左侧竖条、底纹、行尾「未命中 · 保留英文」文字签四重非颜色冗余。
- `build-l10n` 双语模式与传奇名注入下左右行数错位：保留的英文原行归属到同一行内渲染、注入的传奇名单独成行，行数永远等于源行数。
- `build-l10n` 作者写的自由文本此前被完全沉默处理（升级期构筑里超过一半的行是自由文本，容易被读成漏翻）：现在显式标「原样」；而「基底名没在词典里命中」不再混进「原样」，按未命中如实标出。
- `build-l10n` 没有备注的槽位不再渲染成只剩标题的空卡片，改为显式一行「这个槽位没有备注」。
- `build-l10n` 窄屏（≤900px）对照表单列堆叠时，两条列头会挨在一起、下面的内容再逐行中英交替，读者对不上哪条标签管哪一行：窄屏改为隐藏列头，用译文行左侧的古铜竖条与缩进标出「这一行是译文」。
- `build-l10n` 普通装备的基底名不再被染成传奇橙：默认取正文色，只有传奇装备的卡片才用稀有度色，未命中的基底名仍是琥珀。
- `build-l10n` 侧栏文件行的迷你覆盖率轨改成纯装饰：它的缺口格在窄屏只有约 10px 高，够不到 44px 触控目标，跳转统一由概览轨与未命中清单承担；轨的读屏名称也带上文件名，多文件时不再有一串同名的「共 N 条编号行…」。
- `build-l10n` Windows 高对比度（`forced-colors: active`）下覆盖率轨的缺口与图例里的空心样本整块消失（`box-shadow` 在这一模式下被丢弃）：改用 `border` 画同一个框。
- `build-l10n` 传奇槽位只有 `unique_name`、没有 `additional_text` 时预览完全沉默（命中丢英文名、未命中显示「这个槽位没有备注」，相对 0.1.0 是能力回退）：卡头现在显示英文名与译名，未命中用琥珀标签与 `role="note"` 标出，不进覆盖率分母。

## [0.1.0] - 2026-09-09

### Added

- `build-l10n` 静态站：拖入 / 选择 / 粘贴官方 Build Planner `.build` 文件，按 zh-CN（国服术语）或 zh-TW（台服术语）翻译备注里的基底名、传奇名与编号词缀行；槽位 / 宝石 / 天赋左英右中对照预览，未命中行高亮与覆盖率；下载中文 `.build`（多文件打 zip）；输出缩进跟随输入、文件名保持原名。
- 可选：传奇名注入（默认开）、双语模式（默认关）。
- 词典（词缀、天赋、宝石、物品基底与传奇、升华、职业、槽位）随站发布，浏览器零第三方请求。
